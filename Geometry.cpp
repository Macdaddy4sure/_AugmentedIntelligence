/*
    Copyright(C) 2025 Tyler Crockett | Macdaddy4sure.ai

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissionsand
    limitations under the License.
*/

#include "AugmentedIntelligence.hpp"
#include "Geometry.hpp"
#include "Large Language Models.hpp"
#include "Parsers.hpp"
#include "Settings.hpp"
#include "Thought.hpp"
#include "Time.hpp"
#include "Utilities.hpp"
#include "Working-Memory.hpp"

using namespace std;

vector<string> _Geometry::GeometryTermsClassification(string text)
{
    vector<string> geometry_terms(100);
    string temp;

    if (_Geometry::Terms::AATriangleSimilarity(text) != "NULL")
    {
        temp = "AA Triangle Similarity";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AASTriangleCongruence(text) != "NULL")
    {
        temp = "AAS Triangle Congruence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ASATriangleCongruence(text) != "NULL")
    {
        temp = "ASA Triangle Congruence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Abscissa(text) != "NULL")
    {
        temp = "Abscissa";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AcuteAngle(text) != "NULL")
    {
        temp = "Acute Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AcuteTriangle(text) != "NULL")
    {
        temp = "Acute Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AdditivePropertyOfEquality(text) != "NULL")
    {
        temp = "Additive Property Of Equality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AdjacentAngles(text) != "NULL")
    {
        temp = "Adjacent Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AdjacentSides(text) != "NULL")
    {
        temp = "Adjacent Sides";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AlgebraicRepresentation(text) != "NULL")
    {
        temp = "Algebraic Representation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Algorithm(text) != "NULL")
    {
        temp = "Algorithm";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AlternateInteriorAngles(text) != "NULL")
    {
        temp = "Alternate Interior Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AltitudeOfACone(text) != "NULL")
    {
        temp = "Altitude Of A Cone";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AltitudeOfACylinder(text) != "NULL")
    {
        temp = "Altitude Of A Cylinder";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AltitudeOfAPrism(text) != "NULL")
    {
        temp = "Altitude Of APrism";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AltitudeOfAPyramid(text) != "NULL")
    {
        temp = "Altitude Of A Pyramid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AltitudeOfATrapezoid(text) != "NULL")
    {
        temp = "Altitude Of A Trapezoid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AltitudeOfATriangle(text) != "NULL")
    {
        temp = "Altitude Of A Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AnalyticalGeometry(text) != "NULL")
    {
        temp = "Analytical Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AnalyticalGeometricProof(text) != "NULL")
    {
        temp = "Analytical Geometric Proof";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AnalyticalRepresentationOfATransformation(text) != "NULL")
    {
        temp = "Analytical Representation OfATransformation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Analyze(text) != "NULL")
    {
        temp = "Analyze";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AngleBisector(text) != "NULL")
    {
        temp = "Angle Bisector";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AngleMeasure(text) != "NULL")
    {
        temp = "Angle Measure";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Antecedent(text) != "NULL")
    {
        temp = "Antecedent";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Apothem(text) != "NULL")
    {
        temp = "Apothem";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ArcLength(text) != "NULL")
    {
        temp = "Arc Length";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ArcMeasure(text) != "NULL")
    {
        temp = "Arc Measure";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ArcOfACircle(text) != "NULL")
    {
        temp = "Arc Of A Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AreaOfAPolygon(text) != "NULL")
    {
        temp = "Area Of A Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Argument(text) != "NULL")
    {
        temp = "Argument";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Axiom(text) != "NULL")
    {
        temp = "Axiom";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::AxisOfSymmetry(text) != "NULL")
    {
        temp = "Axis Of Symmetry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Base(text) != "NULL")
    {
        temp = "Base";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Betweeness(text) != "NULL")
    {
        temp = "Betweeness";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Biconditional(text) != "NULL")
    {
        temp = "Biconditional";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CartesianCoordinates(text) != "NULL")
    {
        temp = "Cartesian Coordinates";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CartesianPlane(text) != "NULL")
    {
        temp = "Cartesian Plane";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CenterOfADilation(text) != "NULL")
    {
        temp = "Center Of A Dilation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CenterOfGravity(text) != "NULL")
    {
        temp = "Center Of Gravity";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CenterOfARegularPolygon(text) != "NULL")
    {
        temp = "Center Of A Regular Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CenterOfARotation(text) != "NULL")
    {
        temp = "Center Of A Rotation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CentralAngle(text) != "NULL")
    {
        temp = "Central Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CentralAngleOfARegularPolygon(text) != "NULL")
    {
        temp = "Central Angle Of A RegularPolygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Centroid(text) != "NULL")
    {
        temp = "Centroid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Chord(text) != "NULL")
    {
        temp = "Chord";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Circle(text) != "NULL")
    {
        temp = "Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Circumcenter(text) != "NULL")
    {
        temp = "Circumcenter";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Circumcircle(text) != "NULL")
    {
        temp = "Circumcircle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Circumference(text) != "NULL")
    {
        temp = "Circumference";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Clockwise(text) != "NULL")
    {
        temp = "Clockwise";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Closure(text) != "NULL")
    {
        temp = "Closure";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CollinearPoints(text) != "NULL")
    {
        temp = "Collinear Points";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CommonTangents(text) != "NULL")
    {
        temp = "Common Tangents";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ComplementaryAngles(text) != "NULL")
    {
        temp = "Complementary Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CompositionOfFunctions(text) != "NULL")
    {
        temp = "Composition Of Functions";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CompoundLocus(text) != "NULL")
    {
        temp = "Compound Locus";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CompoundStatement(text) != "NULL")
    {
        temp = "Compound Statement";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConcavePolygon(text) != "NULL")
    {
        temp = "Concave Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConcentricCircles(text) != "NULL")
    {
        temp = "Concentric Circles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Conclusion(text) != "NULL")
    {
        temp = "Conclusion";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Concurrence(text) != "NULL")
    {
        temp = "Concurrence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConditionalStatement(text) != "NULL")
    {
        temp = "Conditional Statement";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Cone(text) != "NULL")
    {
        temp = "Cone";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Conjecture(text) != "NULL")
    {
        temp = "Conjecture";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Congruent(text) != "NULL")
    {
        temp = "Congruent";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConicSections(text) != "NULL")
    {
        temp = "ConicSections";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Consistency(text) != "NULL")
    {
        temp = "Consistency";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConstantOfProportionality(text) != "NULL")
    {
        temp = "Constant Of Proportionality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Construct(text) != "NULL")
    {
        temp = "Construct";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Constraints(text) != "NULL")
    {
        temp = "Constraints";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Contradiction(text) != "NULL")
    {
        temp = "Contradiction";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ContrapositiveOfAStatement(text) != "NULL")
    {
        temp = "Contrapositive Of A Statement";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConverseOfAStatement(text) != "NULL")
    {
        temp = "Converse Of A Statement";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ConvexPolygon(text) != "NULL")
    {
        temp = "Convex Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CoordinateGeometry(text) != "NULL")
    {
        temp = "Coordinate Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CoordinatePlane(text) != "NULL")
    {
        temp = "Coordinate Plane";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Coplanar(text) != "NULL")
    {
        temp = "Coplanar";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CorrespondingAngles(text) != "NULL")
    {
        temp = "Corresponding Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::CorrespondingParts(text) != "NULL")
    {
        temp = "Corresponding Parts";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Counterclockwise(text) != "NULL")
    {
        temp = "Counterclockwise";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Crossection(text) != "NULL")
    {
        temp = "Crossection";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Cube(text) != "NULL")
    {
        temp = "Cube";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Decagon(text) != "NULL")
    {
        temp = "Decagon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DeductiveReasoning(text) != "NULL")
    {
        temp = "Deductive Reasoning";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Diagonal(text) != "NULL")
    {
        temp = "Diagonal";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Diameter(text) != "NULL")
    {
        temp = "Diameter";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DiameterOfASphere(text) != "NULL")
    {
        temp = "Diameter Of A Sphere";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DihedralAngle(text) != "NULL")
    {
        temp = "Dihedral Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Dilation(text) != "NULL")
    {
        temp = "Dilation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DirectTransformation(text) != "NULL")
    {
        temp = "Direct Transformation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DistanceBetweenTwoPoints(text) != "NULL")
    {
        temp = "Distance Between Two Points";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DistanceBetweenAPointAndALine(text) != "NULL")
    {
        temp = "Distance Between A Point And A Line";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DistanceBetweenTwoParallelLines(text) != "NULL")
    {
        temp = "Distance Between Two Parallel Lines";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Dodecahedron(text) != "NULL")
    {
        temp = "Dodecahedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::DynamicGeometrySoftware(text) != "NULL")
    {
        temp = "Dynamic Geometry Software";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EdgeOfAPolyhedron(text) != "NULL")
    {
        temp = "Edge Of A Polyhedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Ellipse(text) != "NULL")
    {
        temp = "Ellipse";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Endpoint(text) != "NULL")
    {
        temp = "Endpoint";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Equiangular(text) != "NULL")
    {
        temp = "Equiangular";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Equidistant(text) != "NULL")
    {
        temp = "Equidistant";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EquilateralPolygon(text) != "NULL")
    {
        temp = "Equilateral Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EquilateralTriangle(text) != "NULL")
    {
        temp = "Equilateral Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EquivalenceRelation(text) != "NULL")
    {
        temp = "Equivalence Relation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EuclideanGeometry(text) != "NULL")
    {
        temp = "Euclidean Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EuclideanParallelPostulate(text) != "NULL")
    {
        temp = "Euclidean Parallel Postulate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::EulerLine(text) != "NULL")
    {
        temp = "Euler Line";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::staticalSegmentOfASecant(text) != "NULL")
    {
        temp = "statical Segment Of A Secant";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ExteriorOfAGeometricFigure(text) != "NULL")
    {
        temp = "Exterior Of A Geometric Figure";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ExteriorAngleOfAPolygon(text) != "NULL")
    {
        temp = "Exterior Angle Of A Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::FaceOfAPolyhedron(text) != "NULL")
    {
        temp = "Face Of A Polyhedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::FixedPoint(text) != "NULL")
    {
        temp = "Fixed Point";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::FootOfAnAltitude(text) != "NULL")
    {
        temp = "Foot Of An Altitude";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Function(text) != "NULL")
    {
        temp = "Function";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GeometricInequality(text) != "NULL")
    {
        temp = "Geometric Inequality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GeometricMean(text) != "NULL")
    {
        temp = "Geometric Mean";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GeometricProbability(text) != "NULL")
    {
        temp = "Geometric Probability";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GeometricRepresentationOfTheCircularFunctions(text) != "NULL")
    {
        temp = "Geometric Representation Of The Circular Functions";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GeometricSequence(text) != "NULL")
    {
        temp = "Geometric Sequence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Geometry(text) != "NULL")
    {
        temp = "Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GlideReflection(text) != "NULL")
    {
        temp = "Glide Reflection";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GoldenRatio(text) != "NULL")
    {
        temp = "Golden Ratio";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GoldenRectangle(text) != "NULL")
    {
        temp = "Golden Rectangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GraphicalRepresentation(text) != "NULL")
    {
        temp = "Graphical Representation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::GreatCircle(text) != "NULL")
    {
        temp = "Great Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::HalfTurn(text) != "NULL")
    {
        temp = "Half Turn";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Hemisphere(text) != "NULL")
    {
        temp = "Hemisphere";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::HeronsFormula(text) != "NULL")
    {
        temp = "Herons Formula";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Hexagon(text) != "NULL")
    {
        temp = "Hexagon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Hyperbola(text) != "NULL")
    {
        temp = "Hyperbola";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Hypotenuse(text) != "NULL")
    {
        temp = "Hypotenuse";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::HypotenuseAndLegTriangleCongruence(text) != "NULL")
    {
        temp = "Hypotenuse And Leg Triangle Congruence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Hypothesis(text) != "NULL")
    {
        temp = "Hypothesis";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Icosahedron(text) != "NULL")
    {
        temp = "Icosahedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IdentityElements(text) != "NULL")
    {
        temp = "Identity Elements";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Image(text) != "NULL")
    {
        temp = "Image";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IncenterOfATriangle(text) != "NULL")
    {
        temp = "Incenter Of A Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IncludedAngle(text) != "NULL")
    {
        temp = "Included Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IndirectProof(text) != "NULL")
    {
        temp = "Indirect Proof";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::InductiveReasoning(text) != "NULL")
    {
        temp = "Inductive Reasoning";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::InscribedAngle(text) != "NULL")
    {
        temp = "Inscribed Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::InscribedCircle(text) != "NULL")
    {
        temp = "Inscribed Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::InterceptedArc(text) != "NULL")
    {
        temp = "Intercepted Arc";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Interior(text) != "NULL")
    {
        temp = "Interior";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IntersectingLines(text) != "NULL")
    {
        temp = "Intersecting Lines";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IntersectionOfSets(text) != "NULL")
    {
        temp = "Intersection Of Sets";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Invariant(text) != "NULL")
    {
        temp = "Invariant";
        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }

    }
    if (_Geometry::Terms::InverseOfAStatement(text) != "NULL")
    {
        temp = "Inverse Of A Statement";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Isometry(text) != "NULL")
    {
        temp = "Isometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IsoscelesTrapezoid(text) != "NULL")
    {
        temp = "Isosceles Trapezoid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::IsoscelesTriangle(text) != "NULL")
    {
        temp = "Isosceles Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LateralAreaOfAPrism(text) != "NULL")
    {
        temp = "Lateral Area Of A Prism";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LateralEdge(text) != "NULL")
    {
        temp = "Lateral Edge";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LateralFace(text) != "NULL")
    {
        temp = "Lateral Face";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LengthOfLineSegment(text) != "NULL")
    {
        temp = "Length Of Line Segment";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LineSegment(text) != "NULL")
    {
        temp = "Line Segment";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LineSymmetry(text) != "NULL")
    {
        temp = "Line Symmetry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LinearPairOfAngles(text) != "NULL")
    {
        temp = "Linear Pair Of Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LocusOfPoints(text) != "NULL")
    {
        temp = "Locus Of Points";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::LogicalEquivalence(text) != "NULL")
    {
        temp = "Logical Equivalence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::MajorArc(text) != "NULL")
    {
        temp = "Major Arc";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::MeanProportional(text) != "NULL")
    {
        temp = "Mean Proportional";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::MeasureOfAnArc(text) != "NULL")
    {
        temp = "Measure Of AnArc";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::MedianOfATrapezoid(text) != "NULL")
    {
        temp = "Median Of A Trapezoid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::MedianOfATriangle(text) != "NULL")
    {
        temp = "Median Of A Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Midpoint(text) != "NULL")
    {
        temp = "Midpoint";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Midsegment(text) != "NULL")
    {
        temp = "Midsegment";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::MinorArc(text) != "NULL")
    {
        temp = "MinorArc";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Negation(text) != "NULL")
    {
        temp = "Negation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Net(text) != "NULL")
    {
        temp = "Net";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::NGon(text) != "NULL")
    {
        temp = "N-Gon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::NonCollinearPoints(text) != "NULL")
    {
        temp = "Non Collinear Points";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::NonCoplanarPoints(text) != "NULL")
    {
        temp = "Non Coplanar Points";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::NonEuclideanGeometry(text) != "NULL")
    {
        temp = "Non-Euclidean Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RiemannianGeometry(text) != "NULL")
    {
        temp = "Riemannian Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::HyperbolicGeometry(text) != "NULL")
    {
        temp = "Hyperbolic Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ObliqueLineAndAPlane(text) != "NULL")
    {
        temp = "ObliqueLine And A Plane";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ObtuseAngle(text) != "NULL")
    {
        temp = "Obtuse Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ObtuseTriangle(text) != "NULL")
    {
        temp = "Obtuse Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Octagon(text) != "NULL")
    {
        temp = "Octagon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Octahedron(text) != "NULL")
    {
        temp = "Octahedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::OneToOneFunction(text) != "NULL")
    {
        temp = "One To One Function";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::OppositeRays(text) != "NULL")
    {
        temp = "Opposite Rays";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::OppositeSideInARightTriangle(text) != "NULL")
    {
        temp = "Opposite Side In A Right Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::OppositeTransformation(text) != "NULL")
    {
        temp = "Opposite Trans formation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::OrderedPair(text) != "NULL")
    {
        temp = "Ordered Pair";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::OrderedTriple(text) != "NULL")
    {
        temp = "Ordered Triple";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Ordinate(text) != "NULL")
    {
        temp = "Ordinate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Orientation(text) != "NULL")
    {
        temp = "Orientation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Origin(text) != "NULL")
    {
        temp = "Origin";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Orthocenter(text) != "NULL")
    {
        temp = "Orthocenter";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Parabola(text) != "NULL")
    {
        temp = "Parabola";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ParagraphProof(text) != "NULL")
    {
        temp = "Paragraph Proof";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ParallelPiped(text) != "NULL")
    {
        temp = "Parallel Piped";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ParallelPlanes(text) != "NULL")
    {
        temp = "Parallel Planes";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ParallelPostulate(text) != "NULL")
    {
        temp = "Parallel Postulate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Parallelogram(text) != "NULL")
    {
        temp = "Parallelogram";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Parameter(text) != "NULL")
    {
        temp = "Parameter";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Pentagon(text) != "NULL")
    {
        temp = "Pentagon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Perimeter(text) != "NULL")
    {
        temp = "Perimeter";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PerpendicularBisector(text) != "NULL")
    {
        temp = "Perpendicular Bisector";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PerpendicularLines(text) != "NULL")
    {
        temp = "Perpendicular Lines";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PerpendicularPlanes(text) != "NULL")
    {
        temp = "Perpendicular Planes";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Pi(text) != "NULL")
    {
        temp = "Pi";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Plane(text) != "NULL")
    {
        temp = "Plane";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PlatonicSolids(text) != "NULL")
    {
        temp = "PlatonicSolids";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Point(text) != "NULL")
    {
        temp = "Point";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PointOfConcurrency(text) != "NULL")
    {
        temp = "Point Of Concurrency";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PointOfTangency(text) != "NULL")
    {
        temp = "Point Of Tangency";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PointSlopeEquationOfALine(text) != "NULL")
    {
        temp = "Point Slope Equation Of A Line";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PointSymmetry(text) != "NULL")
    {
        temp = "Point Symmetry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Polygon(text) != "NULL")
    {
        temp = "Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Polyhedron(text) != "NULL")
    {
        temp = "Polyhedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PositionVector(text) != "NULL")
    {
        temp = "PositionVector";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Postulate(text) != "NULL")
    {
        temp = "Postulate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Preimage(text) != "NULL")
    {
        temp = "Preimage";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Premise(text) != "NULL")
    {
        temp = "Premise";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ProofByContradiction(text) != "NULL")
    {
        temp = "Proof By Contradiction";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Proportional(text) != "NULL")
    {
        temp = "Proportional";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Pyramid(text) != "NULL")
    {
        temp = "Pyramid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::PythagoreanTheorem(text) != "NULL")
    {
        temp = "Pythagorean Theorem";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Quadrant(text) != "NULL")
    {
        temp = "Quadrant";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::QuadraticEquation(text) != "NULL")
    {
        temp = "Quadratic Equation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Quadrilateral(text) != "NULL")
    {
        temp = "Quadrilateral";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Radical(text) != "NULL")
    {
        temp = "Radical";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Radius(text) != "NULL")
    {
        temp = "Radius";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Ray(text) != "NULL")
    {
        temp = "Ray";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Reason(text) != "NULL")
    {
        temp = "Reason";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Rectangle(text) != "NULL")
    {
        temp = "Rectangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RectangularCoordinates(text) != "NULL")
    {
        temp = "Rectangular Coordinates";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Reflection(text) != "NULL")
    {
        temp = "Reflection";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ReflexivePropertyOfEquality(text) != "NULL")
    {
        temp = "Reflexive Property Of Equality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RegularPolygon(text) != "NULL")
    {
        temp = "Regular Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RegularPyramid(text) != "NULL")
    {
        temp = "Regular Pyramid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RemoteInteriorAngles(text) != "NULL")
    {
        temp = "Remote Interior Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RestrictedDomain(text) != "NULL")
    {
        temp = "Restricted Domain";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Rhombus(text) != "NULL")
    {
        temp = "Rhombus";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RightAngle(text) != "NULL")
    {
        temp = "Right Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RightCircularCylinder(text) != "NULL")
    {
        temp = "Right Circular Cylinder";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RightCircularCone(text) != "NULL")
    {
        temp = "Right CircularCone";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RightPyramid(text) != "NULL")
    {
        temp = "Right Pyramid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RightTriangle(text) != "NULL")
    {
        temp = "Right Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Rotation(text) != "NULL")
    {
        temp = "Rotation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::RotationalSymmetry(text) != "NULL")
    {
        temp = "Rotational Symmetry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SASTriangleCongruence(text) != "NULL")
    {
        temp = "SAS Triangle Congruence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SASSimilarityTheorem(text) != "NULL")
    {
        temp = "SAS Similarity Theorem";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ScaleneTriangle(text) != "NULL")
    {
        temp = "Scalene Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SecantOfACircle(text) != "NULL")
    {
        temp = "Secant Of A Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SectorOfACircle(text) != "NULL")
    {
        temp = "Sector Of A Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SegmentOfACircle(text) != "NULL")
    {
        temp = "Segment Of A Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SemiCircle(text) != "NULL")
    {
        temp = "Semi Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Set(text) != "NULL")
    {
        temp = "Set";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SimilarPolygons(text) != "NULL")
    {
        temp = "Similar Polygons";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SkewLines(text) != "NULL")
    {
        temp = "SkewLines";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SlantHeight(text) != "NULL")
    {
        temp = "Slant Height";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SlantHeightOfACone(text) != "NULL")
    {
        temp = "Slant Height Of A Cone";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Slope(text) != "NULL")
    {
        temp = "Slope";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SlopeEquation(text) != "NULL")
    {
        temp = "Slope Equation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Sphere(text) != "NULL")
    {
        temp = "Sphere";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Square(text) != "NULL")
    {
        temp = "Square";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SSSTriangleCongruence(text) != "NULL")
    {
        temp = "SSS Triangle Congruence";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Straightedge(text) != "NULL")
    {
        temp = "Straightedge";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SubstitutionProperty(text) != "NULL")
    {
        temp = "Substitution Property";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SubtractionPropertyOfEquality(text) != "NULL")
    {
        temp = "Subtraction Property Of Equality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SupplementaryAngles(text) != "NULL")
    {
        temp = "Supplementary Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SurfaceArea(text) != "NULL")
    {
        temp = "Surface Area";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::SymmetricPropertyOfEquality(text) != "NULL")
    {
        temp = "Symmetric Property Of Equality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::InternalTangentCircles(text) != "NULL")
    {
        temp = "Internal Tangent Circles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::staticalTangentCircles(text) != "NULL")
    {
        temp = "statical Tangent Circles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TangentLineToACircle(text) != "NULL")
    {
        temp = "Tangent Line To A Circle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TangentSegment(text) != "NULL")
    {
        temp = "Tangent Segment";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Tessellation(text) != "NULL")
    {
        temp = "Tessellation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Tetrahedron(text) != "NULL")
    {
        temp = "Tetrahedron";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Theorem(text) != "NULL")
    {
        temp = "Theorem";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ThreeDimensionalSpace(text) != "NULL")
    {
        temp = "Three Dimensional Space";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Transformation(text) != "NULL")
    {
        temp = "Transformation";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TransformationalGeometry(text) != "NULL")
    {
        temp = "Transformational Geometry";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TransformationalProof(text) != "NULL")
    {
        temp = "Transformational Proof";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TransitivePropertyOfEquality(text) != "NULL")
    {
        temp = "Transitive Property Of Equality";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Transversal(text) != "NULL")
    {
        temp = "Transversal";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Trapezoid(text) != "NULL")
    {
        temp = "Trapezoid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TriangleInequalityTheorem(text) != "NULL")
    {
        temp = "Triangle Inequality Theorem";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TrichotomyProperty(text) != "NULL")
    {
        temp = "Trichotomy Property";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TrigonometryOfTheRightTriangle(text) != "NULL")
    {
        temp = "Trigonometry Of The Right Triangle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TruthValue(text) != "NULL")
    {
        temp = "Truth Value";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TwoColumnProof(text) != "NULL")
    {
        temp = "Two Column Proof";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::TwoDimensionalSpace(text) != "NULL")
    {
        temp = "Two Dimensional Space";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::UndefinedTerms(text) != "NULL")
    {
        temp = "Undefined Terms";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::UnionOfSets(text) != "NULL")
    {
        temp = "Union Of Sets";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ValidArgument(text) != "NULL")
    {
        temp = "Valid Argument";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Vector(text) != "NULL")
    {
        temp = "Vector";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::VertexOfAnAngle(text) != "NULL")
    {
        temp = "Vertex Of An Angle";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::VertexOfAPolygon(text) != "NULL")
    {
        temp = "Vertex Of A Polygon";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::VertexOfAConeOrPyramid(text) != "NULL")
    {
        temp = "Vertex Of A Cone Or Pyramid";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::VerticalAngles(text) != "NULL")
    {
        temp = "Vertical Angles";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::Volume(text) != "NULL")
    {
        temp = "Volume";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::XAxis(text) != "NULL")
    {
        temp = "X Axis";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::XCoordinate(text) != "NULL")
    {
        temp = "X Coordinate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::XIntercept(text) != "NULL")
    {
        temp = "X Intercept";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::YAxis(text) != "NULL")
    {
        temp = "Y Axis";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::YCoordinate(text) != "NULL")
    {
        temp = "Y Coordinate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::YIntercept(text) != "NULL")
    {
        temp = "Y Intercept";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ZAxis(text) != "NULL")
    {
        temp = "Z Axis";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ZCoordinate(text) != "NULL")
    {
        temp = "Z Coordinate";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }
    if (_Geometry::Terms::ZeroProductProperty(text) != "NULL")
    {
        temp = "Zero Product Property";

        for (int x = 0; x <= 100; x++)
        {
            if (geometry_terms[x] == "")
            {
                geometry_terms[x] = temp;
                break;
            }
        }
    }

    return geometry_terms;
}

string _Geometry::Terms::AATriangleSimilarity(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles such that two angles of one triangle are congruent to the corresponding two angles of the second triangle, then the two triangles are similar.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "A A Triangle Similarity";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AASTriangleCongruence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles such that two angles and the side opposite one of them in one triangle are congruent to the corresponding parts of the second triangle, then two triangles are congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "A A S Triangle Congruence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ASATriangleCongruence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles such that two angles and the included side of one triangle are congruent to the corresponding parts of the second triangle, then two triangles are congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "ASA Triangle Congruence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Abscissa(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The horizontal or x-coordinate of a two-dimensional coordinate system.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Abscissa";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AcuteAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle whose measure is greater than 0 degrees and less than 90 degrees.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Acute Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AcuteTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A triangle that contains three acute angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Acute Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AdditivePropertyOfEquality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If a, b, and c are real numbers such that a + c = b + c.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Additive Property Of Equality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AdjacentAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two coplanar angles that share a common vertex and a common side but have no common interior points.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Adjacent Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AdjacentSides(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two sides of any polygon that share a common vertex.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Adjacent Sides";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AlgebraicRepresentation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The use of an equation or algebraic expression to model a mathematical relationship.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Algebraic Representation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Algorithm(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a defined series of steps for carrying out a computation or process.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Algorithm";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AlternateInteriorAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " Any two nonadjacent angles that lie on opposite sides of a transversal and that are interior to the lines.  Note: As illustrated in the second example, the lines do not need to be parallel.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Alternate Interior Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AltitudeOfACone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from the vertex of the cone perpendicular to the plane containing its base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Altitude Of A Cone";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AltitudeOfACylinder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from any point on one base of a cylinder perpendicular to the plane containing its other base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Altitude Of A Cylinder";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AltitudeOfAPrism(string text)
{
    string model = llm_model;    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string definition = "A line segment drawn from any point of one base of the prism perpendicular to the plane containing its other base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Altitude Of A Prism";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AltitudeOfAPyramid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from the vertex of the pyramid perpendicular to the plane containing its base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Altitude Of A Pyramid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AltitudeOfATrapezoid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from any point on one base of the trapezoid perpendicular to the other base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Altitude Of A Trapezoid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AltitudeOfATriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from any vertex of the triangle perpendicular to the line containing its opposite side.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Altitude Of A Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AnalyticalGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An approach to geometry in which the points of a figure are represented by coordinates on the Cartesian plane and algebraic methods of reasoning are used to study the figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Analytical Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AnalyticalGeometricProof(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A proof in geometry that employs the coordinate system and algebraic reasoning.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Analytical Geometric Proof";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AnalyticalRepresentationOfATransformation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The functional notation of a transformation using analytical equations.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Analytical Representation Of A Transformation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Analyze(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "to examine methodically by separating into parts and studying their relationships.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Analyze";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Angle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A geometric figure formed by two rays that have a common endpoint.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AngleBisector(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A ray that divides an angle into two adjacent congruent angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Angle Bisector";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AngleMeasure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The number of degrees or radians in an angle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Angle Measure";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Antecedent(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " The \"if\" part of a conditional (if., then.) statement.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Antecedent";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Apothem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from the center of a regular polygon perpendicular to a side of the polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Apothem";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ArcLength(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The distance on the circumference of a circle from one endpoint of an arc to the other endpoint, measured along the arc.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Arc Length";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ArcMeasure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The measure of an arc of the circle in degrees or radians; a unique real number between 0 degrees and 360 degrees or between zero and 2pi radians";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Arc Measure";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ArcOfACircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "See major arc, minor arc.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Arc Of A Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AreaOfAPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The unique real number assigned to any polygon which indicates the number of  non-overlapping square units contained in the polygon\'s interior.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Area Of A Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Argument(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The communication, in verbal or written form, of the reasoning process that leads to a valid conclusion.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Argument";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Axiom(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement that is accepted without proof.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Axiom";
        }
    }

    return "NULL";
}

string _Geometry::Terms::AxisOfSymmetry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line that divides a plane figure into two congruent reflected halves; Any line through a figure such that a point on one side of the line is the same distance to the axis as its corresponding point on the other side.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Axis Of Symmetry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Base(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any side or face of a geometric figure to which an altitude is drawn";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Base";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Betweeness(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A point B is between points A and C if and only if AB + BC = AC";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Betweeness";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Biconditional(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement formed by the conjunction of a conditional statement and its converse; a statement that can be written in  \"if and only if\" form; a definition can always be written as a biconditional statement.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biconditional";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CartesianCoordinates(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An ordered pair of real numbers that establishes the location or address of a point in a coordinate plane using the distances from two perpendicular intersecting lines called the coordinate axes.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cartesian Coordinates";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CartesianPlane(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points in a plane designated by their Cartesian coordinates.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cartesian Plane";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CenterOfADilation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A fixed point in the plane about which all points are expanded or contracted;  the only invariant point under dilation.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Center Of A Dilation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CenterOfGravity(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The balance point of an object";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Center Of Gravity";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CenterOfARegularPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The center of the circle which circumscribes or inscribes a regular polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Center Of A Regular Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CenterOfARotation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A fixed point in the plane about which all points are rotated.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Center Of A Rotation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CentralAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle in a circle with vertex at the center of the circle and sides that are radii.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Central Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CentralAngleOfARegularPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle in a regular polygon with vertex at the center of the polygon and sides that are radii of its circumcircle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Central Angle Of A Regular Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Centroid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point of concurrency of the medians of a triangle; the center of gravity in a triangle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Centroid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Chord(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment joining any two points on a circle.  The diameter is the largest chord of a circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Chord";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Circle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points (or locus of points) in a plane that are a fixed distance, (called the radius) from a fixed point, (called the center).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Circumcenter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The center of the circle circumscribed about a polygon; the point that is equidistant from the vertices of any polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Circumcenter";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Circumcircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A circle that passes through all of the vertices of a polygon. Also called a circumscribed circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Circumcircle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Circumference(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The length of or distance around a circle. The formula for circumference is: C = 2*pi*r = pi*d";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Circumference";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Clockwise(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The direction in which the hands of a clock move around the dial.  Used to indicate the orientation of a transformation.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Clockwise";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Closure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A set \"S\" and a binary operation \"*\" are said to exhibit closure if applying the binary operation to any two elements in \"S\" produces a value that is a member of \"S\".";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Closure";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CollinearPoints(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Points that lie on the same line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Collinear Points";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CommonTangents(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Lines that are tangent to two or more circles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Common Tangents";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ComplementaryAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two angles the sum of whose measures is 90 degrees.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Complementary Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CompositionOfFunctions(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A way of combining functions in which the output of one function is used as the input of another function; the formation of a new function h from functions f and g using the rule h(x) = g o f(x) = g for all x in the domain of f for which f(x) is in the domain of g.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Composition Of Functions";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CompoundLocus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A set of points satisfying  two or more locus conditions.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Compound Locus";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CompoundStatement(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement formed from two or more simple statements using the logic connectives, or, and, if...then, or if and only if.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Compound Statement";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConcavePolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon that has at least one diagonal outside the polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Concave Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConcentricCircles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two or more circles having the same center and different radii.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Concentric Circles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Conclusion(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An answer or solution arrived at through logical or mathematical reasoning; the \"then\" clause in an \"if-then\" statement;  the final statement in a proof which follows logically from previous true statements.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Conclusion";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Concurrence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The concept of three or more lines intersecting in a single (common) point; having a single point of intersection.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Concurrence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConditionalStatement(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement formed from two given statements by connecting them in the form  if..., then...";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Conditional Statement";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Cone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A solid formed by a circular region (the base) and the surface formed by the straight line segments connecting points on the boundary of the base with a fixed point (the vertex) not in the plane of the base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cone";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Conjecture(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An educated guess; an unproven hypothesis based on observation, experimentation, data collection, etc.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Conjecture";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Congruent(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Having the same size and shape.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Congruent";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConicSections(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The plane section created by the intersection of a plane and a cone. Example:  a parabola, a circle, an ellipse, or a hyperbola";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Conic Sections";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Consistency(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A property of an axiomatic system where no axiom(s) can be used to contradict any other axiom(s).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Consistency";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConstantOfProportionality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "constant of proportionality";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Constant Of Proportionality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Construct(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "To draw a figure using only a compass and a straightedge.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Construct";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Constraints(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any restriction placed on the variables in a problem.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Constraints";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Contradiction(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement that has been shown to be both true and false.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Contradiction";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ContrapositiveOfAStatement(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement formed by interchanging the hypothesis and conclusion of a conditional statement and negating each clause.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Contrapositive Of A Statement";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConverseOfAStatement(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " A statement formed by interchanging the hypothesis and conclusion of a conditional statement";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Converse Of A Statement";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ConvexPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon is convex if a line segment connecting any two points of the polygon lies entirely in the polygon\'s interior.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Convex Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CoordinateGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An approach to geometry in which a point is represented by coordinates and algebraic methods of reasoning are used; also called analytical geometry.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Coordinate Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CoordinatePlane(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points in a plane designated by their Cartesian coordinates.  Also called the Cartesian plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Coordinate Plane";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Coplanar(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any three or more points that lie in the same plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Coplanar";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CorrespondingAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A set of angles formed on each of two or more lines cut by a transversal that are in the same position relative to each line and to the transversal. Note: As illustrated in the second example, the lines do not need to be parallel.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Corresponding Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::CorrespondingParts(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In two geometric figures, the points, sides, and/or angles which are in the same relative position when the figures are placed in a one-to-one correspondence.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Corresponding Parts";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Counterclockwise(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The direction opposite the way in which the hands of a clock move around the dial.  Used to indicate the orientation of a rotation.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Counterclockwise";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Crossection(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A plane section perpendicular to the longest axis of a solid.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Crossection";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Cube(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron with six square faces. A cube  (or hexahedron) is one of the five platonic solids.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cube";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Decagon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with ten sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Decagon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DeductiveReasoning(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A process of showing that certain statements follow logically from agreed upon assumptions and proven facts; reasoning from the general to the specific.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Deductive Reasoning";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Diagonal(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that connects two non-consecutive vertices of a polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Diagonal";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Diameter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A chord of the circle that passes through the center of the circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Diameter";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DiameterOfASphere(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that connects two points on the surface of a sphere and that passes through the center of the sphere.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Diameter Of A Sphere";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DihedralAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle formed by two interesting planes.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dihedral Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Dilation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A transformation of the plane such that if O is a fixed point, k is a non-zero real 15 number, and P\' is the image of point P, then O, P and P\' are collinear and OP\'/OP = k.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dilation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DirectTransformation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any transformation of the plane that preserves orientation.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Direct Transformation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DistanceBetweenTwoPoints(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The length of the line segment joining the two points; a unique non-negative real number.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Distance Between Two Points";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DistanceBetweenAPointAndALine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The length of the perpendicular segment from the point to the line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Distance Between A Point And A Line";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DistanceBetweenTwoParallelLines(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The length of a line segment drawn from any point on one line perpendicular to the second line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Distance Between Two Parallel Lines";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Dodecahedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron that has twelve faces.  A regular dodecahedron is one of the five Platonic solids and has twelve regular pentagons as faces.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dodecahedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::DynamicGeometrySoftware(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Computer or calculator software used to construct and manipulate geometric figures.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dynamic Geometry Software";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EdgeOfAPolyhedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that connects two consecutive vertices of a polyhedron.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Edge Of A Polyhedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Ellipse(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A set of points P sub 1 in a plane, such that the sum of the distances from P to two fixed points F sub 1 and F sub 2 is a given constant  k. Any plane section of a circular conical surface which is a closed curve.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ellipse";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Endpoint(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A point at either the end of a line segment, or arc, or the initial point of a ray.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Endpoint";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Equiangular(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with all interior angles congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Equiangular";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Equidistant(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "At the same distance.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Equidistant";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EquilateralPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with all sides congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Equilateral Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EquilateralTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A triangle with three congruent sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Equilateral Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EquivalenceRelation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A relation that exhibits the reflexive, symmetric, and transitive properties.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Equivalence Relation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EuclideanGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The study of geometry based on definitions undefined terms (point, line and plane) and the assumptions of Euclid (c.a. 330 B.C.)";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Euclidean Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EuclideanParallelPostulate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any assumption equivalent to the following statement:  If l is any line and P is any point not on l , then there exists exactly one line through P that is parallel to l.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Euclidean Parallel Postulate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::EulerLine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "For any given triangle, the line that contains the circumcenter, the centroid and the orthocenter.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Euler Line";
        }
    }

    return "NULL";
}

string _Geometry::Terms::staticalSegmentOfASecant(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If a secant is drawn to a circle from an statical point, the portion of the secant that lies outside the circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "statical Segment Of A Secant";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ExteriorOfAGeometricFigure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points outside a geometric figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Exterior Of A Geometric Figure";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ExteriorAngleOfAPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle formed by a side of a polygon and the extention of an adjacent side.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Exterior Angle Of A Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::FaceOfAPolyhedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any one of the polygons that bound a polyhedron.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Face Of A Polyhedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::FixedPoint(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A point that is its own image under a transformation of the plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Fixed Point";
        }
    }

    return "NULL";
}

string _Geometry::Terms::FootOfAnAltitude(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point of intersection of an altitude and the line or plane to which it is perpendicular.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Foot Of An Altitude";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Function(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A rule that assigns to each number x in the function\'s domain a unique number f(x).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Function";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GeometricInequality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement in geometry which indicates that quantity is greater than another quantity.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geometric Inequality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GeometricMean(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The geometric mean, also called the mean proportional, of two numbers aand bis the square root of their product. If a / m = m / b then m is the geometric mean of a and b.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geometric Mean";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GeometricProbability(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A probability based on geometric relationships such as area, surface area or volume.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geometric Probability";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GeometricRepresentationOfTheCircularFunctions(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The representation of circular functions on a circle of unit radius.  The trigonometric functions are called circular functions because their values are related to the lengths of specific line segments associated with a circle of unit radius.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geometric Representation Of The Circular Functions";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GeometricSequence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A set of terms in which each term is formed by multiplying the preceding term by a nonzero constant.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geometric Sequence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Geometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Branch of mathematics that deals with the properties, measurement, and relationships of points, lines, angles, surfaces, and solids.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GlideReflection(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A transformation that is the composition of a line reflection and a translation through a vector parallel to that line of reflection.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Glide Reflection";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GoldenRatio(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "When a line segment AB is divided by an interior point P such that AB / AP = AP / PB, the ratio AB / AP = 1/2(1 + sqrt(5)) is called the golden ratio.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Golden Ratio";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GoldenRectangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A rectangle whose adjacent sides have a ratio equal to the golden ratio.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Golden Rectangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GraphicalRepresentation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A graph or graphs used to model a mathematical relationship.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Graphical Representation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::GreatCircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The intersection of a sphere with any plane passing through the center of the sphere.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Great Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::HalfTurn(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A 180 degree rotation about a point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Half Turn";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Hemisphere(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Half of a sphere bounded by a great circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hemisphere";
        }
    }

    return "NULL";
}

string _Geometry::Terms::HeronsFormula(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The formula expressing the area of a triangle, A, in terms of its sides a, b, 1 and c. A = sqrt(s(s-a)(s-b)(s-c)) where s = 1/2(a+b+c) and is called the sem0perimeter.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Heron's Formula";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Hexagon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with six sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hexagon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Hyperbola(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Set of points P in a plane such that the difference between the distances from P to the foci F sub 1 and F sub 2 is a given constant k.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hyperbola";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Hypotenuse(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The side of a right triangle opposite the right angle; the longest side of a right triangle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hypotenuse";
        }
    }

    return "NULL";
}

string _Geometry::Terms::HypotenuseAndLegTriangleCongruence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two right triangles such that the hypotenuse and leg of one right triangle are congruent to the hypotenuse and corresponding leg of the second right triangle, then the triangles are congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hypotenuse And Leg Triangle Congruence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Hypothesis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An assumed statement used as a premise in a proof; the \"given\"; the \"if\"  clause of an \"if-then\" statement. (See also antecedent.)";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hypothesis";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Icosahedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron having twenty faces. A regular icosahedron is one of the five Platonic solids and has twenty equilateral triangles as faces.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Icosahedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IdentityElements(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "For a binary operation * and a set S, ? is the identity element if a * ? = a and ? * a = a for every element a that is in S.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Identity Elements";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Image(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The resulting point or set of points under a given transformation; in any function f, the image of x is the functional value f(x) corresponding to x.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Image";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IncenterOfATriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The center of the circle that is inscribed in a triangle; the point of concurrence of the three angle bisectors of the triangle which is equidistant from the sides of the triangle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Incenter Of A Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IncludedAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The side between two consecutive angles in a polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Included Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IndirectProof(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A method of proof in which the statement that is to be proven is assumed false and a contradiction results.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Indirect Proof";
        }
    }

    return "NULL";
}

string _Geometry::Terms::InductiveReasoning(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The process of observing data, recognizing patterns and making generalizations about those patterns.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inductive Reasoning";
        }
    }

    return "NULL";
}

string _Geometry::Terms::InscribedAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle whose vertex lies on the circle and whose sides are chords of a circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inscribed Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::InscribedCircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A circle in the interior of a polygon that is tangent to each side of the polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inscribed Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::InterceptedArc(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An arc of a circle whose endpoints lie on the sides of an angle, and all of the points on the arc are in the interior of the angle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Intercepted Arc";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Interior(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points inside a geometric figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Interior";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IntersectingLines(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Lines that share a common point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Intersecting Lines";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IntersectionOfSets(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The intersection of two or more sets is the set of all elements that are common to all of the given sets.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Intersection Of Sets";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Invariant(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A figure or property that remains unchanged under a transformation of the plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Invariant";
        }
    }

    return "NULL";
}

string _Geometry::Terms::InverseOfAStatement(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement formed by negating both the hypothesis and conclusion of a given conditional.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inverse Of A Statement";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Isometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A transformation of the plane  that preserves distance.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Isometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IsoscelesTrapezoid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A trapezoid in which the non-parallel sides are congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Isosceles Trapezoid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::IsoscelesTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A triangle that has at least two congruent sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Isosceles Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LateralAreaOfAPrism(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The sum of the areas of the faces of the prism not including the bases.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lateral Area Of A Prism";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LateralEdge(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that is the intersection of any two lateral faces of a polyhedron.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lateral Edge";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LateralFace(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A face of a polyhedron, not including its bases.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lateral Face";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LengthOfLineSegment(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The distance between the end two end points of a line segment.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Length Of Line Segment";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LineSegment(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Given any two points A and B, AB is equal to the union of points A, B, and all of those points between A and B.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Line Segment";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LineSymmetry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A geometric figure has line symmetry if the figure is the image of itself under a reflection in a line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Line Symmetry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LinearPairOfAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any two adjacent angles whose non-common sides form a line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Linear Pair Of Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LocusOfPoints(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points satisfying a given condition or conditions.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Locus Of Points";
        }
    }

    return "NULL";
}

string _Geometry::Terms::LogicalEquivalence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Statements that have the same truth value.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Logical Equivalence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::MajorArc(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In a circle, any arc whose length is greater than the length of a semicircle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Major Arc";
        }
    }

    return "NULL";
}

string _Geometry::Terms::MeanProportional(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The mean proportional, also called the geometric mean, of two numbers a and b is the square root of their product. If a / m = m / b then m = sqrt(ab) is the geometric mean of a and b.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Mean Proportional";
        }
    }

    return "NULL";
}

string _Geometry::Terms::MeasureOfAnArc(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The measure of the central angle that subtends the arc.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Measure Of An Arc";
        }
    }

    return "NULL";
}

string _Geometry::Terms::MedianOfATrapezoid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that connects the midpoints of the two non-parallel sides of the trapezoid.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Median Of A Trapezoid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::MedianOfATriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that connects any vertex of a triangle to the midpoint of the opposite side.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Median Of A Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Midpoint(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A point that divides a line segment into two congruent line segments.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Midpoint";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Midsegment(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that connects the midpoints of  two sides of a triangle; Also called the midline.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Midsegment";
        }
    }

    return "NULL";
}

string _Geometry::Terms::MinorArc(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In a circle, any arc whose length is less than the length of a semicircle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Minor Arc";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Negation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "For any given statement p, its negation is the statement, ~p (not p) whose truth value is the opposite of the truth value of p.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Negation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Net(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A two dimensional pattern consisting of polygons which can be folded to form a polyhedron.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Net";
        }
    }

    return "NULL";
}

string _Geometry::Terms::NGon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with n sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "N Gon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::NonCollinearPoints(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Three or more points that do not lie on the same line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Non Collinear Points";
        }
    }

    return "NULL";
}

string _Geometry::Terms::NonCoplanarPoints(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Four or more points that do not lie on the same plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Non Coplanar Points";
        }
    }

    return "NULL";
}

string _Geometry::Terms::NonEuclideanGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A geometry that contains an axiom which is equivalent to the negation of the Euclidean parallel postulate.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Non Euclidean Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RiemannianGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A non-Euclidean geometry using as its parallel postulate any statement equivalent to the following: If l is any line and P is any point not on l, then there are no lines through P that are parallel to l. (Also called elliptic  geometry.)";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Riemannian Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::HyperbolicGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A non-Euclidean geometry using as its parallel postulate any statement equivalent to the following: If l is any line and P is any point not on l, then there exists at least two lines through P that are parallel to l.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hyperbolic Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ObliqueLineAndAPlane(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line and a plane that are neither parallel nor perpendicular.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Oblique Line And A Plane";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ObtuseAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle whose measure is greater than 90 degrees and less than 180 degrees.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Obtuse Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ObtuseTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A triangle having one obtuse angle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Obtuse Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Octagon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with 8 sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Octagon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Octahedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron having eight faces. A regular octahedron is one of the five Platonic solids and has eight equilateral triangles as faces.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Octahedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::OneToOneFunction(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A function where the inverse is also a function.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "One To One Function";
        }
    }

    return "NULL";
}

string _Geometry::Terms::OppositeRays(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two collinear rays whose intersection is exactly one point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Opposite Rays";
        }
    }

    return "NULL";
}

string _Geometry::Terms::OppositeSideInARightTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The side across from an angle. In a right triangle the hypotenuse is opposite the right angle and each leg is opposite one of the acute angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Opposite Side In A Right Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::OppositeTransformation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A transformation of the plane that changes the orientation of a figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Opposite Transformation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::OrderedPair(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two numbers that are used to identify the position of a point in a plane. The two numbers are called coordinates and are represented by (x, y).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ordered Pair";
        }
    }

    return "NULL";
}

string _Geometry::Terms::OrderedTriple(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Three numbers that are used to identify the position of a point in space. The three numbers are called coordinates and are represented by (x, y, z).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ordered Triple";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Ordinate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The vertical coordinate of a two-dimensional rectangular coordinate system; usually denoted by y.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ordinate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Orientation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The arrangement of the points, relative to one another, after a transformation; the reference made to the direction traversed (clockwise or counterclockwise) when traveling around a geometric figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Orientation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Origin(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point in the Cartesian coordinate plane at which the horizontal and vertical axes intersect, designated by the ordered pair (0,0).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Origin";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Orthocenter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point of concurrence of the three altitudes of a triangle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Orthocenter";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Parabola(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any plane section of a circular conical surface by a plane parallel to the slant height of the cone.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parabola";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ParagraphProof(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A written proof in which the statements and their corresponding reasons are written, in paragraph form, using complete sentences.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Paragraph Proof";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ParallelPiped(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A prism whose bases are parallelograms.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parallel Piped";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ParallelLines(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two or more coplanar lines that do not intersect. Parallel line segments or rays are line segments or rays that are subsets of parallel lines.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parallel Lines";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ParallelPlanes(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two or more planes that do not intersect.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parallel Planes";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ParallelPostulate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any postulate or axiom that designates the number of lines through a given point that are parallel to a given line.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parallel Postulate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Parallelogram(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A quadrilateral in which both pairs of opposite sides are parallel.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parallelogram";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Parameter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A quantity or constant whose value varies with the circumstances of its application.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parameter";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Pentagon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with 5 sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pentagon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Perimeter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The sum of the lengths of all the sides of any polygon.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Perimeter";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PerpendicularBisector(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line, segment or ray that is perpendicular to a line segment at its midpoint.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Perpendicular Bisector";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PerpendicularLines(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two lines that intersect to form right angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Perpendicular Lines";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PerpendicularPlanes(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two planes that intersect to form right dihedral angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Perpendicular Planes";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Pi(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The irrational number equal to the length of the circumference of a circle divided by the length of its diameter.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pi";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Plane(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An undefined term in geometry usually visualized as a flat surface with no thickness that extends indefinitely in two dimensions.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Plane";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PlatonicSolids(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The five regular polyhedra: tetrahedron, cube, octahedron, dodecahedron and icosahedron.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Platonic Solids";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Point(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An undefined term in geometry usually visualized as a dot representing a non- dimensional location in space.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Point";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PointOfConcurrency(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A point that is the intersection of three or more lines.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Point Of Concurrency";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PointOfTangency(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point where a tangent line intersects a curve.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Point Of Tangency";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PointSlopeEquationOfALine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The equation of a line formed using its slope and the coordinates of a point on the line, where m is the slope of the line and (x1, y1) are the coordinates of the given point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Point Slope Equation Of A Line";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PointSymmetry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A geometric figure has point symmetry if every point on the figure is the image of itself under a rotation of 180 degrees about some fixed point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Point Symmetry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Polygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A closed plane figure formed by three or more line segments that meet only at their endpoints.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Polyhedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A solid figure bounded by polygons.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Polyhedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PositionVector(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A coordinate vector whose initial point is the origin. Any vector can be expressed as an equivalent position vector by translating the vector so that it originates at the origin.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Position Vector";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Postulate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A statement assumed to be true without proof.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Postulate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Preimage(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The original point or points of a transformation.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Preimage";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Premise(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A proposition upon which an argument is based or from which a conclusion is drawn.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Premise";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Prism(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron with two congruent, parallel, polygonal bases and whose lateral faces are parallelograms.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Prism";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ProofByContradiction(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A method of proof which demonstrates the truth of an implication by proving that the negation of the conclusion of that implication leads to a contradiction; also called an indirect proof.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Proof By Contradiction";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Proportional(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two variables are proportional if they maintain a constant ratio. See also direct variation.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Proportional";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Pyramid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron having a polygonal base and triangles as lateral faces.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pyramid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::PythagoreanTheorem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The mathematical relationship stating that in any right triangle the sum of the squares of the lengths of the two legs is equal to the square of the length of the hypotenuse; if a and b are the lengths of the legs and c is the length of the hypotenuse, then a2 + b2 = c2.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pythagorean Theorem";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Quadrant(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The four regions of a plane created by the intersection of the coordinate axes. Each of these quadrants has a number designation:";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Quadrant";
        }
    }

    return "NULL";
}

string _Geometry::Terms::QuadraticEquation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An equation that can be written in the form ax^2 + bx + c = 0. Where a, b, and c are real constants and a != 0.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Quadratic Equation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Quadrilateral(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon with 4 sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Quadrilateral";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Radical(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The root of a quantity as indicated by the radical sign.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Radical";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Radius(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment drawn from the center of a circle to a point on the circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Radius";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Ray(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Given any two points A and B, AB is equal to the union of AB and all of those points X such that B is between X and A.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ray";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Reason(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A true statement justifying a step in a proof; the use of logic, examples, etc. to determine a result.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Reason";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Rectangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A parallelogram containing one right angle; a quadrilateral with four right angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rectangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RectangularCoordinates(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An ordered pair of real numbers that establishes the location of a point in a coordinate plane using the distances from two perpendicular intersecting lines called the coordinate axes. (See also Cartesian coordinates.)";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rectangular Coordinates";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Reflection(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An isometry where if l is any line and P is any point not on l, then r1(P) = P\'";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Reflection";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ReflexivePropertyOfEquality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A property of real numbers that states a = a.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Reflexive Property Of Equality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RegularPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polygon which is both equilateral and equiangular.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Regular Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RegularPyramid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A pyramid whose base is a regular polygon and whose lateral faces are congruent isosceles triangles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Regular Pyramid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RemoteInteriorAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Either interior angle of a triangle that is not adjacent to a given exterior angle of the triangle. Also called non-adjacent interior angles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Remote Interior Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RestrictedDomain(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The domain resulting from a restriction placed on a function, based on the context of the problem.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Restricted Domain";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Rhombus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A parallelogram with two adjacent congruent sides; a quadrilateral with four congruent sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rhombus";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RightAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An angle formed by two perpendicular lines, the measure of which is 90 degrees.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Right Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RightCircularCylinder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A cylinder whose bases are circles and whose altitude passes through the center of both bases";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Right Circular Cylinder";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RightCircularCone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A cone whose base is a circle and whose altitude passes through the center of its base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Right Circular Cone";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RightPyramid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A pyramid whose lateral faces are isosceles triangles.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Right Pyramid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RightTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A triangle with one right angle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Right Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Rotation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An isometry where if P is a fixed point in the plane, theta is any angle and A != P then Rp,theta(A) = A\' where m<APA\' = 0 and Rp,theta(P) = P.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rotation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::RotationalSymmetry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A geometric figure has rotational symmetry if the figure is the image of itself under a rotation about a point through any angle whose measure is not a multiple of 360 degrees.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rotational Symmetry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SASTriangleCongruence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles, such that two sides and the included angle of one triangle are congruent to the corresponding two sides and included angle of the second triangle, then the two triangles are congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "SAS Triangle Congruence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SASSimilarityTheorem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles, such that two pairs of corresponding sides are proportional and their included angles are congruent, then the two triangles are similar.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "SAS Similarity Theorem";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ScaleneTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A triangle with no congruent sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Scalene Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SecantOfACircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line that intersects a circle in exactly two points.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Secant Of A Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SectorOfACircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A region bounded by an arc of the circle and the two radii to the endpoints of the arc.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sector Of A Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SegmentOfACircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The region bounded by a chord and the arc subtended by that chord.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Segment Of A Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SemiCircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Either of the arcs of a circle determined by the endpoints of a diameter.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Semi Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Set(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A well-defined collection of items.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Set";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SimilarPolygons(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two polygons which have the same shape but not necessarily the same size.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Similar Polygons";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SkewLines(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two non-coplanar lines that do not intersect.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Skew Lines";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SlantHeight(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Of a pyramid: The altitude of a lateral face of a pyramid.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Slant Height";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SlantHeightOfACone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The length of a line segment drawn on the lateral surface of a cone from its vertex to a point on the circle that determines its base.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Slant Height Of A Cone";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Slope(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The measure of the steepness of a line; the ratio of vertical change to horizontal change.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Slope";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SlopeEquation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The equation of a line formed using its slope and its y-intercept. If the coordinates of the y-intercept of the line are (0, b) and the slope is m, then the equation of the line is y = mx + b.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Slope Equation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Sphere(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The locus of points in space at a given distance from a fixed point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sphere";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Square(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A rectangle with two congruent adjacent sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Square";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SSSTriangleCongruence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles, such that all three sides of one triangle are congruent to the corresponding sides of the second triangle, then the two triangles are congruent.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "S S S Triangle Congruence";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Straightedge(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "An object with no marked units of measure that is used for drawing straight lines.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Straightedge";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SubstitutionProperty(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Any quantity can be replaced by an equal quantity.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Substitution Property";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SubtractionPropertyOfEquality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If the same or equal quantities are subtracted from same or equal quantities, then the results are equal.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Subtraction Property Of Equality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SupplementaryAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two angles the sum of whose measures is 180 degrees.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Supplementary Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SurfaceArea(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The sum of the areas of all the faces or curved surfaces of a solid figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Surface Area";
        }
    }

    return "NULL";
}

string _Geometry::Terms::SymmetricPropertyOfEquality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A property of the real numbers that states If a = b then b = a.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Symmetric Property Of Equality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::InternalTangentCircles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two circles are internally tangent if they intersect in exactly one point and one circle lies in the interior of the other circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Internal Tangent Circles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::staticalTangentCircles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Two circles are statically tangent if the meet in exactly one point and neither circle has any points in the interior of the other circle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "statical Tangent Circles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TangentLineToACircle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line that intersects a circle in exactly one point.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tangent Line To A Circle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TangentSegment(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line segment that is a subset of a tangent line. This usually refers to the line segment drawn from an statical point to the point of tangency.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tangent Segment";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Tessellation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A repeating pattern covering a plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tessellation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Tetrahedron(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A polyhedron with four faces; one of the five Platonic solids that has four equilateral triangles as faces (pyramid).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tetrahedron";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Theorem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A general statement that requires proof.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Theorem";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ThreeDimensionalSpace(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points in space. The position of each point can be represented by a unique ordered triple (x,y,z).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Three Dimensional Space";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Transformation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A one-to-one mapping of points in the plane to points in the plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Transformation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TransformationalGeometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A method for studying geometry that illustrates congruence and similarity by the use of transformations.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Transformational Geometry";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TransformationalProof(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A proof that employs the use of transformations.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Transformational Proof";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TransitivePropertyOfEquality(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A property of the real numbers that states: If a = b and b = c then a = c.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Transitive Property Of Equality";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Translation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A transformation where every point moves the same direction through the same distance.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Translation";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Transversal(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line that intersects two (or more) other lines in distinct points.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Transversal";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Trapezoid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A quadrilateral with exactly one pair of parallel sides.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Trapezoid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TriangleInequalityTheorem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In any triangle, the sum of the lengths of two sides is greater than the length of the third side.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Triangle Inequality Theorem";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TrichotomyProperty(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A property of the real numbers that states: for every x and y, one and only one of the following conditions is true: x < y; x = y, x > y.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Trichotomy Property";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TrigonometryOfTheRightTriangle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The trigonometric functions for acute angles are the ratios of the sides of the right triangle containing the angle. sinx = length of side opposite of x/length of hypotenouse, cosx = length of the side adjacent to x/length of hypotenouse, tanx = length of the side opposite x/length of side adjacent to x.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Trigonometry Of The Right Triangle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TruthValue(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A value, (typically T or F), indicating whether a statement is true or false.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Truth Value";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TwoColumnProof(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The outline of a written proof in which the statements and their corresponding reasons are listed in two separate columns.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Two Column Proof";
        }
    }

    return "NULL";
}

string _Geometry::Terms::TwoDimensionalSpace(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The set of all points in the plane. The position of each point can be represented by a unique ordered pair (x,y). Figures such as angles, pairs of parallel and intersecting lines, circles and polygons exist in two-dimensional space.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Two Dimensional Space";
        }
    }

    return "NULL";
}

string _Geometry::Terms::UndefinedTerms(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The fundamental components of an axiomatic system whose understanding is agreed upon but not formally defined. In geometry undefined terms traditionally include point, line, and plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Undefined Terms";
        }
    }

    return "NULL";
}

string _Geometry::Terms::UnionOfSets(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The union of two or more sets is the set of all elements contained in at least one of the sets.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Union Of Sets";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ValidArgument(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A logical argument supported by known facts or assumed axioms; an argument in which the premise leads to a conclusion.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Valid Argument";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Vector(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A quantity that has both magnitude and direction; represented geometrically by a directed line segment.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vector";
        }
    }

    return "NULL";
}

string _Geometry::Terms::VertexOfAnAngle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point of intersection of the two rays that form the sides of the angle.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vertex Of An Angle";
        }
    }

    return "NULL";
}

string _Geometry::Terms::VertexOfAPolygon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A point where the edges of a polygon intersect.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vertex Of A Polygon";
        }
    }

    return "NULL";
}

string _Geometry::Terms::VertexOfAConeOrPyramid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The fixed point, not in the plane of the base, to which all points on the perimeter of the base are connected.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vertex Of A Cone Or Pyramid";
        }
    }

    return "NULL";
}

string _Geometry::Terms::VerticalAngles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The two nonadjacent angles formed when two lines intersect.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vertical Angles";
        }
    }

    return "NULL";
}

string _Geometry::Terms::Volume(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A measure of the number of cubic units needed to fill the space inside a solid figure.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Volume";
        }
    }

    return "NULL";
}

string _Geometry::Terms::XAxis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "One of the two intersecting lines used to establish the coordinates of points in the Cartesian plane; in that plane, the line whose equation is y = 0; in space the axis perpendicular to the yz-plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "X Axis";
        }
    }

    return "NULL";
}

string _Geometry::Terms::XCoordinate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The first coordinate in any (x,y) ordered pair; the number represents how many units the point is located to the left or right of the y-axis; also called abscissa.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "X Coordinate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::XIntercept(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point at which the graph of a relation intercepts the x-axis. The ordered pair for this point has a value of y = 0.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "X Intercept";
        }
    }

    return "NULL";
}

string _Geometry::Terms::YAxis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "One of the two intersecting lines used to establish the coordinates of points in the Cartesian plane; in that plane, the line whose equation is x = 0; in space the axis perpendicular to the xz-plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Y Axis";
        }
    }

    return "NULL";
}

string _Geometry::Terms::YCoordinate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The second coordinate in any (x,y) ordered pair; the number represents how many units the point is located above or below of the x-axis; also called ordinate.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Y Coordinate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::YIntercept(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The point at which a graph of a relation intercepts the y-axis. The ordered pair for this point has a value of x = 0.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Y Intercept";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ZAxis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A line perpendicular to the plane determined by the x-axis and y-axis at their point of intersection; this axis is used as a reference to determine the third component of the ordered triple (x, y, z).";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Z Axis";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ZCoordinate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The third coordinate in any (x,y,z) ordered triple; the number represents how many units the point is located above or below of the xy-plane.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Z Coordinate";
        }
    }

    return "NULL";
}

string _Geometry::Terms::ZeroProductProperty(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "If a and b are real numbers, then ab = 0 if and only if a = 0 or b = 0, or a and b = 0.";
    string prompt = "Check if the following text matches the following definition. Limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Zero Product Property";
        }
    }

    return "NULL";
}

string _Geometry::Vision::AATriangleSimilarity(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "If there exists a one-to-one correspondence between the vertices of two triangles such that two angles of one triangle are congruent to the corresponding two angles of the second triangle, then the two triangles are similar.";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "AA Triangle Similarity";
        }
    }
}

string _Geometry::Vision::AASTriangleCongruence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ASATriangleCongruence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Abscissa(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AcuteAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AcuteTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AdditivePropertyOfEquality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AdjacentAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AdjacentSides(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AlgebraicRepresentation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Algorithm(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AlternateInteriorAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AltitudeOfACone(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AltitudeOfACylinder(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AltitudeOfAPrism(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AltitudeOfAPyramid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AltitudeOfATrapezoid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AltitudeOfATriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AnalyticalGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AnalyticalGeometricProof(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AnalyticalRepresentationOfATransformation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Analyze(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Angle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AngleBisector(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AngleMeasure(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Antecedent(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Apothem(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ArcLength(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ArcMeasure(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ArcOfACircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AreaOfAPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Argument(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Axiom(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::AxisOfSymmetry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Base(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Betweeness(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Biconditional(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CartesianCoordinates(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CartesianPlane(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CenterOfADilation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CenterOfGravity(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CenterOfARegularPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CenterOfARotation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CentralAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CentralAngleOfARegularPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Centroid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Chord(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Circle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Circumcenter(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Circumcircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Circumference(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Clockwise(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Closure(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CollinearPoints(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CommonTangents(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ComplementaryAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CompositionOfFunctions(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CompoundLocus(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CompoundStatement(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConcavePolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConcentricCircles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Conclusion(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Concurrence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConditionalStatement(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Cone(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Conjecture(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Congruent(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConicSections(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Consistency(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConstantOfProportionality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Construct(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Constraints(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Contradiction(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ContrapositiveOfAStatement(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConverseOfAStatement(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ConvexPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CoordinateGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CoordinatePlane(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Coplanar(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CorrespondingAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::CorrespondingParts(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Counterclockwise(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Crossection(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Cube(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Decagon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DeductiveReasoning(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Diagonal(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Diameter(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DiameterOfASphere(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DihedralAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Dilation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DirectTransformation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DistanceBetweenTwoPoints(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DistanceBetweenAPointAndALine(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DistanceBetweenTwoParallelLines(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Dodecahedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::DynamicGeometrySoftware(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EdgeOfAPolyhedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Ellipse(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Endpoint(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Equiangular(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Equidistant(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EquilateralPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EquilateralTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EquivalenceRelation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EuclideanGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EuclideanParallelPostulate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::EulerLine(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::staticalSegmentOfASecant(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ExteriorOfAGeometricFigure(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ExteriorAngleOfAPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::FaceOfAPolyhedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::FixedPoint(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::FootOfAnAltitude(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Function(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GeometricInequality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GeometricMean(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GeometricProbability(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GeometricRepresentationOfTheCircularFunctions(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GeometricSequence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Geometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GlideReflection(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GoldenRatio(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GoldenRectangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GraphicalRepresentation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::GreatCircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::HalfTurn(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Hemisphere(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::HeronsFormula(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Hexagon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Hyperbola(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Hypotenuse(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::HypotenuseAndLegTriangleCongruence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Hypothesis(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Icosahedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IdentityElements(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Image(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IncenterOfATriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IncludedAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IndirectProof(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::InductiveReasoning(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::InscribedAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::InscribedCircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::InterceptedArc(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Interior(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IntersectingLines(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IntersectionOfSets(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Invariant(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::InverseOfAStatement(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Isometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IsoscelesTrapezoid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::IsoscelesTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LateralAreaOfAPrism(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LateralEdge(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LateralFace(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LengthOfLineSegment(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LineSegment(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LineSymmetry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LinearPairOfAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LocusOfPoints(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::LogicalEquivalence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::MajorArc(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::MeanProportional(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::MeasureOfAnArc(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::MedianOfATrapezoid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::MedianOfATriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Midpoint(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Midsegment(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::MinorArc(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Negation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Net(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::NGon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::NonCollinearPoints(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::NonCoplanarPoints(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::NonEuclideanGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RiemannianGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::HyperbolicGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ObliqueLineAndAPlane(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ObtuseAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ObtuseTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Octagon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Octahedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::OneToOneFunction(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::OppositeRays(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::OppositeSideInARightTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::OppositeTransformation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::OrderedPair(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::OrderedTriple(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Ordinate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Orientation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Origin(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Orthocenter(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Parabola(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ParagraphProof(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ParallelPiped(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ParallelLines(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ParallelPlanes(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ParallelPostulate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Parallelogram(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Parameter(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Pentagon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Perimeter(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PerpendicularBisector(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PerpendicularLines(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PerpendicularPlanes(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Pi(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Plane(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PlatonicSolids(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Point(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PointOfConcurrency(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PointOfTangency(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PointSlopeEquationOfALine(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PointSymmetry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Polygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Polyhedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PositionVector(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Postulate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Preimage(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Premise(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Prism(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ProofByContradiction(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Proportional(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Pyramid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::PythagoreanTheorem(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Quadrant(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::QuadraticEquation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Quadrilateral(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Radical(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Radius(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Ray(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Reason(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Rectangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RectangularCoordinates(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Reflection(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ReflexivePropertyOfEquality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RegularPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RegularPyramid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RemoteInteriorAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RestrictedDomain(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Rhombus(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RightAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RightCircularCylinder(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RightCircularCone(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RightPyramid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RightTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Rotation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::RotationalSymmetry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SASTriangleCongruence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SASSimilarityTheorem(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ScaleneTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SecantOfACircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SectorOfACircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SegmentOfACircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SemiCircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Set(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SimilarPolygons(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SkewLines(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SlantHeight(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SlantHeightOfACone(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Slope(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SlopeEquation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Sphere(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Square(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SSSTriangleCongruence(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Straightedge(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SubstitutionProperty(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SubtractionPropertyOfEquality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SupplementaryAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SurfaceArea(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::SymmetricPropertyOfEquality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::InternalTangentCircles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::staticalTangentCircles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TangentLineToACircle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TangentSegment(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Tessellation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Tetrahedron(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Theorem(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ThreeDimensionalSpace(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Transformation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TransformationalGeometry(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TransformationalProof(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TransitivePropertyOfEquality(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Translation(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Transversal(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Trapezoid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TriangleInequalityTheorem(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TrichotomyProperty(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TrigonometryOfTheRightTriangle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TruthValue(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TwoColumnProof(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::TwoDimensionalSpace(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::UndefinedTerms(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::UnionOfSets(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ValidArgument(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Vector(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::VertexOfAnAngle(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::VertexOfAPolygon(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::VertexOfAConeOrPyramid(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::VerticalAngles(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::Volume(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::XAxis(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::XCoordinate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::XIntercept(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::YAxis(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::YCoordinate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::YIntercept(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ZAxis(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ZCoordinate(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}

string _Geometry::Vision::ZeroProductProperty(string image_location)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model_image;
    string definition = "";
    string prompt = "Check if the following definition matches the image and limit the response to yes or no: " + definition;
    string response = _LLM2::OllamaAPI(model, prompt, image_location);
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
    oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "";
        }
    }
}
