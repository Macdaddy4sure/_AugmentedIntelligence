/*
    Copyright(C) 2025 Tyler Crockett | Macdaddy4sure.ai

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissionsand
    limitations under the License.
*/

#include "AugmentedIntelligence.hpp"
#include "Medicine.hpp"
#include "Large Language Models.hpp"
#include "Parsers.hpp"
#include "Settings.hpp"
#include "Thought.hpp"
#include "Time.hpp"
#include "Utilities.hpp"
#include "Working-Memory.hpp"

using namespace std;

vector<string> _Medicine::MedicineTermsClassification(string text)
{
    vector<string> detected_terms(100);
    string temp;

    if (_Medicine::Terms::AarskogScottSyndrome(text) != "NULL")
    {
        temp = "Aarskog Scott Syndrome";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Abdomen(text) != "NULL")
    {
        temp = "Abdomen";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AbdominalstaticalObliqueMuscle(text) != "NULL")
    {
        temp = "Abdominal statical Oblique Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AbdominalInternalObliqueMuscle(text) != "NULL")
    {
        temp = "Abdominal Internal Oblique Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AbductorPollicisBrevisMuscle(text) != "NULL")
    {
        temp = "Abductor Pollicis Brevis Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Abscess(text) != "NULL")
    {
        temp = "Abscess";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Accommodation(text) != "NULL")
    {
        temp = "Accommodation";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AccommodationReflex(text) != "NULL")
    {
        temp = "Accommodation Reflex";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Acetabulum(text) != "NULL")
    {
        temp = "Acetabulum";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AchillesTendon(text) != "NULL")
    {
        temp = "Achilles Tendon";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Acne(text) != "NULL")
    {
        temp = "Acne";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Acupressure(text) != "NULL")
    {
        temp = "Acupressure";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Acupuncture(text) != "NULL")
    {
        temp = "Acupuncture";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AdamsApple(text) != "NULL")
    {
        temp = "Adams Apple";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AdaptiveImmuneSystem(text) != "NULL")
    {
        temp = "Adaptive Immune System";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Adenoma(text) != "NULL")
    {
        temp = "Adenoma";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AdrenalGland(text) != "NULL")
    {
        temp = "Adrenal Gland";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Allergy(text) != "NULL")
    {
        temp = "Allergy";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AttentionDeficitHyperactivityDisorder(text) != "NULL")
    {
        temp = "Attention Deficit Hyperactivity Disorder";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ADHD(text) != "NULL")
    {
        temp = "A D H D";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AlzheimersDisease(text) != "NULL")
    {
        temp = "Alzheimers Disease";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AnalCanal(text) != "NULL")
    {
        temp = "Anal Canal";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Anatomy(text) != "NULL")
    {
        temp = "Anatomy";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Anesthesiology(text) != "NULL")
    {
        temp = "Anesthesiology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Angiology(text) != "NULL")
    {
        temp = "Angiology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Ankle(text) != "NULL")
    {
        temp = "Ankle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AnteriorTibialArtery(text) != "NULL")
    {
        temp = "Anterior Tibial Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Antibiotic(text) != "NULL")
    {
        temp = "Antibiotic";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Antibody(text) != "NULL")
    {
        temp = "Antibody";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Aorta(text) != "NULL")
    {
        temp = "Aorta";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Appendix(text) != "NULL")
    {
        temp = "Appendix";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Arm(text) != "NULL")
    {
        temp = "Arm";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Arteriole(text) != "NULL")
    {
        temp = "Arteriole";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Artery(text) != "NULL")
    {
        temp = "Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Arthritis(text) != "NULL")
    {
        temp = "Arthritis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AspergerSyndrome(text) != "NULL")
    {
        temp = "Asperger Syndrome";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Asthma(text) != "NULL")
    {
        temp = "Asthma";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::AtrialFibrillation(text) != "NULL")
    {
        temp = "Atrial Fibrillation";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Auscultation(text) != "NULL")
    {
        temp = "Auscultation";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Axilla(text) != "NULL")
    {
        temp = "Axilla";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Back(text) != "NULL")
    {
        temp = "Back";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BackPain(text) != "NULL")
    {
        temp = "Back Pain";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Barotrauma(text) != "NULL")
    {
        temp = "Barotrauma";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BetaCell(text) != "NULL")
    {
        temp = "Beta Cell";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Biceps(text) != "NULL")
    {
        temp = "Biceps";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BileDuct(text) != "NULL")
    {
        temp = "Bile Duct";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BiliaryTract(text) != "NULL")
    {
        temp = "Biliary Tract";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BingeEatingDisorder(text) != "NULL")
    {
        temp = "Binge Eating Disorder";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BiologicalEngineering(text) != "NULL")
    {
        temp = "Biological Engineering";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Biology(text) != "NULL")
    {
        temp = "Biology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Biochemistry(text) != "NULL")
    {
        temp = "Biochemistry";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Bioinformatics(text) != "NULL")
    {
        temp = "Bioinformatics";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Biopsy(text) != "NULL")
    {
        temp = "Biopsy";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Biostatistics(text) != "NULL")
    {
        temp = "Biostatistics";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BipolarDisorder(text) != "NULL")
    {
        temp = "Bipolar Disorder";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BirthControl(text) != "NULL")
    {
        temp = "Birth Control";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BladderCancer(text) != "NULL")
    {
        temp = "Bladder Cancer";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BloodPressure(text) != "NULL")
    {
        temp = "Blood Pressure";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BloodVessel(text) != "NULL")
    {
        temp = "Blood Vessel";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Bone(text) != "NULL")
    {
        temp = "Bone";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BoneMarrow(text) != "NULL")
    {
        temp = "Bone Marrow";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BrachialArtery(text) != "NULL")
    {
        temp = "Brachial Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BrachialPlexus(text) != "NULL")
    {
        temp = "Brachial Plexus";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BrachialVeins(text) != "NULL")
    {
        temp = "Brachial Veins";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Brachioradialis(text) != "NULL")
    {
        temp = "Brachioradialis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Bradycardia(text) != "NULL")
    {
        temp = "Bradycardia";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Brain(text) != "NULL")
    {
        temp = "Brain";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BrainTumor(text) != "NULL")
    {
        temp = "Brain Tumor";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BrainMetastasis(text) != "NULL")
    {
        temp = "Brain Metastasis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Breast(text) != "NULL")
    {
        temp = "Breast";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BreastCancer(text) != "NULL")
    {
        temp = "Breast Cancer";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BrocasArea(text) != "NULL")
    {
        temp = "Brocas Area";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Bronchiole(text) != "NULL")
    {
        temp = "Bronchiole";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Bronchus(text) != "NULL")
    {
        temp = "Bronchus";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Bruit(text) != "NULL")
    {
        temp = "Bruit";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::BulimiaNervosa(text) != "NULL")
    {
        temp = "Bulimia Nervosa";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Buttocks(text) != "NULL")
    {
        temp = "Buttocks";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Calcium(text) != "NULL")
    {
        temp = "Calcium";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Calf(text) != "NULL")
    {
        temp = "Calf";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cancer(text) != "NULL")
    {
        temp = "Cancer";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Capillary(text) != "NULL")
    {
        temp = "Capillary";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Carcinogen(text) != "NULL")
    {
        temp = "Carcinogen";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Carcinogenesis(text) != "NULL")
    {
        temp = "Carcinogenesis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CardiacArrest(text) != "NULL")
    {
        temp = "Cardiac Arrest";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CardiacCatheterization(text) != "NULL")
    {
        temp = "Cardiac Catheterization";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CardiacMuscle(text) != "NULL")
    {
        temp = "Cardiac Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CardiacSurgery(text) != "NULL")
    {
        temp = "Cardiac Surgery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cardiology(text) != "NULL")
    {
        temp = "Cardiology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CardiothoracicSurgery(text) != "NULL")
    {
        temp = "Cardiothoracic Surgery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CardiovascularDisease(text) != "NULL")
    {
        temp = "Cardiovascular Disease";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CarotidArtery(text) != "NULL")
    {
        temp = "Carotid Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CarotidArterystatical(text) != "NULL")
    {
        temp = "Carotid Artery statical";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CarotidArteryInternal(text) != "NULL")
    {
        temp = "Carotid Artery Internal";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CarotidArteryStenosis(text) != "NULL")
    {
        temp = "Carotid Artery Stenosis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CarpalBones(text) != "NULL")
    {
        temp = "Carpal Bones";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CarpalTunnelSyndrome(text) != "NULL")
    {
        temp = "Carpal Tunnel Syndrome";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cartilage(text) != "NULL")
    {
        temp = "Cartilage";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CartilaginousJoint(text) != "NULL")
    {
        temp = "Cartilaginous Joint";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Catheter(text) != "NULL")
    {
        temp = "Catheter";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CellBiology(text) != "NULL")
    {
        temp = "Cell Biology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CentralNervousSystem(text) != "NULL")
    {
        temp = "Central Nervous System";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CephalicVein(text) != "NULL")
    {
        temp = "Cephalic Vein";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cerebellum(text) != "NULL")
    {
        temp = "Cerebellum";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cerebrum(text) != "NULL")
    {
        temp = "Cerebrum";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CervicalCancer(text) != "NULL")
    {
        temp = "Cervical Cancer";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cervix(text) != "NULL")
    {
        temp = "Cervix";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cheek(text) != "NULL")
    {
        temp = "Cheek";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Chin(text) != "NULL")
    {
        temp = "Chin";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ChronicFatigueSyndrome(text) != "NULL")
    {
        temp = "Chronic Fatigue Syndrome";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CiliaryMuscle(text) != "NULL")
    {
        temp = "Ciliary Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CiliarySulcus(text) != "NULL")
    {
        temp = "Ciliary Sulcus";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CirculatorySystem(text) != "NULL")
    {
        temp = "Circulatory System";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Clavicle(text) != "NULL")
    {
        temp = "Clavicle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Clinic(text) != "NULL")
    {
        temp = "Clinic";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ClinicalResearch(text) != "NULL")
    {
        temp = "Clinical Research";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CoeliacDisease(text) != "NULL")
    {
        temp = "Coeliac Disease";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CommonCarotidArtery(text) != "NULL")
    {
        temp = "Common Carotid Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CommonCold(text) != "NULL")
    {
        temp = "Common Cold";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CommonIliacArtery(text) != "NULL")
    {
        temp = "Common Iliac Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CommonIliacVein(text) != "NULL")
    {
        temp = "Common Iliac Vein";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CoronaryArteries(text) != "NULL")
    {
        temp = "Coronary Arteries";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CorpusCallosum(text) != "NULL")
    {
        temp = "Corpus Callosum";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::CranialNerves(text) != "NULL")
    {
        temp = "Cranial Nerves";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cure(text) != "NULL")
    {
        temp = "Cure";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cytogenetics(text) != "NULL")
    {
        temp = "Cytogenetics";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Cytokines(text) != "NULL")
    {
        temp = "Cytokines";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DecompressionSickness(text) != "NULL")
    {
        temp = "Decompression Sickness";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DeepCircumflexIliacVein(text) != "NULL")
    {
        temp = "Deep Circumflex Iliac Vein";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DeepTemporalArteries(text) != "NULL")
    {
        temp = "Deep Temporal Arteries";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DefinitiveTreatment(text) != "NULL")
    {
        temp = "Definitive Treatment";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DeltoidMuscle(text) != "NULL")
    {
        temp = "Deltoid Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Dentistry(text) != "NULL")
    {
        temp = "Dentistry";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Dermatitis(text) != "NULL")
    {
        temp = "Dermatitis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Diagnosis(text) != "NULL")
    {
        temp = "Diagnosis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DiabetesMellitus(text) != "NULL")
    {
        temp = "Diabetes Mellitus";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DietaryReferenceIntake(text) != "NULL")
    {
        temp = "Dietary Reference Intake";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DifferentialDiagnosis(text) != "NULL")
    {
        temp = "Differential Diagnosis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::DigestiveSystem(text) != "NULL")
    {
        temp = "Digestive System";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Disease(text) != "NULL")
    {
        temp = "Disease";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Ear(text) != "NULL")
    {
        temp = "Ear";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::EarInfection(text) != "NULL")
    {
        temp = "Ear Infection";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Otitisstatica(text) != "NULL")
    {
        temp = "Otitis statica";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::OtitisMedia(text) != "NULL")
    {
        temp = "Otitis Media";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::OtitisInterna(text) != "NULL")
    {
        temp = "Otitis Interna";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Elbow(text) != "NULL")
    {
        temp = "Elbow";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Embryology(text) != "NULL")
    {
        temp = "Embryology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::EmergencyMedicine(text) != "NULL")
    {
        temp = "Emergency Medicine";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::EndocrineSystem(text) != "NULL")
    {
        temp = "Endocrine System";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Endocrinology(text) != "NULL")
    {
        temp = "Endocrinology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Epidemiology(text) != "NULL")
    {
        temp = "Epidemiology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Epiglottis(text) != "NULL")
    {
        temp = "Epiglottis";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Epilepsy(text) != "NULL")
    {
        temp = "Epilepsy";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ErectileDysfunction(text) != "NULL")
    {
        temp = "Erectile Dysfunction";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ErectorSpinaeMuscles(text) != "NULL")
    {
        temp = "Erector Spinae Muscles";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Esophagus(text) != "NULL")
    {
        temp = "Esophagus";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ExtensorPollicisBrevisMuscle(text) != "NULL")
    {
        temp = "Extensor Pollicis Brevis Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ExtensorPollicisEtIndicisCommunisMuscle(text) != "NULL")
    {
        temp = "Extensor Pollicis Et Indicis Communis Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::ExtensorPollicisLongusMuscle(text) != "NULL")
    {
        temp = "Extensor Pollicis Longus Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::staticalCarotidArtery(text) != "NULL")
    {
        temp = "statical Carotid Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::staticalIliacArtery(text) != "NULL")
    {
        temp = "statical Iliac Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::staticalIliacVein(text) != "NULL")
    {
        temp = "statical Iliac Vein";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::staticalJugularVein(text) != "NULL")
    {
        temp = "statical Jugular Vein";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Eye(text) != "NULL")
    {
        temp = "Eye";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Face(text) != "NULL")
    {
        temp = "Face";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::FallopianTube(text) != "NULL")
    {
        temp = "Fallopian Tube";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Fellowship(text) != "NULL")
    {
        temp = "Fellowship";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::FemaleReproductiveSystem(text) != "NULL")
    {
        temp = "Female Reproductive System";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::FemoralArtery(text) != "NULL")
    {
        temp = "Femoral Artery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::FemoralNerve(text) != "NULL")
    {
        temp = "Femoral Nerve";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::FemoralVein(text) != "NULL")
    {
        temp = "Femoral Vein";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Femur(text) != "NULL")
    {
        temp = "Femur";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::FrontalNerve(text) != "NULL")
    {
        temp = "Frontal Nerve";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Gallbladder(text) != "NULL")
    {
        temp = "Gallbladder";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Gamete(text) != "NULL")
    {
        temp = "Gamete";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Ganglion(text) != "NULL")
    {
        temp = "Ganglion";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::GastrocnemiusMuscle(text) != "NULL")
    {
        temp = "Gastrocnemius Muscle";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Gastroenterology(text) != "NULL")
    {
        temp = "Gastroenterology";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::GastrointestinalTract(text) != "NULL")
    {
        temp = "Gastrointestinal Tract";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::GeneTherapy(text) != "NULL")
    {
        temp = "Gene Therapy";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::GeneralSurgery(text) != "NULL")
    {
        temp = "General Surgery";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
    }
    if (_Medicine::Terms::Genetics(text) != "NULL")
    {
        temp = "Genetics";

        for (int x = 0; x < 100; x++)
        {
            if (detected_terms[x] == "")
            {
                detected_terms[x] = temp;
                break;
            }
        }
        if (_Medicine::Terms::GenitourinarySystem(text) != "NULL")
        {
            temp = "Genitourinary System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Geriatrics(text) != "NULL")
        {
            temp = "Geriatrics";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Gonad(text) != "NULL")
        {
            temp = "Gonad";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Gout(text) != "NULL")
        {
            temp = "Gout";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::GracilisMuscle(text) != "NULL")
        {
            temp = "Gracilis Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::GreatSaphenousVein(text) != "NULL")
        {
            temp = "Great Saphenous Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Gynaecology(text) != "NULL")
        {
            temp = "Gynaecology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hand(text) != "NULL")
        {
            temp = "Hand";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HandSurgery(text) != "NULL")
        {
            temp = "Hand Surgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Head(text) != "NULL")
        {
            temp = "Head";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Health(text) != "NULL")
        {
            temp = "Health";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HealthCare(text) != "NULL")
        {
            temp = "Health Care";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hearing(text) != "NULL")
        {
            temp = "Hearing";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Heart(text) != "NULL")
        {
            temp = "Heart";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Heel(text) != "NULL")
        {
            temp = "Heel";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hematemesis(text) != "NULL")
        {
            temp = "Hematemesis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hematology(text) != "NULL")
        {
            temp = "Hematology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hematoma(text) != "NULL")
        {
            temp = "Hematoma";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hematuria(text) != "NULL")
        {
            temp = "Hematuria";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hemodialysis(text) != "NULL")
        {
            temp = "Hemodialysis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hemolysis(text) != "NULL")
        {
            temp = "Hemolysis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hemoperfusion(text) != "NULL")
        {
            temp = "Hemoperfusion";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Haemophilia(text) != "NULL")
        {
            temp = "Haemophilia";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hemoptysis(text) != "NULL")
        {
            temp = "Hemoptysis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hemorrhoid(text) != "NULL")
        {
            temp = "Hemorrhoid";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hyperhidrosis(text) != "NULL")
        {
            temp = "Hyperhidrosis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hypertension(text) != "NULL")
        {
            temp = "Hypertension";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hyperkalemia(text) != "NULL")
        {
            temp = "Hyperkalemia";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HipBone(text) != "NULL")
        {
            temp = "Hip Bone";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Histology(text) != "NULL")
        {
            temp = "Histology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Homeostasis(text) != "NULL")
        {
            temp = "Homeostasis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hormone(text) != "NULL")
        {
            temp = "Hormone";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hospice(text) != "NULL")
        {
            temp = "Hospice";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hospital(text) != "NULL")
        {
            temp = "Hospital";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HospitalMedicine(text) != "NULL")
        {
            temp = "Hospital Medicine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanBack(text) != "NULL")
        {
            temp = "Human Back";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanBody(text) != "NULL")
        {
            temp = "Human Body";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanBrain(text) != "NULL")
        {
            temp = "Human Brain";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanDigestiveSystem(text) != "NULL")
        {
            temp = "Human Digestive System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanHead(text) != "NULL")
        {
            temp = "Human Head";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanMouth(text) != "NULL")
        {
            temp = "Human Mouth";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanMusculoskeletalSystem(text) != "NULL")
        {
            temp = "Human Musculoskeletal System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanNose(text) != "NULL")
        {
            temp = "Human Nose";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanReproductiveSystem(text) != "NULL")
        {
            temp = "Human Reproductive System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanSkeleton(text) != "NULL")
        {
            temp = "Human Skeleton";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Humerus(text) != "NULL")
        {
            temp = "Humerus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hydrocele(text) != "NULL")
        {
            temp = "Hydrocele";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Hypersalivation(text) != "NULL")
        {
            temp = "Hypersalivation";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::IliacArteryCommon(text) != "NULL")
        {
            temp = "Iliac Artery Common";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::IliacArterystatical(text) != "NULL")
        {
            temp = "Iliac Artery statical";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ilium(text) != "NULL")
        {
            temp = "Ilium";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ImmuneSystem(text) != "NULL")
        {
            temp = "Immune System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Immunohistochemistry(text) != "NULL")
        {
            temp = "Immunohistochemistry";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Immunology(text) != "NULL")
        {
            temp = "Immunology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::IliacVeinCommon(text) != "NULL")
        {
            temp = "Iliac Vein Common";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::DeepCircumflexIliacVein(text) != "NULL")
        {
            temp = "Deep Circumflex Iliac Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::staticalIliacVein(text) != "NULL")
        {
            temp = "statical Iliac Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InternalIliacVein(text) != "NULL")
        {
            temp = "Internal Iliac Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::IndexFinger(text) != "NULL")
        {
            temp = "Index Finger";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Infectiology(text) != "NULL")
        {
            temp = "Infectiology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InferiorObliqueMuscle(text) != "NULL")
        {
            temp = "Inferior Oblique Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InferiorThyroidArtery(text) != "NULL")
        {
            temp = "Inferior Thyroid Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InferiorVenaCava(text) != "NULL")
        {
            temp = "Inferior Vena Cava";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Influenza(text) != "NULL")
        {
            temp = "Influenza";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PhysicalExamination(text) != "NULL")
        {
            temp = "Physical Examination";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::IntegumentarySystem(text) != "NULL")
        {
            temp = "Integumentary System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::IntensiveCareMedicine(text) != "NULL")
        {
            temp = "Intensive Care Medicine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InternalCarotidArtery(text) != "NULL")
        {
            temp = "Internal Carotid Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InternalJugularVein(text) != "NULL")
        {
            temp = "Internal Jugular Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InternalMedicine(text) != "NULL")
        {
            temp = "Internal Medicine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Internship(text) != "NULL")
        {
            temp = "Internship";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InterventionalCardiology(text) != "NULL")
        {
            temp = "Interventional Cardiology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InterventionalRadiology(text) != "NULL")
        {
            temp = "Interventional Radiology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ischium(text) != "NULL")
        {
            temp = "Ischium";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Jaundice(text) != "NULL")
        {
            temp = "Jaundice";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Jaw(text) != "NULL")
        {
            temp = "Jaw";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Jejunum(text) != "NULL")
        {
            temp = "Jejunum";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Joint(text) != "NULL")
        {
            temp = "Joint";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::JugularVein(text) != "NULL")
        {
            temp = "Jugular Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Kidney(text) != "NULL")
        {
            temp = "Kidney";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Knee(text) != "NULL")
        {
            temp = "Knee";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::KorsakoffSyndrome(text) != "NULL")
        {
            temp = "Korsakoff Syndrome";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LargeIntestine(text) != "NULL")
        {
            temp = "Large Intestine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LaryngealProminence(text) != "NULL")
        {
            temp = "Laryngeal Prominence";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LaryngealVentricle(text) != "NULL")
        {
            temp = "Laryngeal Ventricle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Laryngospasm(text) != "NULL")
        {
            temp = "Laryngospasm";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ligament(text) != "NULL")
        {
            temp = "Ligament";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Lips(text) != "NULL")
        {
            temp = "Lips";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LittleFinger(text) != "NULL")
        {
            temp = "Little Finger";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Liver(text) != "NULL")
        {
            temp = "Liver";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LongBone(text) != "NULL")
        {
            temp = "Long Bone";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LumbarVertebrae(text) != "NULL")
        {
            temp = "Lumbar Vertebrae";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Lung(text) != "NULL")
        {
            temp = "Lung";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LungCancer(text) != "NULL")
        {
            temp = "Lung Cancer";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LupusErythematosus(text) != "NULL")
        {
            temp = "Lupus Erythematosus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Lymph(text) != "NULL")
        {
            temp = "Lymph";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LymphaticSystem(text) != "NULL")
        {
            temp = "Lymphatic System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LymphaticVessel(text) != "NULL")
        {
            temp = "Lymphatic Vessel";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::LymphNode(text) != "NULL")
        {
            temp = "Lymph Node";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Lymphocyte(text) != "NULL")
        {
            temp = "Lymphocyte";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Lymphoma(text) != "NULL")
        {
            temp = "Lymphoma";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MajorDepressiveDisorder(text) != "NULL")
        {
            temp = "Major Depressive Disorder";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MaleReproductiveSystem(text) != "NULL")
        {
            temp = "Male Reproductive System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MammaryGland(text) != "NULL")
        {
            temp = "Mammary Gland";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Mandible(text) != "NULL")
        {
            temp = "Mandible";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MasseterMuscle(text) != "NULL")
        {
            temp = "Masseter Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MaternalFetalMedicine(text) != "NULL")
        {
            temp = "Maternal Fetal Medicine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Maxilla(text) != "NULL")
        {
            temp = "Maxilla";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MCAT(text) != "NULL")
        {
            temp = "M C A T";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalBiology(text) != "NULL")
        {
            temp = "Medical Biology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalClassification(text) != "NULL")
        {
            temp = "Medical Classification";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalCoding(text) != "NULL")
        {
            temp = "Medical Coding";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalCollegeAdmissionTest(text) != "NULL")
        {
            temp = "Medical College Admission Test";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalDevice(text) != "NULL")
        {
            temp = "Medical Device";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalDiagnosis(text) != "NULL")
        {
            temp = "Medical Diagnosis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalEthics(text) != "NULL")
        {
            temp = "Medical Ethics";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalHistory(text) != "NULL")
        {
            temp = "Medical History";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalImaging(text) != "NULL")
        {
            temp = "Medical Imaging";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalLaboratory(text) != "NULL")
        {
            temp = "Medical Laboratory";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalResearch(text) != "NULL")
        {
            temp = "Medical Research";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalSchool(text) != "NULL")
        {
            temp = "Medical School";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SignsAndSymptoms(text) != "NULL")
        {
            temp = "Signs And Symptoms";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalSpecialty(text) != "NULL")
        {
            temp = "Medical Specialty";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Medication(text) != "NULL")
        {
            temp = "Medication";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedullaOblongata(text) != "NULL")
        {
            temp = "Medulla Oblongata";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MetacarpalBones(text) != "NULL")
        {
            temp = "Metacarpal Bones";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MetatarsalBones(text) != "NULL")
        {
            temp = "Metatarsal Bones";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Microbiology(text) != "NULL")
        {
            temp = "Microbiology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MiddleFinger(text) != "NULL")
        {
            temp = "Middle Finger";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MiddleTemporalArtery(text) != "NULL")
        {
            temp = "Middle Temporal Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MolecularBiology(text) != "NULL")
        {
            temp = "Molecular Biology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanMouth(text) != "NULL")
        {
            temp = "Human Mouth";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Muscle(text) != "NULL")
        {
            temp = "Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MuscularSystem(text) != "NULL")
        {
            temp = "Muscular System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanMusculoskeletalSystem(text) != "NULL")
        {
            temp = "Human Musculoskeletal System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Nail(text) != "NULL")
        {
            temp = "Nail";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Nanobiotechnology(text) != "NULL")
        {
            temp = "Nanobiotechnology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::NasalCavity(text) != "NULL")
        {
            temp = "Nasal Cavity";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Nasopharynx(text) != "NULL")
        {
            temp = "Nasopharynx";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Navel(text) != "NULL")
        {
            temp = "Navel";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Nephrology(text) != "NULL")
        {
            temp = "Nephrology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Nerve(text) != "NULL")
        {
            temp = "Nerve";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::NervousSystem(text) != "NULL")
        {
            temp = "Nervous System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Neurology(text) != "NULL")
        {
            temp = "Neurology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Neuroscience(text) != "NULL")
        {
            temp = "Neuroscience";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Neurosurgery(text) != "NULL")
        {
            temp = "Neurosurgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::NuclearMedicine(text) != "NULL")
        {
            temp = "Nuclear Medicine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Nutrition(text) != "NULL")
        {
            temp = "Nutrition";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ObliqueMuscleOfAuricle(text) != "NULL")
        {
            temp = "Oblique Muscle Of Auricle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Obstetrics(text) != "NULL")
        {
            temp = "Obstetrics";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ObstetricsAndGynaecology(text) != "NULL")
        {
            temp = "Obstetrics And Gynaecology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::OccipitalBone(text) != "NULL")
        {
            temp = "Occipital Bone";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::OcularSurgery(text) != "NULL")
        {
            temp = "Ocular Surgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Olfaction(text) != "NULL")
        {
            temp = "Olfaction";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Oncology(text) != "NULL")
        {
            temp = "Oncology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ophthalmology(text) != "NULL")
        {
            temp = "Ophthalmology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Optometry(text) != "NULL")
        {
            temp = "Optometry";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Organ(text) != "NULL")
        {
            temp = "Organ";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::OralAndMaxillofacialSurgery(text) != "NULL")
        {
            temp = "Oral And Maxillofacial Surgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::OrbicularisOculiMuscle(text) != "NULL")
        {
            temp = "Orbicularis Oculi Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::OrbicularisOrisMuscle(text) != "NULL")
        {
            temp = "Orbicularis Oris Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::OrthopedicSurgery(text) != "NULL")
        {
            temp = "Orthopedic Surgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ossicles(text) != "NULL")
        {
            temp = "Ossicles";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Otitis(text) != "NULL")
        {
            temp = "Otitis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Otorhinolaryngology(text) != "NULL")
        {
            temp = "Otorhinolaryngology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ovary(text) != "NULL")
        {
            temp = "Ovary";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Palate(text) != "NULL")
        {
            temp = "Palate";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PalliativeCare(text) != "NULL")
        {
            temp = "Palliative Care";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Palpation(text) != "NULL")
        {
            temp = "Palpation";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pancreas(text) != "NULL")
        {
            temp = "Pancreas";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Papillary(text) != "NULL")
        {
            temp = "Papillary";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Parasitology(text) != "NULL")
        {
            temp = "Parasitology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ParathyroidGlands(text) != "NULL")
        {
            temp = "Parathyroid Glands";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ParkinsonsDisease(text) != "NULL")
        {
            temp = "Parkinsons Disease";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Patella(text) != "NULL")
        {
            temp = "Patella";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pathology(text) != "NULL")
        {
            temp = "Pathology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PectineusMuscle(text) != "NULL")
        {
            temp = "Pectineus Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PectoralisMajor(text) != "NULL")
        {
            temp = "Pectoralis Major";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PectoralisMinor(text) != "NULL")
        {
            temp = "Pectoralis Minor";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pediatrics(text) != "NULL")
        {
            temp = "Pediatrics";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pelvis(text) != "NULL")
        {
            temp = "Pelvis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Penis(text) != "NULL")
        {
            temp = "Penis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Percussion(text) != "NULL")
        {
            temp = "Percussion";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PeripheralNervousSystem(text) != "NULL")
        {
            temp = "Peripheral Nervous System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PeripheralVision(text) != "NULL")
        {
            temp = "Peripheral Vision";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PhalanxBone(text) != "NULL")
        {
            temp = "Phalanx Bone";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pharmacology(text) != "NULL")
        {
            temp = "Pharmacology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pharynx(text) != "NULL")
        {
            temp = "Pharynx";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Physician(text) != "NULL")
        {
            temp = "Physician";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Physiology(text) != "NULL")
        {
            temp = "Physiology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PinealGland(text) != "NULL")
        {
            temp = "Pineal Gland";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PituitaryGland(text) != "NULL")
        {
            temp = "Pituitary Gland";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Placenta(text) != "NULL")
        {
            temp = "Placenta";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PlasticSurgery(text) != "NULL")
        {
            temp = "Plastic Surgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Plexus(text) != "NULL")
        {
            temp = "Plexus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pons(text) != "NULL")
        {
            temp = "Pons";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PosteriorTibialArtery(text) != "NULL")
        {
            temp = "Posterior Tibial Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PreventiveHealthcare(text) != "NULL")
        {
            temp = "Preventive Healthcare";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Prognosis(text) != "NULL")
        {
            temp = "Prognosis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Prostate(text) != "NULL")
        {
            temp = "Prostate";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Psychiatry(text) != "NULL")
        {
            temp = "Psychiatry";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pubis(text) != "NULL")
        {
            temp = "Pubis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PulmonaryArtery(text) != "NULL")
        {
            temp = "Pulmonary Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PulmonaryCirculation(text) != "NULL")
        {
            temp = "Pulmonary Circulation";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::PulmonaryVein(text) != "NULL")
        {
            temp = "Pulmonary Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pulmonology(text) != "NULL")
        {
            temp = "Pulmonology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Pulse(text) != "NULL")
        {
            temp = "Pulse";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Quadriplegia(text) != "NULL")
        {
            temp = "Quadriplegia";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RadialArtery(text) != "NULL")
        {
            temp = "Radial Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RadialNerve(text) != "NULL")
        {
            temp = "Radial Nerve";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Radiology(text) != "NULL")
        {
            temp = "Radiology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Radius(text) != "NULL")
        {
            temp = "Radius";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Rectum(text) != "NULL")
        {
            temp = "Rectum";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RectusAbdominisMuscle(text) != "NULL")
        {
            temp = "Rectus Abdominis Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RectusFemorisMuscle(text) != "NULL")
        {
            temp = "Rectus Femoris Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RedBloodCell(text) != "NULL")
        {
            temp = "Red Blood Cell";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RenalArtery(text) != "NULL")
        {
            temp = "Renal Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RenalVein(text) != "NULL")
        {
            temp = "Renal Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Residency(text) != "NULL")
        {
            temp = "Residency";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RespiratorySystem(text) != "NULL")
        {
            temp = "Respiratory System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Rheumatology(text) != "NULL")
        {
            temp = "Rheumatology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RibCage(text) != "NULL")
        {
            temp = "Rib Cage";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::RingFinger(text) != "NULL")
        {
            temp = "Ring Finger";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Sacrum(text) != "NULL")
        {
            temp = "Sacrum";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SalivaryGland(text) != "NULL")
        {
            temp = "Salivary Gland";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SaphenousNerve(text) != "NULL")
        {
            temp = "Saphenous Nerve";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SaphenousVeinGreat(text) != "NULL")
        {
            temp = "Saphenous Vein Great";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SmallSaphenousVein(text) != "NULL")
        {
            temp = "Small Saphenous Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SartoriusMuscle(text) != "NULL")
        {
            temp = "Sartorius Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Scalp(text) != "NULL")
        {
            temp = "Scalp";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Scapula(text) != "NULL")
        {
            temp = "Scapula";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SciaticNerve(text) != "NULL")
        {
            temp = "Sciatic Nerve";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Scrotum(text) != "NULL")
        {
            temp = "Scrotum";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SebaceousGland(text) != "NULL")
        {
            temp = "Sebaceous Gland";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SeminalVesicles(text) != "NULL")
        {
            temp = "Seminal Vesicles";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SensoryNervousSystem(text) != "NULL")
        {
            temp = "Sensory Nervous System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SensoryProcessing(text) != "NULL")
        {
            temp = "Sensory Processing";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SerratusAnteriorMuscle(text) != "NULL")
        {
            temp = "Serratus Anterior Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SerratusPosteriorInferiorMuscle(text) != "NULL")
        {
            temp = "Serratus Posterior Inferior Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SerratusPosteriorSuperiorMuscle(text) != "NULL")
        {
            temp = "Serratus Posterior Superior Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SkeletalMuscle(text) != "NULL")
        {
            temp = "Skeletal Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanSkin(text) != "NULL")
        {
            temp = "Human Skin";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Skull(text) != "NULL")
        {
            temp = "Skull";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SmallIntestine(text) != "NULL")
        {
            temp = "Small Intestine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SmoothMuscle(text) != "NULL")
        {
            temp = "Smooth Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SpecialSenses(text) != "NULL")
        {
            temp = "Special Senses";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::MedicalSpecialty(text) != "NULL")
        {
            temp = "Medical Specialty";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SpinalCord(text) != "NULL")
        {
            temp = "Spinal Cord";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SpinalNerve(text) != "NULL")
        {
            temp = "Spinal Nerve";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Sole(text) != "NULL")
        {
            temp = "Sole";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SoleusMuscle(text) != "NULL")
        {
            temp = "Soleus Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Spleen(text) != "NULL")
        {
            temp = "Spleen";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SportsMedicine(text) != "NULL")
        {
            temp = "Sports Medicine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SternohyoidMuscle(text) != "NULL")
        {
            temp = "Sternohyoid Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Sternum(text) != "NULL")
        {
            temp = "Sternum";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Stomach(text) != "NULL")
        {
            temp = "Stomach";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::StriatedMuscleTissue(text) != "NULL")
        {
            temp = "Striated Muscle Tissue";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SubclavianArtery(text) != "NULL")
        {
            temp = "Subclavian Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SubcutaneousTissue(text) != "NULL")
        {
            temp = "Subcutaneous Tissue";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SuperficialTemporalArtery(text) != "NULL")
        {
            temp = "Superficial Temporal Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SuperiorObliqueMuscle(text) != "NULL")
        {
            temp = "Superior Oblique Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SuperiorThyroidArtery(text) != "NULL")
        {
            temp = "Superior Thyroid Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SuperiorVenaCava(text) != "NULL")
        {
            temp = "Superior Vena Cava";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Surgery(text) != "NULL")
        {
            temp = "Surgery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SweatGland(text) != "NULL")
        {
            temp = "Sweat Gland";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Symptoms(text) != "NULL")
        {
            temp = "Symptoms";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SynovialBursa(text) != "NULL")
        {
            temp = "Synovial Bursa";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SynovialJoint(text) != "NULL")
        {
            temp = "Synovial Joint";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SynovialMembrane(text) != "NULL")
        {
            temp = "Synovial Membrane";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Lupus(text) != "NULL")
        {
            temp = "Lupus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SystemsBiology(text) != "NULL")
        {
            temp = "Systems Biology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Tarsus(text) != "NULL")
        {
            temp = "Tarsus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Taste(text) != "NULL")
        {
            temp = "Taste";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::HumanTooth(text) != "NULL")
        {
            temp = "Human Tooth";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Temple(text) != "NULL")
        {
            temp = "Temple";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::DeepTemporalArteries(text) != "NULL")
        {
            temp = "Deep Temporal Arteries";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::TemporalisMuscle(text) != "NULL")
        {
            temp = "Temporalis Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Tendon(text) != "NULL")
        {
            temp = "Tendon";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::TensorFasciaeLataeMuscle(text) != "NULL")
        {
            temp = "Tensor Fasciae Latae Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Testicle(text) != "NULL")
        {
            temp = "Testicle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Thigh(text) != "NULL")
        {
            temp = "Thigh";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ThoracicDiaphragm(text) != "NULL")
        {
            temp = "Thoracic Diaphragm";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Thorax(text) != "NULL")
        {
            temp = "Thorax";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Throat(text) != "NULL")
        {
            temp = "Throat";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Thumb(text) != "NULL")
        {
            temp = "Thumb";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Thymus(text) != "NULL")
        {
            temp = "Thymus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Thyroid(text) != "NULL")
        {
            temp = "Thyroid";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ThyroidImaArtery(text) != "NULL")
        {
            temp = "Thyroid Ima Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Tibia(text) != "NULL")
        {
            temp = "Tibia";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::TibialisAnteriorMuscle(text) != "NULL")
        {
            temp = "Tibialis Anterior Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::TibialisAnteriorMuscle(text) != "NULL")
        {
            temp = "Tibialis Anterior Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::TibialisPosteriorMuscle(text) != "NULL")
        {
            temp = "Tibialis Posterior Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Tissue(text) != "NULL")
        {
            temp = "Tissue";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Toe(text) != "NULL")
        {
            temp = "Toe";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::TollLikeReceptor(text) != "NULL")
        {
            temp = "Toll Like Receptor";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Tongue(text) != "NULL")
        {
            temp = "Tongue";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Toxicology(text) != "NULL")
        {
            temp = "Toxicology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Trachea(text) != "NULL")
        {
            temp = "Trachea";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Trapezius(text) != "NULL")
        {
            temp = "Trapezius";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Triceps(text) != "NULL")
        {
            temp = "Triceps";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ulna(text) != "NULL")
        {
            temp = "Ulna";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::UlnarArtery(text) != "NULL")
        {
            temp = "Ulnar Artery";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::UlnarNerve(text) != "NULL")
        {
            temp = "Ulnar Nerve";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ureter(text) != "NULL")
        {
            temp = "Ureter";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Urethra(text) != "NULL")
        {
            temp = "Urethra";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::UrinaryBladder(text) != "NULL")
        {
            temp = "Urinary Bladder";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::UrinarySystem(text) != "NULL")
        {
            temp = "Urinary System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Urology(text) != "NULL")
        {
            temp = "Urology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Uterus(text) != "NULL")
        {
            temp = "Uterus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Vaccine(text) != "NULL")
        {
            temp = "Vaccine";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Vagina(text) != "NULL")
        {
            temp = "Vagina";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VasDeferens(text) != "NULL")
        {
            temp = "Vas Deferens";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VastusIntermediusMuscle(text) != "NULL")
        {
            temp = "Vastus Intermedius Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VastusLateralisMuscle(text) != "NULL")
        {
            temp = "Vastus Lateralis Muscle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VastusMedialis(text) != "NULL")
        {
            temp = "Vastus Medialis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Vein(text) != "NULL")
        {
            temp = "Vein";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::InferiorVenaCava(text) != "NULL")
        {
            temp = "Inferior Vena Cava";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SuperiorVenaCava(text) != "NULL")
        {
            temp = "Superior Vena Cava";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Ventricle(text) != "NULL")
        {
            temp = "Ventricle";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VentricularSystem(text) != "NULL")
        {
            temp = "Ventricular System";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Venule(text) != "NULL")
        {
            temp = "Venule";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::SpinalColumn(text) != "NULL")
        {
            temp = "Spinal Column";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Virology(text) != "NULL")
        {
            temp = "Virology";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VisualAcuity(text) != "NULL")
        {
            temp = "Visual Acuity";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VisualCortex(text) != "NULL")
        {
            temp = "Visual Cortex";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VisualFieldTest(text) != "NULL")
        {
            temp = "Visual Field Test";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VisualPerception(text) != "NULL")
        {
            temp = "Visual Perception";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VitalSigns(text) != "NULL")
        {
            temp = "Vital Signs";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VitaminD(text) != "NULL")
        {
            temp = "Vitamin D";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Vitrectomy(text) != "NULL")
        {
            temp = "Vitrectomy";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::VitreousBody(text) != "NULL")
        {
            temp = "Vitreous Body";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Vulva(text) != "NULL")
        {
            temp = "Vulva";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Waist(text) != "NULL")
        {
            temp = "Waist";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Wart(text) != "NULL")
        {
            temp = "Wart";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::WeberTest(text) != "NULL")
        {
            temp = "Weber Test";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::WernickeKorsakoffSyndrome(text) != "NULL")
        {
            temp = "Wernicke Korsakoff Syndrome";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::WernickesArea(text) != "NULL")
        {
            temp = "Wernicke's Area";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Whiplash(text) != "NULL")
        {
            temp = "Whiplash";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::WhiteBloodCell(text) != "NULL")
        {
            temp = "White Blood Cell";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::WhiteMatter(text) != "NULL")
        {
            temp = "White Matter";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::WorkingMemory(text) != "NULL")
        {
            temp = "Working Memory";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Wrist(text) != "NULL")
        {
            temp = "Wrist";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Xanthoma(text) != "NULL")
        {
            temp = "Xanthoma";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Yaws(text) != "NULL")
        {
            temp = "Yaws";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::YellowFever(text) != "NULL")
        {
            temp = "Yellow Fever";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ZellwegerSpectrumDisorders(text) != "NULL")
        {
            temp = "Zellweger Spectrum Disorders";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ZikaVirus(text) != "NULL")
        {
            temp = "Zika Virus";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::Zoonosis(text) != "NULL")
        {
            temp = "Zoonosis";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
        if (_Medicine::Terms::ZygomaticBone(text) != "NULL")
        {
            temp = "Zygomatic Bone";

            for (int x = 0; x < 100; x++)
            {
                if (detected_terms[x] == "")
                {
                    detected_terms[x] = temp;
                    break;
                }
            }
        }
    }
    return detected_terms;
}

string _Medicine::Terms::AarskogScottSyndrome(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "(AAS) A rare, inherited (X-linked) disease characterized by short stature, facial abnormalities, skeletal and genital anomalies.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Aarskog Scott Syndrome";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Abdomen(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The part of the body between the chest and pelvis, which contains most of the tubelike organs of the digestive tract, as well as several solid organs";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Abdomen";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AbdominalstaticalObliqueMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The largest, and outermost, of the three flat muscles of the lateral anterior abdominal wall.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Abdominal statical Oblique Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AbdominalInternalObliqueMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A muscle of the abdominal wall, which lies below the statical oblique and just above the transverse abdominal muscles.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Abdominal Internal Oblique Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AbductorPollicisBrevisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A muscle in the hand that abducts (straightens) the thumb.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Abductor Pollicis Brevis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Abscess(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A collection of pus that has built up within the tissue of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Abscess";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Accommodation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the process by which the eye focuses on an object";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Accommodation";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AccommodationReflex(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a reflex action of the eye, measured as a response to focusing on a near object, then looking at a distant object (and vice versa).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Accommodation Reflex";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Acetabulum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a concave surface of the pelvis, which forms the pelvic section of the hip joint.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Acetabulum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AchillesTendon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a tendon of the back of the leg, and the thickest in the human body. It attaches the plantaris, gastrocnemius (calf) and soleus muscles to the calcaneus (heel) bone.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Achilles Tendon";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Acne(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long-term skin disease that occurs when hair follicles are clogged with dead skin cells and oil from the skin.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Acne";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Acupressure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an alternative medicine technique where pressure is applied to acupuncture points. Pressure may be applied by hand, by elbow, or with various devices.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Acupressure";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Acupuncture(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a form of alternative medicine in which thin needles are inserted into the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Acupuncture";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AdamsApple(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the lump or protrusion that is formed by the angle of the thyroid cartilage surrounding the larynx seen especially in males.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Adams Apple";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AdaptiveImmuneSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as the acquired immune system or, more rarely, as the specific immune system, is a subsystem of the overall immune system that is composed of highly specialized, systemic cells and processes that eliminate pathogens or prevent their growth.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Adaptive Immune System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Adenoma(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "(plural adenomas or adenomata) is a benign tumor of epithelial tissue with glandular origin, glandular characteristics, or both.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Adenoma";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AdrenalGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The adrenal glands (also known as suprarenal glands) are endocrine glands that produce a variety of hormones including adrenaline and the steroids aldosterone and cortisol. They are found above the kidneys.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Adrenal Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Allergy(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Allergies, also known as allergic diseases, are a number of conditions caused by hypersensitivity of the immune system to typically harmless substances in the environment. These diseases include hay fever, food allergies, atopic dermatitis, allergic asthma, and anaphylaxis. Symptoms may include red eyes, an itchy rash, sneezing, a runny nose, shortness of breath, or swelling. Food intolerances and food poisoning are separate conditions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Allergy";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AttentionDeficitHyperactivityDisorder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a neurodevelopmental disorder characterised by symptoms of inattention, hyperactivity, impulsivity, and emotional dysregulation that are excessive and pervasive, impairing in multiple contexts, and developmentally inappropriate. ADHD symptoms arise from executive dysfunction.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Attention Deficit Hyperactivity Disorder";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ADHD(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a neurodevelopmental disorder characterised by symptoms of inattention, hyperactivity, impulsivity, and emotional dysregulation that are excessive and pervasive, impairing in multiple contexts, and developmentally inappropriate. ADHD symptoms arise from executive dysfunction.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "A D H D";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AlzheimersDisease(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " also referred to simply as Alzheimer\'s, is a chronic neurodegenerative disease that usually starts slowly and worsens over time. It is the cause of 60-70% of cases of dementia. The most common early symptom is difficulty in remembering recent events (short-term memory loss).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Alzheimers Disease";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AnalCanal(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the terminal part of the large intestine. It is situated between the rectum and anus, below the level of the pelvic diaphragm. In humans it is approximately 2.5 to 4 cm (0.98-1.58 in) long. It lies in the anal triangle of perineum in between the right and left ischioanal fossa.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Anal Canal";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Anatomy(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " the branch of biology concerned with the study of the structure of organisms and their parts. Anatomy is a branch of natural science which deals with the structural organization of living things.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Anatomy";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Anesthesiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical speciality concerned with the total perioperative care of patients before, during and after surgery.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Anesthesiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Angiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical specialty which studies the diseases of the circulatory system and of the lymphatic system, i.e., arteries, veins and lymphatic vessels, and its diseases.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Angiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ankle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The ankle, or the talocrural region, is the region where the foot and the leg meet. The ankle includes three joints: the ankle joint proper or talocrural joint, the subtalar joint, and the inferior tibiofibular joint. The movements produced at this joint are dorsiflexion and plantarflexion of the foot. In common usage, the term ankle refers exclusively to the ankle region. In medical terminology, \"ankle\" (without qualifiers) can refer broadly to the region or specifically to the talocrural joint.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ankle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AnteriorTibialArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The anterior tibial artery of the leg carries blood to the anterior compartment of the leg and dorsal surface of the foot, from the popliteal artery.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Anterior Tibial Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Antibiotic(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a type of antimicrobial substance active against bacteria and is the most important type of antibacterial agent for fighting bacterial infections. Antibiotic medications are widely used in the treatment and prevention of such infections.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Antibiotic";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Antibody(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as an immunoglobulin (Ig), is a large, Y-shaped protein produced mainly by plasma cells that is used by the immune system to neutralize pathogens such as pathogenic bacteria and viruses.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Antibody";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Aorta(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the main artery in the human body, originating from the left ventricle of the heart and extending down to the abdomen, where it splits into two smaller arteries (the common iliac arteries). The aorta distributes oxygenated blood to all parts of the body through the systemic circulation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Aorta";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Appendix(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The appendix (or vermiform appendix; also cecal  appendix; vermix; or vermiform process) is a finger-like, blind-ended tube connected to the cecum, from which it develops in the embryo. The cecum is a pouch-like structure of the colon, located at the junction of the small and the large intestines. The term \"vermiform\" comes from Latin and means \"worm-shaped.\" The appendix used to be considered a vestigial organ, but this view has changed over the past decades.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Appendix";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Arm(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " the part of the upper limb between the glenohumeral joint (shoulder joint) and the elbow joint. In common usage, the arm extends to the hand. It can be divided into the upper arm, which extends from the shoulder to the elbow, the forearm which extends from the elbow to the hand, and the hand. Anatomically the shoulder girdle with bones and corresponding muscles is by definition a part of the arm. The Latin term brachium may refer to either the arm as a whole or to the upper arm on its own.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Arm";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Arteriole(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a small-diameter blood vessel in the microcirculation that extends and branches out from an artery and leads to capillaries. Arterioles have muscular walls (usually only one to two layers of smooth muscle) and are the primary site of vascular resistance. The greatest change in blood pressure and velocity of blood flow occurs at the transition of arterioles to capillaries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Arteriole";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Artery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a blood vessel that takes blood away from the heart to all parts of the body (tissues, lungs, etc.). Most arteries carry oxygenated blood; the two exceptions are the pulmonary and the umbilical arteries, which carry deoxygenated blood to the organs that oxygenate it. The effective arterial blood volume is that extracellular fluid which fills the arterial system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Arthritis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a term often used to mean any disorder that affects joints. Symptoms generally include joint pain and stiffness. Other symptoms may include redness, warmth, swelling, and decreased range of motion of the affected joints.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Arthritis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AspergerSyndrome(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a developmental disorder characterized by significant difficulties in social interaction and nonverbal communication, along with restricted and repetitive patterns of behavior and interests. As a milder autism spectrum disorder (ASD), it differs from other ASDs by relatively normal language and intelligence. Although not required for diagnosis, physical clumsiness and unusual use of language are common.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Asperger Syndrome";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Asthma(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a common long-term inflammatory disease of the airways of the lungs. It is characterized by variable and recurring symptoms, reversible airflow obstruction, and bronchospasm. Symptoms include episodes of wheezing, coughing, chest tightness, and shortness of breath.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Asthma";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AtrialFibrillation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an abnormal heart rhythm characterized by rapid and irregular beating of the atria. Often it starts as brief periods of abnormal beating which become longer and possibly constant over time. Often episodes have no symptoms.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Atrial Fibrillation";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Auscultation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "listening to the internal sounds of the body, usually using a stethoscope. Auscultation is performed for the purposes of examining the circulatory and respiratory systems (heart and breath sounds), as well as the gastrointestinal system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Auscultation";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Autism(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a developmental disorder characterized by troubles with social interaction and communication, and by restricted and repetitive behavior. Parents usually notice signs during the first two or three years of their child\'s life. These signs often develop gradually, though some children with autism reach their developmental milestones at a normal pace before worsening.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Autism";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Axilla(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "(also, armpit, underarm or oxter) is the area on the human body directly under the joint where the arm connects to the shoulder. It also provides the under-arm sweat gland.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Axilla";
        }
    }

    return "NULL";
}

string _Medicine::Terms::AxillaryArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large blood vessel that conveys oxygenated blood to the lateral aspect of the thorax, the axilla (armpit) and the upper limb. Its origin is at the lateral margin of the first rib, before which it is called the subclavian artery.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Axillary Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Back(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The human back is the large posterior area of the human body, rising from the top of the buttocks to the back of the neck and the shoulders. It is the surface of the body opposite from the chest. The vertebral column runs the length of the back and creates a central area of recession. The breadth of the back is created by the shoulders at the top and the pelvis at the bottom.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Back";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BackPain(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "is pain felt in the back. It is divided into neck pain (cervical), middle back pain (thoracic), lower back pain (lumbar) or coccydynia (tailbone or sacral pain) based on the segment affected. The lumbar area is the most common area for pain, as it supports most of the weight in the upper body. Episodes of back pain may be acute, sub-acute, or chronic depending on the duration. The pain may be characterized as a dull ache, shooting or piercing pain, or a burning sensation. Discomfort can radiate into the arms and hands as well as the legs or feet, and may include numbness, or weakness in the legs and arms.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Back Pain";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Barotrauma(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "injury caused by a pressure difference between tissues and a gas filled space.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Barotrauma";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BetaCell(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Beta cells (B cells) are a type of cell found in pancreatic islets that synthesize and secrete insulin. Beta cells make up 50-70% of the cells in human islets. In patients with type I or type II diabetes, beta-cell mass and function are diminished, leading to insufficient insulin secretion and hyperglycemia.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Beta Cell";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Biceps(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large muscle that lies on the front of the upper arm between the shoulder and the elbow. Both heads of the muscle arise on the scapula and join to form a single muscle belly which is attached to the upper forearm. While the biceps crosses both the shoulder and elbow joints, its main function is at the elbow where it flexes the forearm and supinates the forearm. Both these movements are used when opening a bottle with a corkscrew: first biceps unscrews the cork (supination), then it pulls the cork out (flexion).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biceps";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BileDuct(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "any of a number of long tube-like structures that carry bile. Bile, required for the digestion of food, is secreted by the liver into passages that carry bile toward the hepatic duct, which joins with the cystic duct (carrying bile to and from the gallbladder) to form the common bile duct, which opens into the intestine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bile Duct";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BiliaryTract(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the liver, gall bladder and bile ducts, and how they work together to make, store and secrete bile. Bile consists of water, electrolytes, bile acids, cholesterol, phospholipids and conjugated bilirubin. Some components are synthesised by hepatocytes (liver cells), the rest are extracted from the blood by the liver.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biliary Tract";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BingeEatingDisorder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an eating disorder characterized by frequent and recurrent binge eating episodes with associated negative psychological and social problems, but without subsequent purging episodes (e.g. vomiting). BED is a recently described condition, which was required to distinguish binge eating similar to that seen in bulimia nervosa but without characteristic purging. Individuals who are diagnosed with bulimia nervosa and binge eating disorder exhibit similar patterns of compulsive overeating, neurobiological features of dysfunctional cognitive control and food addiction, and biological and environmental risk factors. Indeed, some consider BED a milder version of bulimia, and that the conditions are on the same spectrum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Binge Eating Disorder";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BiologicalEngineering(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the application of principles of biology and the tools of engineering to create usable, tangible, economically viable products. Biological engineering employs knowledge and expertise from a number of pure and applied sciences, such as mass and heat transfer, kinetics, biocatalysts, biomechanics, bioinformatics, separation and purification processes, bioreactor design, surface science, fluid mechanics, thermodynamics, and polymer science. It is used in the design of medical devices, diagnostic equipment, biocompatible materials, renewable bioenergy, ecological engineering, agricultural engineering, and other areas that improve the living standards of societies.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biological Engineering";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Biology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the natural science that studies life and living organisms, including their physical structure, chemical processes, molecular interactions, physiological mechanisms, development and evolution.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Biochemistry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "sometimes called biological chemistry, is the study of chemical processes within and relating to living organisms.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biochemistry";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Bioinformatics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an interdisciplinary field that develops methods and software tools for understanding biological data. As an interdisciplinary field of science, bioinformatics combines biology, computer science, information engineering, mathematics and statistics to analyze and interpret biological data.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bioinformatics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Biopsy(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical test commonly performed by a surgeon, interventional radiologist, or an interventional cardiologist involving extraction of sample cells or tissues for examination to determine the presence or extent of a disease.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biopsy";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Biostatistics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the application of statistics to a wide range of topics in biology. It encompasses the design of biological experiments, especially in medicine, pharmacy, agriculture and fishery; the collection, summarization, and analysis of data from those experiments; and the interpretation of, and inference from, the results. A major branch is medical biostatistics, which is exclusively concerned with medicine and health.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Biostatistics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BipolarDisorder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a mental disorder that causes periods of depression and periods of abnormally elevated mood";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bipolar Disorder";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BirthControl(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as contraception and fertility control, is a method or device used to prevent pregnancy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Birth Control";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BladderCancer(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "any of several types of cancer arising from the tissues of the urinary bladder. It is a disease in which cells grow abnormally and have the potential to spread to other parts of the body. Symptoms include blood in the urine, pain with urination, and low back pain.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bladder Cancer";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BloodPressure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the pressure of circulating blood on the walls of blood vessels. Used without further specification, \"blood pressure\" usually refers to the pressure in large arteries of the systemic circulation. Blood pressure is usually expressed in terms of the systolic pressure (maximum during one heartbeat) over diastolic pressure (minimum in between two heartbeats) and is measured in millimeters of mercury (mmHg), above the surrounding atmospheric pressure.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Blood Pressure";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BloodVessel(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The blood vessels are the part of the circulatory system, and microcirculation, that transports blood throughout the human body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Blood Vessel";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Bone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a rigid organ that constitutes part of the vertebrate skeleton. Bones support and protect the various organs of the body, produce red and white blood cells, store minerals, provide structure and support for the body, and enable mobility. Bones come in a variety of shapes and sizes and have a complex internal and statical structure. They are lightweight yet strong and hard, and serve multiple functions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bone";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BoneMarrow(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a semi-solid tissue which may be found within the spongy or cancellous portions of bones. Bone marrow is the primary site of new blood cell production or hematopoiesis. It is composed of hematopoietic cells, marrow adipose tissue, and supportive stromal cells. In adult humans, bone marrow is primarily located in the ribs, vertebrae, sternum, and bones of the pelvis. On average, bone marrow constitutes 4% of the total body mass of humans; in an adult having 65 kilograms of mass (143 lb), bone marrow typically accounts for approximately 2.6 kilograms (5.7 lb).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bone Marrow";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BrachialArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the major blood vessel of the (upper) arm. It is the continuation of the axillary artery beyond the lower margin of teres major muscle. It continues down the ventral surface of the arm until it reaches the cubital fossa at the elbow. It then divides into the radial and ulnar arteries which run down the forearm. In some individuals, the bifurcation occurs much earlier and the ulnar and radial arteries extend through the upper arm. The pulse of the brachial artery is palpable on the anterior aspect of the elbow, medial to the tendon of the biceps, and, with the use of a stethoscope and sphygmomanometer (blood pressure cuff) often used to measure the blood pressure.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brachial Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BrachialPlexus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a network of nerves formed by the ventral rami of the lower four cervical nerves and first thoracic nerve (C5, C6, C7, C8, and T1). This plexus extends from the spinal cord, through the cervicoaxillary canal in the neck, over the first rib, and into the armpit. It supplies afferent and efferent nerve fibers to the chest, shoulder, arm and hand.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brachial Plexus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BrachialVeins(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In human anatomy, the brachial veins are venae comitantes of the brachial artery in the arm proper. Because they are deep to muscle, they are considered deep veins. Their course is that of the brachial artery (in reverse): they begin where radial veins and ulnar veins join (corresponding to the bifurcation of the brachial artery). They end at the inferior border of the teres major muscle. At this point, the brachial veins join the basilic vein to form the axillary vein. The brachial veins also have small tributaries that drain the muscles of the upper arm, such as biceps brachii muscle and triceps brachii muscle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brachial Veins";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Brachioradialis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscle of the forearm that flexes the forearm at the elbow. It is also capable of both pronation and supination, depending on the position of the forearm. It is attached to the distal styloid process of the radius by way of the brachioradialis tendon, and to the lateral supracondylar ridge of the humerus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brachioradialis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Bradycardia(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a condition typically defined wherein an individual has a resting heart rate of under 60 beats per minute (BPM) in adults.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bradycardia";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Brain(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the central organ of the human nervous system, and with the spinal cord makes up the central nervous system. The brain consists of the cerebrum, the brainstem and the cerebellum. It controls most of the activities of the body, processing, integrating, and coordinating the information it receives from the sense organs, and making decisions as to the instructions sent to the rest of the body. The brain is contained in, and protected by, the skull bones of the head.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brain";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BrainTumor(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "occurs when abnormal cells form within the brain. There are two main types of tumors: malignant or cancerous tumors and benign tumors.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brain Tumor";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BrainMetastasis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a cancer that has metastasized (spread) to the brain from another location in the body and is therefore considered a secondary brain tumor. The metastasis typically shares a cancer cell type with the original site of the cancer.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brain Metastasis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Breast(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of two prominences located on the upper ventral region of the torso of primates. In females, it serves as the mammary gland, which produces and secretes milk to feed infants. Both females and males develop breasts from the same embryological tissues. At puberty, estrogens, in conjunction with growth hormone, cause breast development in female humans and to a much lesser extent in other primates. Breast development in other primate females generally only occurs with pregnancy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Breast";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BreastCancer(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "cancer that develops from breast tissue. Signs of breast cancer may include a lump in the breast, a change in breast shape, dimpling of the skin, fluid coming from the nipple, a newly inverted nipple, or a red or scaly patch of skin. In those with distant spread of the disease, there may be bone pain, swollen lymph nodes, shortness of breath, or yellow skin.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Breast Cancer";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BrocasArea(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a region in the frontal lobe of the dominant hemisphere, usually the left, of the brain with functions linked to speech production.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Brocas Area";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Bronchiole(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the passageways by which air passes through the nose or mouth to the alveoli (air sacs) of the lungs, in which branches no longer contain cartilage or glands in their submucosa. They are branches of the bronchi, and are part of the conducting zone of the respiratory system. The bronchioles divide further into smaller terminal bronchioles which are still in the conducting zone and these then divide into the smaller respiratory bronchioles which mark the beginning of the respiratory region.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bronchiole";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Bronchus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a passage of airway in the respiratory system that conducts air into the lungs. The first bronchi to branch from the trachea are the right main bronchus and the left main bronchus. These are the widest and enter the lungs at each hilum, where they branch into narrower secondary bronchi known as lobar bronchi, and these branch into narrower tertiary bronchi known as segmental bronchi. Further divisions of the segmental bronchi are known as 4th order, 5th order, and 6th order segmental bronchi, or grouped together as subsegmental bronchi. ";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bronchus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Bruit(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the abnormal sound generated by turbulent flow of blood in an artery due to either an area of partial obstruction or a localized high rate of blood flow through an unobstructed artery.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bruit";
        }
    }

    return "NULL";
}

string _Medicine::Terms::BulimiaNervosa(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as simply bulimia, is an eating disorder characterized by binge eating followed by purging. Binge eating refers to eating a large amount of food in a short amount of time. Purging refers to the attempts to get rid of the food consumed.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Bulimia Nervosa";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Buttocks(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "two rounded portions of the anatomy, located on the posterior of the pelvic region and comprise a layer of fat superimposed on the gluteus maximus and gluteus medius muscles. Physiologically, the buttocks enable weight to be taken off the feet while sitting.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Buttocks";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Calcium(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Calcium ions (Ca2+) contribute to the physiology and biochemistry of organisms and the cell. They play an important role in signal transduction pathways, where they act as a second messenger, in neurotransmitter release from neurons, in contraction of all muscle cell types, and in fertilization. Many enzymes require calcium ions as a cofactor, those of the blood-clotting cascade being notable examples. Extracellular calcium is also important for maintaining the potential difference across excitable cell membranes, as well as proper bone formation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Calcium";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Calf(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the back portion of the lower leg in human anatomy. The muscles within the calf correspond to the posterior compartment of the leg. The two largest muscles within this compartment are known together as the calf muscle and attach to the heel via the Achilles tendon. Several other, smaller muscles attach to the knee, the ankle, and via long tendons to the toes.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Calf";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cancer(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of diseases involving abnormal cell growth with the potential to invade or spread to other parts of the body. These contrast with benign tumors, which do not spread to other parts of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cancer";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Capillary(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a small blood vessel from 5 to 10 micrometres (µm) in diameter, and having a wall one endothelial cell thick. They are the smallest blood vessels in the body: they convey blood between the arterioles and venules. These microvessels are the site of exchange of many substances with the interstitial fluid surrounding them.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Capillary";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Carcinogen(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "any substance, radionuclide, or radiation that promotes carcinogenesis, the formation of cancer. This may be due to the ability to damage the genome or to the disruption of cellular metabolic processes.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carcinogen";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Carcinogenesis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also called oncogenesis or tumorigenesis, is the formation of a cancer, whereby normal cells are transformed into cancer cells.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carcinogenesis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CardiacArrest(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a sudden loss of blood flow resulting from the failure of the heart to effectively pump. Symptoms include loss of consciousness and abnormal or absent breathing. Some individuals may experience chest pain, shortness of breath, or nausea before cardiac arrest. If not treated within minutes, it typically leads to death.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiac Arrest";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CardiacCatheterization(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the insertion of a catheter into a chamber or vessel of the heart. This is done both for diagnostic and interventional purposes.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiac Catheterization";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CardiacMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of three types of vertebrate muscles, with the other two being skeletal and smooth muscles. It is an involuntary, striated muscle that constitutes the main tissue of the walls of the heart. The myocardium forms a thick middle layer between the outer layer of the heart wall (the epicardium) and the inner layer (the endocardium), with blood supplied via the coronary circulation. It is composed of individual heart muscle cells (cardiomyocytes) joined by intercalated discs, encased by collagen fibres and other substances that form the extracellular matrix.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiac Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CardiacSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "or cardiovascular surgery, is surgery on the heart or great vessels performed by cardiac surgeons. It is often used to treat complications of ischemic heart disease (for example, with coronary artery bypass grafting); to correct congenital heart disease; or to treat valvular heart disease from various causes, including endocarditis, rheumatic heart disease, and atherosclerosis. It also includes heart transplantation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiac Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cardiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine dealing with disorders of the heart as well as parts of the circulatory system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CardiothoracicSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the field of medicine involved in surgical treatment of organs inside the thorax (the chest)-generally treatment of conditions of the heart (heart disease) and lungs (lung disease).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiothoracic Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CardiovascularDisease(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a class of diseases that involve the heart or blood vessels. CVD includes coronary artery diseases (CAD) such as angina and myocardial infarction (commonly known as a heart attack). Other CVDs include stroke, heart failure, hypertensive heart disease, rheumatic heart disease, cardiomyopathy, heart arrhythmia, congenital heart disease, valvular heart disease, carditis, aortic aneurysms, peripheral artery disease, thromboembolic disease, and venous thrombosis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cardiovascular Disease";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CarotidArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " are arteries that supply the head and neck with oxygenated blood; they divide in the neck to form the statical and internal carotid arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carotid Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CarotidArterystatical(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The statical carotid artery is a major artery of the head and neck. It arises from the common carotid artery when it splits into the statical and internal carotid artery. It supplies blood to the face and neck.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carotid Artery statical";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CarotidArteryInternal(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The internal carotid artery is a major paired artery, one on each side of the head and neck, in human anatomy. They arise from the common carotid arteries where these bifurcate into the internal and statical carotid arteries at cervical vertebral level 3 or 4; the internal carotid artery supplies the brain, while the statical carotid nourishes other portions of the head, such as face, scalp, skull, and meninges.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carotid Artery Internal";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CarotidArteryStenosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a narrowing or constriction of any part of the carotid arteries, usually caused by atherosclerosis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carotid Artery Stenosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CarpalBones(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the eight small bones that make up the wrist (or carpus) that connects the hand to the forearm. In human anatomy, the main role of the wrist is to facilitate effective positioning of the hand and powerful use of the extensors and flexors of the forearm, and the mobility of individual carpal bones increase the freedom of movements at the wrist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carpal Bones";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CarpalTunnelSyndrome(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical condition due to compression of the median nerve as it travels through the wrist at the carpal tunnel. The main symptoms are pain, numbness and tingling in the thumb, index finger, middle finger and the thumb side of the ring fingers.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Carpal Tunnel Syndrome";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cartilage(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a resilient and smooth elastic tissue, a rubber-like padding that covers and protects the ends of long bones at the joints, and is a structural component of the rib cage, the ear, the nose, the bronchial tubes, the intervertebral discs, and many other body components. It is not as hard and rigid as bone, but it is much stiffer and much less flexible than muscle. The matrix of cartilage is made up of chondrin.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cartilage";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CartilaginousJoint(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "connected entirely by cartilage (fibrocartilage or hyaline). Cartilaginous joints allow more movement between bones than a fibrous joint but less than the highly mobile synovial joint. Cartilaginous joints also forms the growth regions of immature long bones and the intervertebral discs of the spinal column.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cartilaginous Joint";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Catheter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a thin tube made from medical grade materials serving a broad range of functions. Catheters are medical devices that can be inserted in the body to treat diseases or perform a surgical procedure. By modifying the material or adjusting the way catheters are manufactured, it is possible to tailor catheters for cardiovascular, urological, gastrointestinal, neurovascular, and ophthalmic applications.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Catheter";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CellBiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of biology that studies the structure and function of the cell, which is the basic unit of life. Cell biology is concerned with the physiological properties, metabolic processes, signaling pathways, life cycle, chemical composition and interactions of the cell with their environment.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cell Biology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CentralNervousSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the part of the nervous system consisting of the brain and spinal cord.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Central Nervous System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CephalicVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a superficial vein in the arm. It communicates with the basilic vein via the median cubital vein at the elbow and is located in the superficial fascia along the anterolateral surface of the biceps brachii muscle. Near the shoulder, the cephalic vein passes between the deltoid and pectoralis major muscles (deltopectoral groove) and through the deltopectoral triangle, where it empties into the axillary vein.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cephalic Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cerebellum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " a major feature of the hindbrain of all vertebrates. Although usually smaller than the cerebrum, in some animals such as the mormyrid fishes it may be as large as or even larger. In humans, the cerebellum plays an important role in motor control. It may also be involved in some cognitive functions such as attention and language as well as in regulating fear and pleasure responses, but its movement-related functions are the most solidly established. The human cerebellum does not initiate movement, but contributes to coordination, precision, and accurate timing: it receives input from sensory systems of the spinal cord and from other parts of the brain, and integrates these inputs to fine-tune motor activity. Cerebellar damage produces disorders in fine movement, equilibrium, posture, and motor learning in humans.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cerebellum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cerebrum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large part of the brain containing the cerebral cortex (of the two cerebral hemispheres), as well as several subcortical structures, including the hippocampus, basal ganglia, and olfactory bulb. In the human brain, the cerebrum is the uppermost region of the central nervous system. The prosencephalon is the embryonic structure from which the cerebrum develops prenatally. In mammals, the dorsal telencephalon, or pallium, develops into the cerebral cortex, and the ventral telencephalon, or subpallium, becomes the basal ganglia. The cerebrum is also divided into approximately symmetric left and right cerebral hemispheres. With the assistance of the cerebellum, the cerebrum controls all voluntary actions in the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cerebrum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CervicalCancer(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a cancer arising from the cervix. It is due to the abnormal growth of cells that have the ability to invade or spread to other parts of the body. Early on, typically no symptoms are seen. Later symptoms may include abnormal vaginal bleeding, pelvic pain, or pain during sexual intercourse. While bleeding after sex may not be serious, it may also indicate the presence of cervical cancer.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cervical Cancer";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cervix(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the lower part of the uterus in the human female reproductive system. The cervix is usually 2 to 3 cm long (~1 inch) and roughly cylindrical in shape, which changes during pregnancy. The narrow, central cervical canal runs along its entire length, connecting the uterine cavity and the lumen of the vagina. The opening into the uterus is called the internal os, and the opening into the vagina is called the statical os. The lower part of the cervix, known as the vaginal portion of the cervix (or ectocervix), bulges into the top of the vagina.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cervix";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cheek(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the area of the face below the eyes and between the nose and the left or right ear. \"Buccal\" means relating to the cheek. In humans, the region is innervated by the buccal nerve. The area between the inside of the cheek and the teeth and gums is called the vestibule or buccal pouch or buccal cavity and forms part of the mouth.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cheek";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Chin(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the area of the face below the lower lip and including the mandibular prominence. It is formed by the lower front of the mandible.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Chin";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ChronicFatigueSyndrome(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also referred to as myalgic encephalomyelitis (ME), is a medical condition characterized by long-term fatigue and other persistent symptoms that limit a person\'s ability to carry out ordinary daily activities.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Chronic Fatigue Syndrome";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CiliaryMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a ring of smooth muscle in the eye\'s middle layer (vascular layer) that controls accommodation for viewing objects at varying distances and regulates the flow of aqueous humor into Schlemm\'s canal. It changes the shape of the lens within the eye, not the size of the pupil which is carried out by the sphincter pupillae muscle and dilator pupillae.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ciliary Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CiliarySulcus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The space between the anterior surface of the ciliary body of the eye and the posterior surface of the base of the iris. It is one of the sites for intraocular lens implantation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ciliary Sulcus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CirculatorySystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also called the cardiovascular system or the vascular system, is an organ system that permits blood to circulate and transport nutrients (such as amino acids and electrolytes), oxygen, carbon dioxide, hormones, and blood cells to and from the cells in the body to provide nourishment and help in fighting diseases, stabilize temperature and pH, and maintain homeostasis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Circulatory System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Clavicle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long bone that serves as a strut between the shoulder blade and the sternum. There are two, one on the right, and one on the left side of the trunk. Along with the shoulder blade, the clavicles make up the shoulder girdle. The clavicle has many functions. It connects the axial and appendicular skeleton in conjunction with the scapula, helps extend range of motion, and protects neurovascular structures.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Clavicle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Clinic(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a healthcare facility that is primarily focused on the care of outpatients. Clinics can be privately operated or publicly managed and funded.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Clinic";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ClinicalResearch(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " a branch of healthcare science that determines the safety and effectiveness (efficacy) of medications, devices, diagnostic products and treatment regimens intended for human use. These may be used for prevention, treatment, diagnosis or for relieving symptoms of a disease. Clinical research is different from clinical practice. In clinical practice established treatments are used, while in clinical research evidence is collected to establish a treatment.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Clinical Research";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CoeliacDisease(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long-term autoimmune disorder that primarily affects the small intestine. Classic symptoms include gastrointestinal problems such as chronic diarrhoea, abdominal distention, malabsorption, loss of appetite and among children failure to grow normally. This often begins between six months and two years of age. Non-classic symptoms are more common, especially in people older than two years. There may be mild or absent gastrointestinal symptoms, a wide number of symptoms involving any part of the body or no obvious symptoms. Coeliac disease was first described in childhood; however, it may develop at any age. It is associated with other autoimmune diseases, such as diabetes mellitus type 1 and thyroiditis, among others.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Coeliac Disease";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CommonCarotidArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the left and right common carotid arteries (carotids) ) are arteries that supply the head and neck with oxygenated blood; they divide in the neck to form the statical and internal carotid arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Common Carotid Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CommonCold(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a viral infectious disease of the upper respiratory tract that primarily affects the nose. The throat, sinuses, and larynx may also be affected. Signs and symptoms may appear less than two days after exposure to the virus. These may include coughing, sore throat, runny nose, sneezing, headache, and fever. People usually recover in seven to ten days, but some symptoms may last up to three weeks. Occasionally, those with other health problems may develop pneumonia.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Common Cold";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CommonIliacArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The common iliac arteries are two large arteries that originate from the aortic bifurcation at the level of the fourth lumbar vertebra. They end in front of the sacroiliac joint, one on either side, and each bifurcates into the statical and internal iliac arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Common Iliac Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CommonIliacVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "formed by the statical iliac veins and internal iliac veins. The left and right common iliac veins come together in the abdomen at the level of the fifth lumbar vertebra, forming the inferior vena cava. They drain blood from the pelvis and lower limbs. Both common iliac veins are accompanied along their course by common iliac arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Common Iliac Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CoronaryArteries(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the blood vessels (arteries) of coronary circulation, which transports oxygenated blood to the actual heart muscle. The heart requires a continuous supply of oxygen to function and survive, much like any other tissue or organ of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Coronary Arteries";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CorpusCallosum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a wide, thick nerve tract consisting of a flat bundle of commissural fibers, beneath the cerebral cortex in the brain. The corpus callosum is only found in placental mammals. It spans part of the longitudinal fissure, connecting the left and right cerebral hemispheres, enabling communication between them. It is the largest white matter structure in the human brain, about ten centimetres in length and consisting of 200-300 million axonal projections.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Corpus Callosum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::CranialNerves(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the nerves that emerge directly from the brain (including the brainstem), in contrast to spinal nerves (which emerge from segments of the spinal cord). Ten of the cranial nerves originate in the brainstem. Cranial nerves relay information between the brain and parts of the body, primarily to and from regions of the head and neck.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cranial Nerves";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cure(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a substance or procedure that ends a medical condition, such as a medication, a surgical operation, a change in lifestyle or even a philosophical mindset that helps end a person\'s sufferings; or the state of being healed, or cured.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cure";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cytogenetics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of genetics that is concerned with how the chromosomes relate to cell behaviour, particularly to their behaviour during mitosis and meiosis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cytogenetics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Cytokines(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a broad and loose category of small proteins (~5-20 kDa) that are important in cell signaling. Cytokines are peptides, and cannot cross the lipid bilayer of cells to enter the cytoplasm.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Cytokines";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DecompressionSickness(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a condition caused by inert gas bubbles forming in supersaturated tissues after a reduction in ambient pressure, and either obstructing perfusion or causing local damage.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Decompression Sickness";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DeepCircumflexIliacVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "ormed by the union of the venae comitantes of the deep iliac circumflex artery, and joins the statical iliac vein about 2 cm. above the inguinal ligament. It also receives small tributary branches from the thoracoepigastric vein";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Deep Circumflex Iliac Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DeepTemporalArteries(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The deep temporal arteries, two in number, anterior and posterior, ascend between the temporalis and the pericranium. They supply the muscle, and anastomose with the middle temporal artery. The anterior communicates with the lacrimal artery by means of small branches which perforate the zygomatic bone and great wing of the sphenoid.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Deep Temporal Arteries";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DefinitiveTreatment(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " Medical treatment generally accepted as most appropriate for the condition.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Definitive Treatment";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DeltoidMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the muscle forming the rounded contour of the human shoulder. Anatomically, it appears to be made up of three distinct sets of fibers though electromyography suggests that it consists of at least seven groups that can be independently coordinated by the nervous system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Deltoid Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Dentistry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine that consists of the study, diagnosis, prevention, and treatment of diseases, disorders, and conditions of the oral cavity, commonly in the dentition but also the oral mucosa, and of adjacent and related structures and tissues, particularly in the maxillofacial (jaw and facial) area.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dentistry";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Dermatitis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as eczema, is a group of diseases that result in inflammation of the skin. These diseases are characterized by itchiness, red skin and a rash. In cases of short duration, there may be small blisters, while in long-term cases the skin may become thickened. The area of skin involved can vary from small to the entire body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dermatitis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Diagnosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the process of determining which disease or condition explains a person\'s symptoms and signs. It is most often referred to as diagnosis with the medical context being implicit. The information required for diagnosis is typically collected from a history and physical examination of the person seeking medical care. Often, one or more diagnostic procedures, such as medical tests, are also done during the process. Sometimes posthumous diagnosis is considered a kind of medical diagnosis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Diagnosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DiabetesMellitus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of metabolic disorders characterized by a high blood sugar level over a prolonged period. Symptoms of high blood sugar include frequent urination, increased thirst, and increased hunger. If left untreated, diabetes can cause many complications. Acute complications can include diabetic ketoacidosis, hyperosmolar hyperglycemic state, or death. Serious long-term complications include cardiovascular disease, stroke, chronic kidney disease, foot ulcers, and damage to the eyes.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Diabetes Mellitus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DietaryReferenceIntake(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a system of nutrition recommendations from the Institute of Medicine (IOM) of the National Academies (United States).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Dietary Reference Intake";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DifferentialDiagnosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the distinguishing of a particular disease or condition from others that present similar clinical features.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Differential Diagnosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::DigestiveSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "consists of the gastrointestinal tract plus the accessory organs of digestion (the tongue, salivary glands, pancreas, liver, and gallbladder). Digestion involves the breakdown of food into smaller and smaller components, until they can be absorbed and assimilated into the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Digestive System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Disease(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an abnormal condition in an organism, or part of it, that negatively affects structure or function. Disease can be caused by statical factors, or internal dysfunctions, such as abnormal immune responses.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Disease";
        }
    }

    return "NULL";
}
string _Medicine::Terms::Ear(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the organ of hearing and, in mammals, balance. In mammals, the ear is usually described as having three parts-the outer ear, the middle ear and the inner ear. The outer ear consists of the pinna and the ear canal. Since the outer ear is the only visible portion of the ear in most animals, the word \"ear\" often refers to the statical part alone. The middle ear includes the tympanic cavity and the three ossicles. The inner ear sits in the bony labyrinth, and contains structures which are key to several senses: the semicircular canals, which enable balance and eye tracking when moving; the utricle and saccule, which enable balance when stationary; and the cochlea, which enables hearing. The ears of vertebrates are placed somewhat symmetrically on either side of the head, an arrangement that aids sound localisation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ear";
        }
    }

    return "NULL";
}

string _Medicine::Terms::EarInfection(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a general term for inflammation or infection of the ear, in both humans and other animals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ear Infection";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Otitisstatica(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "statical otitis, or \"swimmer\'s ear\", involves the outer ear and ear canal. In statical otitis, the ear hurts when touched or pulled.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Otitis statica";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OtitisMedia(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "middle ear infection, involves the middle ear. In otitis media, the ear is infected or clogged with fluid behind the ear drum, in the normally air-filled middle-ear space. This very common childhood infection sometimes requires a surgical procedure called myringotomy and tube insertion.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Otitis Media";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OtitisInterna(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "labyrinthitis, involves the inner ear. The inner ear includes sensory organs for balance and hearing. When the inner ear is inflamed, vertigo is a common symptom.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Otitis Interna";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Elbow(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "he visible joint between the upper and lower parts of the arm. It includes prominent landmarks such as the olecranon, the elbow pit, the lateral and medial epicondyles, and the elbow joint. The elbow joint is the synovial hinge joint between the humerus in the upper arm and the radius and ulna in the forearm which allows the forearm and hand to be moved towards and away from the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Elbow";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Embryology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the branch of biology that studies the prenatal development of gametes (sex cells), fertilization, and development of embryos and fetuses. Additionally, embryology encompasses the study of congenital disorders that occur before birth, known as teratology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Embryology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::EmergencyMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical specialty concerned with the care of illnesses or injuries requiring immediate medical attention. Emergency physicians care for unscheduled and undifferentiated patients of all ages. As first-line providers, their primary responsibility is to initiate resuscitation and stabilization and to start investigations and interventions to diagnose and treat illnesses in the acute phase.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Emergency Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::EndocrineSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a chemical messenger system comprising feedback loops of hormones released by internal glands of an organism directly into the circulatory system, regulating distant target organs. In humans, the major endocrine glands are the thyroid gland and the adrenal glands. In vertebrates, the hypothalamus is the neural control center for all endocrine systems. The study of the endocrine system and its disorders is known as endocrinology. Endocrinology is a branch of internal medicine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Endocrine System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Endocrinology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of biology and medicine dealing with the endocrine system, its diseases, and its specific secretions known as hormones. It is also concerned with the integration of developmental events proliferation, growth, and differentiation, and the psychological or behavioral activities of metabolism, growth and development, tissue function, sleep, digestion, respiration, excretion, mood, stress, lactation, movement, reproduction, and sensory perception caused by hormones. Specializations include behavioral endocrinology and comparative endocrinology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Endocrinology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Epidemiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the study and analysis of the distribution (who, when, and where), patterns and determinants of health and disease conditions in defined populations. It is a cornerstone of public health, and shapes policy decisions and evidence-based practice by identifying risk factors for disease and targets for preventive healthcare. Epidemiologists help with study design, collection, and statistical analysis of data, amend interpretation and dissemination of results (including peer review and occasional systematic review). Epidemiology has helped develop methodology used in clinical research, public health studies, and, to a lesser extent, basic research in the biological sciences.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Epidemiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Epiglottis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a leaf-shaped flap in the throat that prevents food from entering the windpipe and the lungs. It stands open during breathing, allowing air into the larynx. During swallowing, it closes to prevent aspiration of food into the lungs, forcing the swallowed liquids or food to go along the esophagus toward the stomach instead. It is thus the valve that diverts passage to either the trachea or the esophagus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Epiglottis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Epilepsy(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of neurological disorders characterized by recurrent epileptic seizures. Epileptic seizures are episodes that can vary from brief and nearly undetectable periods to long periods of vigorous shaking. These episodes can result in physical injuries, including occasionally broken bones. In epilepsy, seizures have a tendency to recur and, as a rule, have no immediate underlying cause. Isolated seizures that are provoked by a specific cause such as poisoning are not deemed to represent epilepsy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Epilepsy";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ErectileDysfunction(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "ED also called impotence, is the type of sexual dysfunction in which the penis fails to become or stay erect during sexual activity. It is the most common sexual problem in men. Through its connection to self-image and to problems in sexual relationships, erectile dysfunction can cause psychological harm.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Erectile Dysfunction";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ErectorSpinaeMuscles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The erector spinae or spinal erectors is a set of muscles that straighten and rotate the back.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Erector Spinae Muscles";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Esophagus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "informally known as the food pipe or gullet, is an organ in vertebrates through which food passes, aided by peristaltic contractions, from the pharynx to the stomach. The esophagus is a fibromuscular tube, about 25 cm (10 in) long in adults, which travels behind the trachea and heart, passes through the diaphragm and empties into the uppermost region of the stomach. During swallowing, the epiglottis tilts backwards to prevent food from going down the larynx and lungs.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Esophagus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ExtensorPollicisBrevisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the extensor pollicis brevis is a skeletal muscle on the dorsal side of the forearm. It lies on the medial side of, and is closely connected with, the abductor pollicis longus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Extensor Pollicis Brevis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ExtensorPollicisEtIndicisCommunisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an aberrant muscle in the posterior compartment of forearm. It was first described in 1863. The muscle has a prevalence from 0.5% to 4%.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Extensor Pollicis Et Indicis Communis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ExtensorPollicisLongusMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a skeletal muscle located dorsally on the forearm. It is much larger than the extensor pollicis brevis, the origin of which it partly covers and acts to stretch the thumb together with this muscle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Extensor Pollicis Longus Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::staticalCarotidArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a major artery of the head and neck. It arises from the common carotid artery when it splits into the statical and internal carotid artery. statical carotid artery supplies blood to the face and neck.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "statical Carotid Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::staticalIliacArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The statical iliac arteries are two major arteries which bifurcate off the common iliac arteries anterior to the sacroiliac joint of the pelvis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "statical Iliac Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::staticalIliacVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The statical iliac veins are large veins that connect the femoral veins to the common iliac veins. Their origin is at the inferior margin of the inguinal ligaments and they terminate when they join the internal iliac veins (to form the common iliac veins). Both statical iliac veins are accompanied along their course by statical iliac arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "statical Iliac Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::staticalJugularVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "receives the greater part of the blood from the exterior of the cranium and the deep parts of the face, being formed by the junction of the posterior division of the retromandibular vein with the posterior auricular vein.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "statical Jugular Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Eye(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a sense organ that reacts to light and allows vision. Rod and cone cells in the retina are photoreceptive cells which are able to detect visible light and convey this information to the brain. Eyes signal information which is used by the brain to elicit the perception of color, shape, depth, movement, and other features. The eye is part of the sensory nervous system. Similar to the eyes of other mammals, the human eye\'s non-image-forming photosensitive ganglion cells in the retina receive light signals which affect adjustment of the size of the pupil, regulation and suppression of the hormone melatonin, and entrainment of the circadian rhythm.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Eye";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Face(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the front of an animal\'s head that features three of the head\'s sense organs, the eyes, nose, and mouth, and through which animals express many of their emotions. The face is crucial for human identity, and damage such as scarring or developmental deformities affects the psyche adversely.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Face";
        }
    }

    return "NULL";
}

string _Medicine::Terms::FallopianTube(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "tubes that stretch from the uterus to the ovaries, and are part of the female reproductive system. The fertilized egg passes through the fallopian tubes from the ovaries of female mammals to the uterus. The fallopian tube is simple columnar epithelium with hair-like extensions called cilia which carry the fertilized egg. In other animals, the equivalent of a fallopian tube is an oviduct.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Fallopian Tube";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Fellowship(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the period of medical training, in the United States and Canada, that a physician, dentist, or veterinarian may undertake after completing a specialty training program (residency). During this time (usually more than one year), the physician is known as a fellow. Fellows are capable of acting as an attending physician or a consultant physician in the specialist field in which they were trained, such as Internal Medicine or Pediatrics. After completing a fellowship in the relevant sub-specialty, the physician is permitted to practice without direct supervision by other physicians in that sub-specialty, such as Cardiology or Oncology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Fellowship";
        }
    }

    return "NULL";
}

string _Medicine::Terms::FemaleReproductiveSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "made up of the internal and statical sex organs that function in reproduction of new offspring. In humans, the female reproductive system is immature at birth and develops to maturity at puberty to be able to produce gametes, and to carry a foetus to full term. The internal sex organs are the uterus, fallopian tubes, and ovaries. The uterus or womb accommodates the embryo which develops into the foetus. The uterus also produces vaginal and uterine secretions which help the transit of sperm to the fallopian tubes. The ovaries produce the ova (egg cells). The statical sex organs are also known as the genitals and these are the organs of the vulva including the labia, clitoris, and vaginal opening. The vagina is connected to the uterus at the cervix.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Female Reproductive System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::FemoralArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large artery in the thigh and the main arterial supply to the thigh and leg. It enters the thigh from behind the inguinal ligament as the continuation of the statical iliac artery.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Femoral Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::FemoralNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a nerve in the thigh that supplies skin on the upper thigh and inner leg, and the muscles that extend the knee.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Femoral Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::FemoralVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a blood vessel that accompanies the femoral artery in the femoral sheath. It begins at the adductor hiatus (an opening in the adductor magnus muscle) and is a continuation of the popliteal vein. It ends at the inferior margin of the inguinal ligament, where it becomes the statical iliac vein. The femoral vein bears valves which are mostly bicuspid and whose number is variable between individuals and often between left and right leg.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Femoral Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Femur(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the proximal bone of the hindlimb in tetrapod vertebrate, the largest bone of the human body. The head of the femur articulates with the acetabulum in the pelvic bone forming the hip joint, while the distal part of the femur articulates with the tibia and kneecap, forming the knee joint.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Femur";
        }
    }

    return "NULL";
}

string _Medicine::Terms::FrontalNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the largest branch of the ophthalmic nerve (V1), itself a branch of the trigeminal nerve (CN V). The frontal nerve branches from the ophthalmic nerve immediately before entering the superior orbital fissure. In then travels superolateral to the annulus of Zinn between the lacrimal nerve and inferior ophthalmic vein. After entering the orbit it travels anteriorly between the roof periosteum and the levator palpebrae superioris. Midway between the apex and base of the orbit it divides into two branches, the supratrochlear nerve and supraorbital nerve. The two branches of the frontal nerve provide sensory innervation to the skin of the forehead, mucosa of the frontal sinus, and the skin of the upper eyelid.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Frontal Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Gallbladder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a small hollow organ where bile is stored and concentrated before it is released into the small intestine. In humans, the pear-shaped gallbladder lies beneath the liver, although the structure and position of the gallbladder can vary significantly among animal species. It receives and stores bile, produced by the liver, via the common hepatic duct and releases it via the common bile duct into the duodenum, where the bile helps in the digestion of fats.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gallbladder";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Gamete(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a haploid cell that fuses with another haploid cell during fertilization in organisms that reproduce sexually. Gametes are an organism\'s reproductive cells, also referred to as sex cells.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gamete";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ganglion(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of neuron cell bodies in the peripheral nervous system. In the somatic nervous system this includes dorsal root ganglia and trigeminal ganglia among a few others. In the autonomic nervous system there are both sympathetic and parasympathetic ganglia which contain the cell bodies of postganglionic sympathetic and parasympathetic neurons respectively.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ganglion";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::Ganglion(string text)
//{
//    string model = llm_model;
//    string definition = "a group of neuron cell bodies in the peripheral nervous system. In the somatic nervous system this includes dorsal root ganglia and trigeminal ganglia among a few others. In the autonomic nervous system there are both sympathetic and parasympathetic ganglia which contain the cell bodies of postganglionic sympathetic and parasympathetic neurons respectively.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no: " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Ganglion";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::GastrocnemiusMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a superficial two-headed muscle that is in the back part of the lower leg of humans. It runs from its two heads just above the knee to the heel, a three joint muscle (knee, ankle and subtalar joints). The muscle is named via Latin, from Greek ?αστ?? (gaster) \'belly\' or \'stomach\' and ???µ? (kn?me) \'leg\', meaning \'stomach of leg\' (referring to the bulging shape of the calf).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gastrocnemius Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Gastroenterology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the branch of medicine focused on the digestive system and its disorders. Diseases affecting the gastrointestinal tract, which include the organs from mouth into anus, along the alimentary canal, are the focus of this speciality.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gastroenterology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::GastrointestinalTract(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the tract from the mouth to the anus which includes all the organs of the digestive system in humans and other animals. Food taken in through the mouth is digested to extract nutrients and absorb energy, and the waste expelled as feces. The mouth, esophagus, stomach and intestines are all part of the gastrointestinal tract. Gastrointestinal is an adjective meaning of or pertaining to the stomach and intestines. A tract is a collection of related anatomic structures or a series of connected body organs.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gastrointestinal Tract";
        }
    }

    return "NULL";
}

string _Medicine::Terms::GeneTherapy(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical field which focuses on the utilization of the therapeutic delivery of nucleic acids into a patient\'s cells as a drug to treat disease.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gene Therapy";
        }
    }

    return "NULL";
}

string _Medicine::Terms::GeneralSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a surgical specialty that focuses on abdominal contents including esophagus, stomach, small intestine, large intestine, liver, pancreas, gallbladder, appendix and bile ducts, and often the thyroid gland (depending on local referral patterns). They also deal with diseases involving the skin, breast, soft tissue, trauma, Peripheral artery disease and hernias and perform endoscopic procedures such as gastroscopy and colonoscopy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "General Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Genetics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of biology concerned with the study of genes, genetic variation, and heredity in organisms.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Genetics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::GenitourinarySystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the organs of the reproductive system and the urinary system. These are grouped together because of their proximity to each other, their common embryological origin and the use of common pathways, like the male urethra. Also, because of their proximity, the systems are sometimes imaged together.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Genitourinary System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Geriatrics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a specialty that focuses on health care of elderly people. It aims to promote health by preventing and treating diseases and disabilities in older adults. There is no set age at which patients may be under the care of a geriatrician, or geriatric physician, a physician who specializes in the care of elderly people. Rather, this decision is determined by the individual patient\'s needs, and the availability of a specialist. It is important to note the difference between geriatrics, the care of aged people, and gerontology, which is the study of the aging process itself. The term geriatrics comes from the Greek ????? geron meaning \"old man\", and ?ατ??? iatros meaning \"healer\". However, geriatrics is sometimes called medical gerontology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Geriatrics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Gonad(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A gonad, sex gland, or reproductive gland is a mixed gland that produces the gametes (sex cells) and sex hormones of an organism. In the female of the species the reproductive cells are the egg cells, and in the male the reproductive cells are the sperm. The male gonad, the testicle, produces sperm in the form of spermatozoa. The female gonad, the ovary, produces egg cells. Both of these gametes are haploid cells. Some hermaphroditic animals have a type of gonad called an ovotestis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gonad";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Gout(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a form of inflammatory arthritis characterized by recurrent attacks of a red, tender, hot, and swollen joint. Pain typically comes on rapidly, reaching maximal intensity in less than 12 hours. The joint at the base of the big toe is affected in about half of cases. It may also result in tophi, kidney stones, or urate nephropathy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gout";
        }
    }

    return "NULL";
}

string _Medicine::Terms::GracilisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the most superficial muscle on the medial side of the thigh. It is thin and flattened, broad above, narrow and tapering below.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gracilis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::GreatSaphenousVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large, subcutaneous, superficial vein of the leg. It is the longest vein in the body, running along the length of the lower limb, returning blood from the foot, leg and thigh to the deep femoral vein at the femoral triangle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Great Saphenous Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Gynaecology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical practice dealing with the health of the female reproductive system. Almost all modern gynaecologists are also obstetricians (see obstetrics and gynaecology). In many areas, the specialities of gynaecology and obstetrics overlap. The term means \"the science of women\". Its counterpart is andrology, which deals with medical issues specific to the male reproductive system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Gynaecology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hand(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a prehensile, multi-fingered appendage located at the end of the forearm or forelimb of primates. The human hand normally has five digits: four fingers plus one thumb; these are often referred to collectively as five fingers, however, whereby the thumb is included as one of the fingers. It has 27 bones, not including sesmoid bones, the number of which varies between people, 14 of which are the phalanges (proximal, intermediate and distal) of the fingers and thumb. The metacarpal bones connect the fingers and the carpal bones of the wrist. Each human hand has five metacarpals and eight carpal bones.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hand";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HandSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "deals with both surgical and non-surgical treatment of conditions and problems that may take place in the hand or upper extremity (commonly from the tip of the hand to the shoulder) including injury and infection. Hand surgery may be practiced by graduates of general surgery, orthopedic surgery and plastic surgery. Chiroplasty, or cheiroplasty, is plastic surgery of the hands.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hand Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Head(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "at the top of the human body. It supports the face and is maintained by the skull, which itself encloses the brain. The human head consists of a fleshy outer portion, which surrounds the bony skull. The brain is enclosed within the skull. There are 22 bones in the human head. The head rests on the neck, and the seven cervical vertebrae support it. The human head typically weighs between 2.3 and 5 kilograms (5.1 and 11.0 lb) The face is the anterior part of the head, containing the eyes, nose, and mouth. On either side of the mouth, the cheeks provide a fleshy border to the oral cavity. The ears sit to either side of the head.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Head";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Health(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "\"a state of complete physical, mental and social well-being and not merely the absence of disease or infirmity.\" This definition has been subject to controversy, as it may have limited value for implementation. Health may be defined as the ability to adapt and manage physical, mental and social challenges throughout life.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Health";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HealthCare(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the maintenance or improvement of health via the prevention, diagnosis, treatment, recovery, or cure of disease, illness, injury, and other physical and mental impairments in people. Health care is delivered by health professionals and allied health fields. Physicians and physician associates are a part of these health professionals. Dentistry, pharmacy, midwifery, nursing, medicine, optometry, audiology, psychology, occupational therapy, physical therapy, athletic training and other health professions are all part of health care. It includes work done in providing primary care, secondary care, and tertiary care, as well as in public health.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Health Care";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hearing(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the ability to perceive sounds through an organ, such as an ear, by detecting vibrations as periodic changes in the pressure of a surrounding medium. The academic field concerned with hearing is auditory science.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hearing";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Heart(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscular organ found in humans and other animals. This organ pumps blood through the blood vessels. The heart and blood vessels together make the circulatory system. The pumped blood carries oxygen and nutrients to the tissue, while carrying metabolic waste such as carbon dioxide to the lungs. In humans, the heart is approximately the size of a closed fist and is located between the lungs, in the middle compartment of the chest, called the mediastinum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Heart";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Heel(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the prominence at the posterior end of the foot. It is based on the projection of one bone, the calcaneus or heel bone, behind the articulation of the bones of the lower leg.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Heel";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hematemesis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the vomiting of blood. It can be confused with hemoptysis (coughing up blood) or epistaxis (nosebleed), which are more common. The source is generally the upper gastrointestinal tract, typically above the suspensory muscle of duodenum. It may be caused by ulcers, tumors of the stomach or esophagus, varices, prolonged and vigorous retching, gastroenteritis, ingested blood (from bleeding in the mouth, nose, or throat), or certain drugs.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hematemesis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hematology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the branch of medicine concerned with the study of the cause, prognosis, treatment, and prevention of diseases related to blood. It involves treating diseases that affect the production of blood and its components, such as blood cells, hemoglobin, blood proteins, bone marrow, platelets, blood vessels, spleen, and the mechanism of coagulation. Such diseases might include hemophilia, sickle cell anemia, blood clots (thrombus), other bleeding disorders, and blood cancers such as leukemia, multiple myeloma, and lymphoma. The laboratory analysis of blood is frequently performed by a medical technologist or medical laboratory scientist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hematology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hematoma(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a localized bleeding outside of blood vessels, due to either disease or trauma including injury or surgery and may involve blood continuing to seep from broken capillaries. A hematoma is benign and is initially in liquid form spread among the tissues including in sacs between tissues where it may coagulate and solidify before blood is reabsorbed into blood vessels. An ecchymosis is a hematoma of the skin larger than 10 mm.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hematoma";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hematuria(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "defined as the presence of blood or red blood cells in the urine. \"Gross hematuria\" occurs when urine appears red, brown, or tea-colored due to the presence of blood. Hematuria may also be subtle and only detectable with a microscope or laboratory test. Blood that enters and mixes with the urine can come from any location within the urinary system, including the kidney, ureter, urinary bladder, urethra, and in men, the prostate. Common causes of hematuria include urinary tract infection (UTI), kidney stones, viral illness, trauma, bladder cancer, and exercise. These causes are grouped into glomerular and non-glomerular causes, depending on the involvement of the glomerulus of the kidney. But not all red urine is hematuria. Other substances such as certain medications and some foods (e.g. blackberries, beets, food dyes) can cause urine to appear red. Menstruation in women may also cause the appearance of hematuria and may result in a positive urine dipstick test for hematuria. A urine dipstick test may also give an incorrect positive result for hematuria if there are other substances in the urine such as myoglobin, a protein excreted into urine during rhabdomyolysis. A positive urine dipstick test should be confirmed with microscopy, where hematuria is defined by three or more red blood cells per high power field. When hematuria is detected, a thorough history and physical examination with appropriate further evaluation (e.g. laboratory testing) can help determine the underlying cause.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hematuria";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hemodialysis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a process of filtering the blood of a person whose kidneys are not working normally. This type of dialysis achieves the extracorporeal removal of waste products such as creatinine and urea and free water from the blood when the kidneys are in a state of kidney failure. Hemodialysis is one of three renal replacement therapies (the other two being kidney transplant and peritoneal dialysis). An alternative method for extracorporeal separation of blood components such as plasma or cells is apheresis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hemodialysis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hemolysis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the rupturing (lysis) of red blood cells (erythrocytes) and the release of their contents (cytoplasm) into surrounding fluid (e.g. blood plasma). Hemolysis may occur in vivo or in vitro.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hemolysis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hemoperfusion(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a method of filtering the blood extracorporeally (that is, outside the body) to remove a toxin. As with other extracorporeal methods, such as hemodialysis (HD), hemofiltration (HF), and hemodiafiltration (HDF), the blood travels from the patient into a machine, gets filtered, and then travels back into the patient, typically by venovenous access (out of a vein and back into a vein).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hemoperfusion";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Haemophilia(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a mostly inherited genetic disorder that impairs the body\'s ability to make blood clots, a process needed to stop bleeding. This results in people bleeding for a longer time after an injury, easy bruising, and an increased risk of bleeding inside joints or the brain. Those with a mild case of the disease may have symptoms only after an accident or during surgery. Bleeding into a joint can result in permanent damage while bleeding in the brain can result in long term headaches, seizures, or an altered level of consciousness.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Haemophilia";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hemoptysis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the discharge of blood or blood-stained mucus through the mouth coming from the bronchi, larynx, trachea, or lungs. It does not necessarily involve coughing. In other words, it is the airway bleeding. This can occur with lung cancer, infections such as tuberculosis, bronchitis, or pneumonia, and certain cardiovascular conditions. Hemoptysis is considered massive at 300 mL (11 imp fl oz; 10 US fl oz). In such cases, there are always severe injuries. The primary danger comes from choking, rather than blood loss.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hemoptysis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hemorrhoid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "vascular structures in the anal canal. In their normal state, they are cushions that help with stool control. They become a disease when swollen or inflamed; the unqualified term hemorrhoid is often used to refer to the disease. The signs and symptoms of hemorrhoids depend on the type present. Internal hemorrhoids often result in painless, bright red rectal bleeding when defecating. statical hemorrhoids often result in pain and swelling in the area of the anus. If bleeding occurs, it is usually darker. Symptoms frequently get better after a few days. A skin tag may remain after the healing of an statical hemorrhoid.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hemorrhoid";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hyperhidrosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical condition in which a person exhibits excessive sweating, more than is required for the regulation of body temperature. Although it is primarily a physical burden, hyperhidrosis can deteriorate the quality of life of the people who are affected, frequently leading to psychological, physical, and social consequences. Hyperhidrosis can lead to difficulties in professional fields, with more than 80% of patients experiencing moderate to severe emotional effects from the disease.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hyperhidrosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hypertension(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long-term medical condition in which the blood pressure in the arteries is persistently elevated. High blood pressure usually does not cause symptoms itself. It is, however, a major risk factor for stroke, coronary artery disease, heart failure, atrial fibrillation, peripheral arterial disease, vision loss, chronic kidney disease, and dementia. Hypertension is a major cause of premature death worldwide.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hypertension";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hyperkalemia(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "elevated level of potassium (K+) in the blood. Normal potassium levels are between 3.5 and 5.0 mmol/L (3.5 and 5.0 mEq/L) with levels above 5.5 mmol/L defined as hyperkalemia. Typically hyperkalemia does not cause symptoms. Occasionally when severe it can cause palpitations, muscle pain, muscle weakness, or numbness. Hyperkalemia can cause an abnormal heart rhythm which can result in cardiac arrest and death.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hyperkalemia";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HipBone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large flat bone, constricted in the center and expanded above and below. In some vertebrates (including humans before puberty) it is composed of three parts: the ilium, ischium, and the pubis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hip Bone";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Histology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the branch of biology that studies the microscopic anatomy of biological tissues. Histology is the microscopic counterpart to gross anatomy, which looks at larger structures visible without a microscope. Although one may divide microscopic anatomy into organology, the study of organs, histology, the study of tissues, and cytology, the study of cells, modern usage places all of these topics under the field of histology. In medicine, histopathology is the branch of histology that includes the microscopic identification and study of diseased tissue. In the field of paleontology, the term paleohistology refers to the histology of fossil organisms.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Histology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Homeostasis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the state of steady internal physical and chemical conditions maintained by living systems. This is the condition of optimal functioning for the organism and includes many variables, such as body temperature and fluid balance, being kept within certain pre-set limits (homeostatic range). Other variables include the pH of extracellular fluid, the concentrations of sodium, potassium, and calcium ions, as well as the blood sugar level, and these need to be regulated despite changes in the environment, diet, or level of activity. Each of these variables is controlled by one or more regulators or homeostatic mechanisms, which together maintain life.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Homeostasis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hormone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a class of signaling molecules in multicellular organisms that are sent to distant organs or tissues by complex biological processes to regulate physiology and behavior. Hormones are required for the normal development of animals, plants and fungi. Due to the broad definition of a hormone (as a signaling molecule that exerts its effects far from its site of production), numerous kinds of molecules can be classified as hormones. Among the substances that can be considered hormones, are eicosanoids (e.g. prostaglandins and thromboxanes), steroids (e.g. oestrogen and brassinosteroid), amino acid derivatives (e.g. epinephrine and auxin), protein or peptides (e.g. insulin and CLE peptides), and gases (e.g. ethylene and nitric oxide).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hormone";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hospice(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a type of health care that focuses on the palliation of a terminally ill patient\'s pain and symptoms and attending to their emotional and spiritual needs at the end of life. Hospice care prioritizes comfort and quality of life by reducing pain and suffering. Hospice care provides an alternative to therapies focused on life-prolonging measures that may be arduous, likely to cause more symptoms, or are not aligned with a person\'s goals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hospice";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hospital(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a healthcare institution providing patient treatment with specialized health science and auxiliary healthcare staff and medical equipment. The best-known type of hospital is the general hospital, which typically has an emergency department to treat urgent health problems ranging from fire and accident victims to a sudden illness. A district hospital typically is the major health care facility in its region, with many beds for intensive care and additional beds for patients who need long-term care.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hospital";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HospitalMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty that exists in some countries as a branch of family medicine or internal medicine, dealing with the care of acutely ill hospitalized patients. Physicians whose primary professional focus is caring for hospitalized patients only while they are in the hospital are called hospitalists. Originating in the United States, this type of medical practice has extended into Australia and Canada. The vast majority of physicians who refer to themselves as hospitalists focus their practice upon hospitalized patients. Hospitalists are not necessarily required to have separate board certification in hospital medicine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hospital Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanBack(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the large posterior area of the human body, rising from the top of the buttocks to the back of the neck. It is the surface of the body opposite from the chest and the abdomen. The vertebral column runs the length of the back and creates a central area of recession. The breadth of the back is created by the shoulders at the top and the pelvis at the bottom.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Back";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanBody(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the entire structure of a human being. It is composed of many different types of cells that together create tissues and subsequently organs and then organ systems.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Body";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanBrain(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the central organ of the nervous system, and with the spinal cord, comprises the central nervous system. It consists of the cerebrum, the brainstem and the cerebellum. The brain controls most of the activities of the body, processing, integrating, and coordinating the information it receives from the sensory nervous system. The brain integrates sensory information and coordinates instructions sent to the rest of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Brain";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanDigestiveSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "consists of the gastrointestinal tract plus the accessory organs of digestion (the tongue, salivary glands, pancreas, liver, and gallbladder). Digestion involves the breakdown of food into smaller and smaller components, until they can be absorbed and assimilated into the body. The process of digestion has three stages: the cephalic phase, the gastric phase, and the intestinal phase.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Digestive System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanEye(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a sensory organ in the visual system that reacts to visible light allowing eyesight. Other functions include maintaining the circadian rhythm, and keeping balance. The eye can be considered as a living optical device. It is approximately spherical in shape, with its outer layers, such as the outermost, white part of the eye (the sclera) and one of its inner layers (the pigmented choroid) keeping the eye essentially light tight except on the eye\'s optic axis. In order, along the optic axis, the optical components consist of a first lens (the cornea-the clear part of the eye) that accounts for most of the optical power of the eye and accomplishes most of the focusing of light from the outside world; then an aperture (the pupil) in a diaphragm (the iris-the coloured part of the eye) that controls the amount of light entering the interior of the eye; then another lens (the crystalline lens) that accomplishes the remaining focusing of light into images; and finally a light-sensitive part of the eye (the retina), where the images fall and are processed. The retina makes a connection to the brain via the optic nerve. The remaining components of the eye keep it in its required shape, nourish and maintain it, and protect it.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Eye";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanHead(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "at the top of the human body. It supports the face and is maintained by the skull, which itself encloses the brain.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Head";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanMouth(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the first portion of the alimentary canal that receives food and produces saliva. The oral mucosa is the mucous membrane epithelium lining the inside of the mouth.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Mouth";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanMusculoskeletalSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ system that gives humans the ability to move using their muscular and skeletal systems. The musculoskeletal system provides form, support, stability, and movement to the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Musculoskeletal System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanNose(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the first organ of the respiratory system. It is also the principal organ in the olfactory system. The shape of the nose is determined by the nasal bones and the nasal cartilages, including the nasal septum, which separates the nostrils and divides the nasal cavity into two.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Nose";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanReproductiveSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "includes the male reproductive system, which functions to produce and deposit sperm, and the female reproductive system, which functions to produce egg cells and to protect and nourish the fetus until birth. Humans have a high level of sexual differentiation. In addition to differences in nearly every reproductive organ, there are numerous differences in typical secondary sex characteristics.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Reproductive System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanSkeleton(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the internal framework of the human body. It is composed of around 270 bones at birth - this total decreases to around 206 bones by adulthood after some bones get fused together. The bone mass in the skeleton makes up about 14% of the total body weight (ca. 10-11 kg for an average person) and reaches maximum mass between the ages of 25 and 30. The human skeleton can be divided into the axial skeleton and the appendicular skeleton. The axial skeleton is formed by the vertebral column, the rib cage, the skull and other associated bones. The appendicular skeleton, which is attached to the axial skeleton, is formed by the shoulder girdle, the pelvic girdle and the bones of the upper and lower limbs.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Skeleton";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Humerus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long bone in the arm that runs from the shoulder to the elbow. It connects the scapula and the two bones of the lower arm, the radius and ulna, and consists of three sections. The humeral upper extremity consists of a rounded head, a narrow neck, and two short processes (tubercles, sometimes called tuberosities). The body is cylindrical in its upper portion, and more prismatic below. The lower extremity consists of 2 epicondyles, 2 processes (trochlea and capitulum), and 3 fossae (radial fossa, coronoid fossa, and olecranon fossa). As well as its true anatomical neck, the constriction below the greater and lesser tubercles of the humerus is referred to as its surgical neck due to its tendency to fracture, thus often becoming the focus of surgeons.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Humerus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hydrocele(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an accumulation of serous fluid in a body cavity. A hydrocele testis, the most common form of hydrocele, is the accumulation of fluids around a testicle. It is often caused by fluid collecting within a layer wrapped around the testicle, called the tunica vaginalis, which is derived from peritoneum. Provided there is no hernia present, it goes away without treatment in the first year. Although hydroceles usually develop in males, rare instances have been described in females in the canal of Nuck.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hydrocele";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Hypersalivation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the excessive production of saliva. It has also been defined as increased amount of saliva in the mouth, which may also be caused by decreased clearance of saliva.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Hypersalivation";
        }
    }

    return "NULL";
}

string _Medicine::Terms::IliacArteryCommon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "two large arteries that originate from the aortic bifurcation at the level of the fourth lumbar vertebra. They end in front of the sacroiliac joint, one on either side, and each bifurcates into the statical and internal iliac arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Iliac Artery Common";
        }
    }

    return "NULL";
}

string _Medicine::Terms::IliacArterystatical(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "two major arteries which bifurcate off the common iliac arteries anterior to the sacroiliac joint of the pelvis. They proceed anterior and inferior along the medial border of the psoas major muscles. They exit the pelvic girdle posterior and inferior to the inguinal ligament about one third laterally from the insertion point of the inguinal ligament on the pubic tubercle at which point they are referred to as the femoral arteries. The statical iliac artery is usually the artery used to attach the renal artery to the recipient of a kidney transplant.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Iliac Artery statical";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ilium(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the uppermost and largest part of the hip bone, and appears in most vertebrates including mammals and birds, but not bony fish. All reptiles have an ilium except snakes, although some snake species have a tiny bone which is considered to be an ilium. The ilium of the human is divisible into two parts, the body and the wing; the separation is indicated on the top surface by a curved line, the arcuate line, and on the statical surface by the margin of the acetabulum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ilium";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ImmuneSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a network of biological processes that protects an organism against disease. It detects and responds to a wide variety of pathogens, from viruses to parasitic worms, as well as objects such as wood splinters, distinguishing them from the organism\'s own healthy tissue. Many species have two major subsystems of the immune system. The innate immune system provides a preconfigured response to broad groups of situations and stimuli. The adaptive immune system provides a tailored response to each stimulus by learning to recognize molecules it has previously encountered. Both use molecules and cells to perform their functions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Immune System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Immunohistochemistry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a form of immunostaining. It involves the process of selectively identifying antigens in cells and tissue, by exploiting the principle of antibodies binding specifically to antigens in biological tissues. Albert Hewett Coons, Ernest Berliner, Norman Jones and Hugh J Creech was the first to develop immunofluorescence in 1941. This led to the later development of immunohistochemistry.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Immunohistochemistry";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Immunology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of biology that covers the study of immune systems in all organisms. Immunology charts, measures, and contextualizes the physiological functioning of the immune system in states of both health and diseases; malfunctions of the immune system in immunological disorders (such as autoimmune diseases, hypersensitivities, immune deficiency, and transplant rejection); and the physical, chemical, and physiological characteristics of the components of the immune system in vitro, in situ, and in vivo. Immunology has applications in numerous disciplines of medicine, particularly in the fields of organ transplantation, oncology, rheumatology, virology, bacteriology, parasitology, psychiatry, and dermatology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Immunology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::IliacVeinCommon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "formed by the statical iliac veins and internal iliac veins. The left and right common iliac veins come together in the abdomen at the level of the fifth lumbar vertebra, forming the inferior vena cava. They drain blood from the pelvis and lower limbs. Both common iliac veins are accompanied along their course by common iliac arteries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Iliac Vein Common";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::DeepCircumflexIliacVein(string text)
//{
//    string model = llm_model;
//    string definition = "The deep circumflex iliac vein is formed by the union of the venae comitantes of the deep iliac circumflex artery. It drains venous blood from the walls of the iliac fossa. It empties into the statical iliac vein about 2 cm superior to the inguinal ligament, just distal to where the inferior epigastric vein does so. The DCIV runs across the statical iliac artery. It forms anastomoses with the iliolumbar vein, and lower two lumbar veins.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Deep Circumflex Iliac Vein";
//        }
//    }
//
//    return "NULL";
//}

//string _Medicine::Terms::staticalIliacVein(string text)
//{
//    string model = llm_model;
//    string definition = "large veins that connect the femoral veins to the common iliac veins. Their origin is at the inferior margin of the inguinal ligaments and they terminate when they join the internal iliac veins (to form the common iliac veins). Both statical iliac veins are accompanied along their course by statical iliac arteries.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "statical Iliac Vein";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::InternalIliacVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "begins near the upper part of the greater sciatic foramen, passes upward behind and slightly medial to the internal iliac artery and, at the brim of the pelvis, joins with the statical iliac vein to form the common iliac vein.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Internal Iliac Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::IndexFinger(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the second digit of a human hand. It is located between the thumb and the middle finger. It is usually the most dextrous and sensitive digit of the hand, though not the longest. It is shorter than the middle finger, and may be shorter or longer than the ring finger (see digit ratio).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Index Finger";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Infectiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty dealing with the diagnosis and treatment of infections. An infectious diseases specialist\'s practice consists of managing nosocomial (healthcare-acquired) infections or community-acquired infections. An ID specialist investigates and determines the cause of a disease (bacteria, virus, parasite, fungus or prions). Once the cause is known, an ID specialist can then run various tests to determine the best drug to treat the disease. While infectious diseases have always been around, the infectious disease specialty did not exist until the late 1900s after scientists and physicians in the 19th century paved the way with research on the sources of infectious disease and the development of vaccines.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Infectiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InferiorObliqueMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a thin, narrow muscle placed near the anterior margin of the floor of the orbit. The inferior oblique is one of the extraocular muscles, and is attached to the maxillary bone (origin) and the posterior, inferior, lateral surface of the eye (insertion). The inferior oblique is innervated by the inferior branch of the oculomotor nerve.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inferior Oblique Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InferiorThyroidArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an artery in the neck. It arises from the thyrocervical trunk and passes upward, in front of the vertebral artery and longus colli muscle. It then turns medially behind the carotid sheath and its contents, and also behind the sympathetic trunk, the middle cervical ganglion resting upon the vessel. Reaching the lower border of the thyroid gland it divides into two branches, which supply the postero-inferior parts of the gland, and anastomose with the superior thyroid artery, and with the corresponding artery of the opposite side.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inferior Thyroid Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InferiorVenaCava(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large vein that carries the deoxygenated blood from the lower and middle body into the right atrium of the heart. It is formed by the joining of the right and the left common iliac veins, usually at the level of the fifth lumbar vertebra.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Inferior Vena Cava";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Influenza(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an infectious disease caused by influenza viruses. Symptoms range from mild to severe and often include fever, runny nose, sore throat, muscle pain, headache, coughing, and fatigue. These symptoms begin one to four (typically two) days after exposure to the virus and last for about two to eight days. Diarrhea and vomiting can occur, particularly in children. Influenza may progress to pneumonia from the virus or a subsequent bacterial infection. Other complications include acute respiratory distress syndrome, meningitis, encephalitis, and worsening of pre-existing health problems such as asthma and cardiovascular disease.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Influenza";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PhysicalExamination(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical practitioner examines a patient for any possible medical signs or symptoms of a medical condition. It generally consists of a series of questions about the patient\'s medical history followed by an examination based on the reported symptoms. Together, the medical history and the physical examination help to determine a diagnosis and devise the treatment plan. These data then become part of the medical record.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Physical Examination";
        }
    }

    return "NULL";
}

string _Medicine::Terms::IntegumentarySystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the set of organs forming the outermost layer of an animal\'s body. It comprises the skin and its appendages, which act as a physical barrier between the statical environment and the internal environment that it serves to protect and maintain the body of the animal. Mainly it is the body\'s outer skin. Includes skin, hair, scales, feathers, hooves, claws, and nails. It has a variety of additional functions: it may serve to maintain water balance, protect the deeper tissues, excrete wastes, and regulate body temperature, and is the attachment site for sensory receptors which detect pain, sensation, pressure, and temperature.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Integumentary System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::IntensiveCareMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty that deals with seriously or critically ill patients who have, are at risk of, or are recovering from conditions that may be life-threatening. It includes providing life support, invasive monitoring techniques, resuscitation, and end-of-life care. Doctors in this specialty are often called intensive care physicians, critical care physicians, or intensivists.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Intensive Care Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InternalCarotidArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an artery in the neck which supplies the anterior and middle cerebral circulation. In human anatomy, the internal and statical carotid arise from the common carotid artery, where it bifurcates at cervical vertebrae C3 or C4. The internal carotid artery supplies the brain, including the eyes, while the statical carotid nourishes other portions of the head, such as the face, scalp, skull, and meninges.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Internal Carotid Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InternalJugularVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "paired jugular vein that collects blood from the brain and the superficial parts of the face and neck. This vein runs in the carotid sheath with the common carotid artery and vagus nerve. It begins in the posterior compartment of the jugular foramen, at the base of the skull. It is somewhat dilated at its origin, which is called the superior bulb. This vein also has a common trunk into which drains the anterior branch of the retromandibular vein, the facial vein, and the lingual vein. It runs down the side of the neck in a vertical direction, being at one end lateral to the internal carotid artery, and then lateral to the common carotid artery, and at the root of the neck, it unites with the subclavian vein to form the brachiocephalic vein (innominate vein); a little above its termination is a second dilation, the inferior bulb. Above, it lies upon the rectus capitis lateralis, behind the internal carotid artery and the nerves passing through the jugular foramen. Lower down, the vein and artery lie upon the same plane, the glossopharyngeal and hypoglossal nerves passing forward between them. The vagus nerve descends between and behind the vein and the artery in the same sheath (the carotid sheath), and the accessory runs obliquely backward, superficial or deep to the vein. At the root of the neck, the right internal jugular vein is a little distance from the common carotid artery, and crosses the first part of the subclavian artery, while the left internal jugular vein usually overlaps the common carotid artery. The left vein is generally smaller than the right, and each contains a pair of valves, which exist about 2.5 cm above the termination of the vessel.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Internal Jugular Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InternalMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty for medical doctors focused on the prevention, diagnosis, and treatment of diseases in adults. Its namesake stems from \"treatment of diseases of the internal organs\". Medical practitioners of internal medicine are referred to as internists, or physicians in Commonwealth nations. Internists possess specialized skills in managing patients with undifferentiated or multi-system disease processes. They provide care to both hospitalized (inpatient) and ambulatory (outpatient) patients and often contribute significantly to teaching and research. Internists are qualified physicians who have undergone postgraduate training in internal medicine, and should not be confused with \"interns\", a term commonly used for a medical doctor who has obtained a medical degree but does not yet have a license to practice medicine unsupervised.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Internal Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Internship(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a physician in training who has completed medical school and has a medical degree, but does not yet have a license to practice medicine unsupervised. Under the guidance of senior doctors, interns will learn how to diagnose and treat patients, handle medical records and deal with different clinical situations. Medical education generally ends with a period of practical training similar to internship, but the way the overall program of academic and practical medical training is structured differs depending upon the country, as does the terminology used (see medical education and medical school for further details).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Internship";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InterventionalCardiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of cardiology that deals specifically with the catheter based treatment of structural heart diseases. Andreas Gruentzig is considered the father of interventional cardiology after the development of angioplasty by interventional radiologist Charles Dotter.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Interventional Cardiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::InterventionalRadiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty that performs various minimally-invasive procedures using medical imaging guidance, such as x-ray fluoroscopy, computed tomography, magnetic resonance imaging, or ultrasound. IR performs both diagnostic and therapeutic procedures through very small incisions or body orifices. Diagnostic IR procedures are those intended to help make a diagnosis or guide further medical treatment, and include image-guided biopsy of a tumor or injection of an imaging contrast agent into a hollow structure, such as a blood vessel or a duct. By contrast, therapeutic IR procedures provide direct treatment-they include catheter-based medicine delivery, medical device placement (e.g., stents), and angioplasty of narrowed structures.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Interventional Radiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ischium(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a paired bone forming the lower and back part of the hip bone. Situated below the ilium and behind the pubis, it is one of three regions whose fusion creates the coxal bone. The superior portion of this region forms approximately one-third of the acetabulum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ischium";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Jaundice(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a yellowish or greenish pigmentation of the skin and whites of the eyes due to high bilirubin levels. It is commonly associated with itchiness. The feces may be pale and the urine dark. Jaundice in babies occurs in over half in the first week following birth and does not pose a serious threat in most. If bilirubin levels in babies are very high for too long, a type of brain damage, known as kernicterus, may occur.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Jaundice";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Jaw(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "any opposable articulated structure at the entrance of the mouth, typically used for grasping and manipulating food. The term jaws is also broadly applied to the whole of the structures constituting the vault of the mouth and serving to open and close it and is part of the body plan of humans and most animals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Jaw";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Jejunum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the second part of the small intestine in humans and most higher vertebrates, including mammals, reptiles, and birds. Its lining is specialised for the absorption by enterocytes of small nutrient molecules which have been previously digested by enzymes in the duodenum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Jejunum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Joint(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the connection made between bones in the body which link the skeletal system into a functional whole. They are constructed to allow for different degrees and types of movement. Some joints, such as the knee, elbow, and shoulder, are self-lubricating, almost frictionless, and are able to withstand compression and maintain heavy loads while still executing smooth and precise movements. Other joints such as sutures between the bones of the skull permit very little movement (only during birth) in order to protect the brain and the sense organs. The connection between a tooth and the jawbone is also called a joint, and is described as a fibrous joint known as a gomphosis. Joints are classified both structurally and functionally.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Joint";
        }
    }

    return "NULL";
}

string _Medicine::Terms::JugularVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "veins that take deoxygenated blood from the head back to the heart via the superior vena cava.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Jugular Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Kidney(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "two reddish-brown bean-shaped organs found in vertebrates. They are located on the left and right in the retroperitoneal space, and in adult humans are about 12 centimetres (4+1/2 inches) in length. They receive blood from the paired renal arteries; blood exits into the paired renal veins. Each kidney is attached to a ureter, a tube that carries excreted urine to the bladder.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Kidney";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Knee(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In humans and other primates, the knee joins the thigh with the leg and consists of two joints: one between the femur and tibia (tibiofemoral joint), and one between the femur and patella (patellofemoral joint). It is the largest joint in the human body. The knee is a modified hinge joint, which permits flexion and extension as well as slight internal and statical rotation. The knee is vulnerable to injury and to the development of osteoarthritis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Knee";
        }
    }

    return "NULL";
}

string _Medicine::Terms::KorsakoffSyndrome(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an amnestic disorder caused by thiamine (vitamin B1) deficiency typically associated with prolonged use of alcohol. The syndrome and psychosis are named after Sergei Korsakoff, the Russian neuropsychiatrist who discovered it during the late 19th century. This neurological disorder is caused by a lack of thiamine in the brain, and is also exacerbated by the neurotoxic effects of alcohol. When Wernicke encephalopathy accompanies Korsakoff syndrome the combination is called Wernicke-Korsakoff syndrome; however, a recognized episode of Wernicke encephalopathy is not always obvious.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Korsakoff Syndrome";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LargeIntestine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as the large bowel or colon, is the last part of the gastrointestinal tract and of the digestive system in vertebrates. Water is absorbed here and the remaining waste material is stored as feces before being removed by defecation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Large Intestine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LaryngealProminence(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The Adam\'s apple, or laryngeal prominence, colloquially known as the neck triangle, is the lump or protrusion in the human neck formed by the angle of the thyroid cartilage surrounding the larynx seen especially in males.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Laryngeal Prominence";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LaryngealVentricle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a fusiform fossa, situated between the vestibular and vocal folds on either side, and extending nearly their entire length. There is also a sinus of Morgagni in the pharynx.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Laryngeal Ventricle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Laryngospasm(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an uncontrolled or involuntary muscular contraction (spasm) of the vocal folds. It may be triggered when the vocal cords or the area of the trachea below the vocal folds detects the entry of water, mucus, blood, or other substance. It may be associated with stridor or retractions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Laryngospasm";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ligament(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the fibrous connective tissue that connects bones to other bones.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ligament";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Lips(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a visible body part at the mouth of many animals, including humans. Lips are soft, movable, and serve as the opening for food intake and in the articulation of sound and speech. Human lips are a tactile sensory organ, and can be an erogenous zone when used in kissing and other acts of intimacy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lips";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LittleFinger(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "or pinky finger, also known as the fifth digit, or pinkie, is the most ulnar and smallest finger of the human hand, opposite the thumb, and next to the ring finger.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Little Finger";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Liver(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ only found in vertebrates which detoxifies various metabolites, synthesizes proteins and produces biochemicals necessary for digestion and growth. In humans, it is located in the right upper quadrant of the abdomen, below the diaphragm. Its other roles in metabolism include the regulation of glycogen storage, decomposition of red blood cells, and the production of hormones.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Liver";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LongBone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "are those that are longer than they are wide. They are one of five types of bones: long, short, flat, irregular and sesamoid. Long bones, especially the femur and tibia, are subjected to most of the load during daily activities and they are crucial for skeletal mobility. They grow primarily by elongation of the diaphysis, with an epiphysis at each end of the growing bone. The ends of epiphyses are covered with hyaline cartilage (\"articular cartilage\"). The longitudinal growth of long bones is a result of endochondral ossification at the epiphyseal plate. Bone growth in length is stimulated by the production of growth hormone (GH), a secretion of the anterior lobe of the pituitary gland.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Long Bone";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LumbarVertebrae(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "located between the thoracic vertebrae and pelvis. They form the lower part of the back in humans, and the tail end of the back in quadrupeds. In humans, there are five lumbar vertebrae. The term is used to describe the anatomy of humans and quadrupeds, such as horses, pigs, or cattle. These bones are found in particular cuts of meat, including tenderloin or sirloin steak.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lumbar Vertebrae";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Lung(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the primary organs of the respiratory system in many animals, including humans. In mammals and most other tetrapods, two lungs are located near the backbone on either side of the heart. Their function in the respiratory system is to extract oxygen from the atmosphere and transfer it into the bloodstream, and to release carbon dioxide from the bloodstream into the atmosphere, in a process of gas exchange. Respiration is driven by different muscular systems in different species. Mammals, reptiles and birds use their musculoskeletal systems to support and foster breathing. In early tetrapods, air was driven into the lungs by the pharyngeal muscles via buccal pumping, a mechanism still seen in amphibians. In humans, the primary muscle that drives breathing is the diaphragm. The lungs also provide airflow that makes vocalisation including speech possible.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lung";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LungCancer(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as lung carcinoma, is a malignant tumor that begins in the lung. Lung cancer is caused by genetic damage to the DNA of cells in the airways, often caused by cigarette smoking or inhaling damaging chemicals. Damaged airway cells gain the ability to multiply unchecked, causing the growth of a tumor. Without treatment, tumors spread throughout the lung, damaging lung function. Eventually lung tumors metastasize, spreading to other parts of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lung Cancer";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LupusErythematosus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a collection of autoimmune diseases in which the human immune system becomes hyperactive and attacks healthy tissues. Symptoms of these diseases can affect many different body systems, including joints, skin, kidneys, blood cells, heart, and lungs. The most common and most severe form is systemic lupus erythematosus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lupus Erythematosus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Lymph(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the fluid that flows through the lymphatic system, a system composed of lymph vessels (channels) and intervening lymph nodes whose function, like the venous system, is to return fluid from the tissues to be recirculated. At the origin of the fluid-return process, interstitial fluid-the fluid between the cells in all body tissues-enters the lymph capillaries. This lymphatic fluid is then transported via progressively larger lymphatic vessels through lymph nodes, where substances are removed by tissue lymphocytes and circulating lymphocytes are added to the fluid, before emptying ultimately into the right or the left subclavian vein, where it mixes with central venous blood.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lymph";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LymphaticSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ system in vertebrates that is part of the immune system and complementary to the circulatory system. It consists of a large network of lymphatic vessels, lymph nodes, lymphoid organs, lymphatic tissue and lymph. Lymph is a clear fluid carried by the lymphatic vessels back to the heart for re-circulation. The Latin word for lymph, lympha, refers to the deity of fresh water, \"Lympha\".";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lymphatic System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LymphaticVessel(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "thin-walled vessels (tubes), structured like blood vessels, that carry lymph. As part of the lymphatic system, lymph vessels are complementary to the cardiovascular system. Lymph vessels are lined by endothelial cells, and have a thin layer of smooth muscle, and adventitia that binds the lymph vessels to the surrounding tissue. Lymph vessels are devoted to the propulsion of the lymph from the lymph capillaries, which are mainly concerned with the absorption of interstitial fluid from the tissues. Lymph capillaries are slightly bigger than their counterpart capillaries of the vascular system. Lymph vessels that carry lymph to a lymph node are called afferent lymph vessels, and those that carry it from a lymph node are called efferent lymph vessels, from where the lymph may travel to another lymph node, may be returned to a vein, or may travel to a larger lymph duct. Lymph ducts drain the lymph into one of the subclavian veins and thus return it to general circulation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lymphatic Vessel";
        }
    }

    return "NULL";
}

string _Medicine::Terms::LymphNode(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a kidney-shaped organ of the lymphatic system and the adaptive immune system. A large number of lymph nodes are linked throughout the body by the lymphatic vessels. They are major sites of lymphocytes that include B and T cells. Lymph nodes are important for the proper functioning of the immune system, acting as filters for foreign particles including cancer cells, but have no detoxification function.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lymph Node";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Lymphocyte(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a type of white blood cell (leukocyte) in the immune system of most vertebrates. Lymphocytes include T cells (for cell-mediated and cytotoxic adaptive immunity), B cells (for humoral, antibody-driven adaptive immunity), and innate lymphoid cells (ILCs; \"innate T cell-like\" cells involved in mucosal immunity and homeostasis), of which natural killer cells are an important subtype (which functions in cell-mediated, cytotoxic innate immunity). They are the main type of cell found in lymph, which prompted the name \"lymphocyte\" (with cyte meaning cell). Lymphocytes make up between 18% and 42% of circulating white blood cells.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lymphocyte";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Lymphoma(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of blood and lymph tumors that develop from lymphocytes (a type of white blood cell). The name typically refers to just the cancerous versions rather than all such tumours. Signs and symptoms may include enlarged lymph nodes, fever, drenching sweats, unintended weight loss, itching, and constantly feeling tired. The enlarged lymph nodes are usually painless. The sweats are most common at night.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lymphoma";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MajorDepressiveDisorder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known simply as depression, is a mental disorder characterized by at least two weeks of pervasive low mood. Low self-esteem, loss of interest in normally enjoyable activities, low energy, and pain without a clear cause are common symptoms. Those affected may also occasionally have delusions or hallucinations. Some people have periods of depression separated by years, while others nearly always have symptoms present. Major depression is more severe and lasts longer than sadness, which is a normal part of life.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Major Depressive Disorder";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MaleReproductiveSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "consists of a number of sex organs that play a role in the process of human reproduction. These organs are located on the outside of the body, and within the pelvis. The main male sex organs are the penis and the scrotum, which contains the testicles that produce semen and sperm, which, as part of sexual intercourse, fertilize an ovum in the female\'s body; the fertilized ovum (zygote) develops into a fetus, which is later born as an infant. The corresponding system in females is the female reproductive system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Male Reproductive System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MammaryGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an exocrine gland that produces milk in humans and other mammals. Mammals get their name from the Latin word mamma, \"breast\". The mammary glands are arranged in organs such as the breasts in primates (for example, humans and chimpanzees), the udder in ruminants (for example, cows, goats, sheep, and deer), and the dugs of other animals (for example, dogs and cats) to feed young offspring. Lactorrhea, the occasional production of milk by the glands, can occur in any mammal, but in most mammals, lactation, the production of enough milk for nursing, occurs only in phenotypic females who have gestated in recent months or years. It is directed by hormonal guidance from sex steroids. In a few mammalian species, male lactation can occur. With humans, male lactation can occur only under specific circumstances.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Mammary Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Mandible(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "lower jaw or jawbone is the largest, strongest and lowest bone in the human face. It forms the lower jaw and holds the lower teeth in place. The mandible sits beneath the maxilla. It is the only movable bone of the skull (discounting the ossicles of the middle ear).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Mandible";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MasseterMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the muscles of mastication. Found only in mammals, it is particularly powerful in herbivores to facilitate chewing of plant matter. The most obvious muscle of mastication is the masseter muscle, since it is the most superficial and one of the strongest.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Masseter Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MaternalFetalMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as perinatology, is a branch of medicine that focuses on managing health concerns of the mother and fetus prior to, during, and shortly after pregnancy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Maternal Fetal Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Maxilla(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " in vertebrates, is the upper fixed (not fixed in Neopterygii) bone of the jaw formed from the fusion of two maxillary bones. In humans, the upper jaw includes the hard palate in the front of the mouth. The two maxillary bones are fused at the intermaxillary suture, forming the anterior nasal spine. This is similar to the mandible (lower jaw), which is also a fusion of two mandibular bones at the mandibular symphysis. The mandible is the movable part of the jaw.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Maxilla";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MCAT(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Medical College Admission Test.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "M C A T";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalBiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a field of biology that has practical applications in medicine, health care, and laboratory diagnostics. It includes many biomedical disciplines and areas of specialty that typically contains the \"bio-\" prefix such as: molecular biology, biochemistry, biophysics, biotechnology, cell biology, embryology, nanobiotechnology, biological engineering, laboratory medical biology, cytogenetics, genetics, gene therapy, bioinformatics, biostatistics, systems biology, microbiology, virology, parasitology, physiology, pathology, toxicology, and many others that generally concern life sciences as applied to medicine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Biology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalClassification(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A medical classification is a list of standardized codes used in the process of medical coding and medical billing.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Classification";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalCoding(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The practice of assigning statistical codes to medical statements, such as those made during a hospital stay. Closely related to medical billing.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Coding";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalCollegeAdmissionTest(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a computer-based standardized examination for prospective medical students in the United States, Australia, Canada, and Caribbean Islands. It is designed to assess problem solving, critical thinking, written analysis and knowledge of scientific concepts and principles.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical College Admission Test";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalDevice(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "any device intended to be used for medical purposes. Medical devices benefit patients by helping health care providers diagnose and treat patients and helping patients overcome sickness or disease, improving their quality of life. Significant potential for hazards are inherent when using a device for medical purposes and thus medical devices must be proved safe and effective with reasonable assurance before regulating governments allow marketing of the device in their country. As a general rule, as the associated risk of the device increases the amount of testing required to establish safety and efficacy also increases. Further, as associated risk increases the potential benefit to the patient must also increase.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Device";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalDiagnosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the process of determining which disease or condition explains a person\'s symptoms and signs. It is most often referred to as diagnosis with the medical context being implicit. The information required for diagnosis is typically collected from a history and physical examination of the person seeking medical care. Often, one or more diagnostic procedures, such as medical tests, are also done during the process. Sometimes posthumous diagnosis is considered a kind of medical diagnosis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Diagnosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalEthics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an applied branch of ethics which analyzes the practice of clinical medicine and related scientific research. Medical ethics is based on a set of values that professionals can refer to in the case of any confusion or conflict. These values include the respect for autonomy, non-maleficence, beneficence, and justice. Such tenets may allow doctors, care providers, and families to create a treatment plan and work towards the same common goal. These four values are not ranked in order of importance or relevance and they all encompass values pertaining to medical ethics. However, a conflict may arise leading to the need for hierarchy in an ethical system, such that some moral elements overrule others with the purpose of applying the best moral judgement to a difficult medical situation. Medical ethics is particularly relevant in decisions regarding involuntary treatment and involuntary commitment.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Ethics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalHistory(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "of a patient is a set of information the physicians collect over medical interviews. It involves the patient, and eventually people close to them, so to collect reliable/objective information for managing the medical diagnosis and proposing efficient medical treatments. The medically relevant complaints reported by the patient or others familiar with the patient are referred to as symptoms, in contrast with clinical signs, which are ascertained by direct examination on the part of medical personnel. Most health encounters will result in some form of history being taken. Medical histories vary in their depth and focus. For example, an ambulance paramedic would typically limit their history to important details, such as name, history of presenting complaint, allergies, etc. In contrast, a psychiatric history is frequently lengthy and in depth, as many details about the patient\'s life are relevant to formulating a management plan for a psychiatric illness.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical History";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalImaging(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Medical imaging is the technique and process of imaging the interior of a body for clinical analysis and medical intervention, as well as visual representation of the function of some organs or tissues (physiology). Medical imaging seeks to reveal internal structures hidden by the skin and bones, as well as to diagnose and treat disease. Medical imaging also establishes a database of normal anatomy and physiology to make it possible to identify abnormalities. Although imaging of removed organs and tissues can be performed for medical reasons, such procedures are usually considered part of pathology instead of medical imaging.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Imaging";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalLaboratory(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a laboratory where tests are conducted out on clinical specimens to obtain information about the health of a patient to aid in diagnosis, treatment, and prevention of disease. Clinical medical laboratories are an example of applied science, as opposed to research laboratories that focus on basic science, such as found in some academic institutions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Laboratory";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalResearch(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "efers to the process of using scientific methods with the aim to produce knowledge about human diseases, the prevention and treatment of illness, and the promotion of health.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Research";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalSchool(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "A medical school is a tertiary educational institution, professional school, or forms a part of such an institution, that teaches medicine, and awards a professional degree for physicians. Such medical degrees include the Bachelor of Medicine, Bachelor of Surgery (MBBS, MBChB, MBBCh, BMBS), Master of Medicine (MM, MMed), Doctor of Medicine (MD), or Doctor of Osteopathic Medicine (DO). Many medical schools offer additional degrees, such as a Doctor of Philosophy (PhD), master\'s degree (MSc) or other post-secondary education.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical School";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SignsAndSymptoms(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Signs and symptoms are diagnostic indications of an illness, injury, or condition. Signs are objective and statically observable; symptoms are a person\'s reported subjective experiences. A sign for example may be a higher or lower temperature than normal, raised or lowered blood pressure or an abnormality showing on a medical scan. A symptom is something out of the ordinary that is experienced by an individual such as feeling feverish, a headache or other pains in the body, which occur as the body\'s immune system fights off an infection.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Signs And Symptoms";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedicalSpecialty(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medical practice that is focused on a defined group of patients, diseases, skills, or philosophy. Examples include those branches of medicine that deal exclusively with children (pediatrics), cancer (oncology), laboratory medicine (pathology), or primary care (family medicine). After completing medical school or other basic training, physicians or surgeons and other clinicians usually further their medical education in a specific specialty of medicine by completing a multiple-year residency to become a specialist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medical Specialty";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Medication(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a drug used to diagnose, cure, treat, or prevent disease. Drug therapy (pharmacotherapy) is an important part of the medical field and relies on the science of pharmacology for continual advancement and on pharmacy for appropriate management. Drugs are classified in many ways. One of the key divisions is by level of control, which distinguishes prescription drugs (those that a pharmacist dispenses only on the medical prescription) from over-the-counter drugs (those that consumers can order for themselves). Medicines may be classified by mode of action, route of administration, biological system affected, or therapeutic effects. The World Health Organization keeps a list of essential medicines.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medication";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MedullaOblongata(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long stem-like structure which makes up the lower part of the brainstem. It is anterior and partially inferior to the cerebellum. It is a cone-shaped neuronal mass responsible for autonomic (involuntary) functions, ranging from vomiting to sneezing. The medulla contains the cardiovascular center, the respiratory center, vomiting and vasomotor centers, responsible for the autonomic functions of breathing, heart rate and blood pressure as well as the sleep-wake cycle. \"Medulla\" is from Latin, `pith or marrow\'. And \"oblongata\" is from Latin, `lengthened or longish or elongated\'.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Medulla Oblongata";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MetacarpalBones(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as the \"palm bones\", are the appendicular bones that form the intermediate part of the hand between the phalanges (fingers) and the carpal bones (wrist bones), which articulate with the forearm. The metacarpal bones are homologous to the metatarsal bones in the foot.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Metacarpal Bones";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MetatarsalBones(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of five long bones in the midfoot, located between the tarsal bones (which form the heel and the ankle) and the phalanges (toes). Lacking individual names, the metatarsal bones are numbered from the medial side (the side of the great toe): the first, second, third, fourth, and fifth metatarsal (often depicted with Roman numerals). The metatarsals are analogous to the metacarpal bones of the hand. The lengths of the metatarsal bones in humans are, in descending order, second, third, fourth, fifth, and first. A bovine hind leg has two metatarsals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Metatarsal Bones";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Microbiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the scientific study of microorganisms, those being of unicellular (single-celled), multicellular (consisting of complex cells), or acellular (lacking cells). Microbiology encompasses numerous sub-disciplines including virology, bacteriology, protistology, mycology, immunology, and parasitology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Microbiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MiddleFinger(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the third digit of the human hand, typically located between the index finger and the ring finger. It is typically the longest digit. In anatomy, it is also called the third finger, digitus medius, digitus tertius or digitus III.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Middle Finger";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MiddleTemporalArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a major artery which arises immediately above the zygomatic arch, and, perforating the temporal fascia, gives branches to the temporalis, anastomosing with the deep temporal branches of the internal maxillary.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Middle Temporal Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MolecularBiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of biology that seeks to understand the molecular basis of biological activity in and between cells, including biomolecular synthesis, modification, mechanisms, and interactions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Molecular Biology";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::HumanMouth(string text)
//{
//    string model = llm_model;
//    string definition = "the first portion of the alimentary canal that receives food and produces saliva. The oral mucosa is the mucous membrane epithelium lining the inside of the mouth. In addition to its primary role as the beginning of the digestive system, the mouth also plays a significant role in communication. While primary aspects of the voice are produced in the throat, the tongue, lips, and jaw are also needed to produce the range of sounds included in speech. The mouth consists of two regions, the vestibule and the oral cavity proper. The mouth, normally moist, is lined with a mucous membrane, and contains the teeth. The lips mark the transition from mucous membrane to skin, which covers most of the body.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Human Mouth";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::Muscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a soft tissue, one of the four basic types of animal tissue. There are three types of muscle tissue in vertebrates: skeletal muscle, cardiac muscle, and smooth muscle. Muscle tissue gives skeletal muscles the ability to contract. Muscle tissue contains special contractile proteins called actin and myosin which interact to cause movement. Among many other muscle proteins, present are two regulatory proteins, troponin and tropomyosin. Muscle is formed during embryonic development, in a process known as myogenesis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::MuscularSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ system consisting of skeletal, smooth, and cardiac muscle. It permits movement of the body, maintains posture, and circulates blood throughout the body. The muscular systems in vertebrates are controlled through the nervous system although some muscles (such as the cardiac muscle) can be completely autonomous. Together with the skeletal system in the human, it forms the musculoskeletal system, which is responsible for the movement of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Muscular System";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::HumanMusculoskeletalSystem(string text)
//{
//    string model = llm_model;
//    string definition = "an organ system that gives humans the ability to move using their muscular and skeletal systems. The musculoskeletal system provides form, support, stability, and movement to the body.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Human Musculoskeletal System";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::Nail(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a claw-like keratinous plate at the tip of the fingers and toes in most primates. Nails correspond to claws found in other animals. Fingernails and toenails are made of a tough protective protein called alpha-keratin which is found in the hooves, hair, claws and horns of vertebrates.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nail";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Nanobiotechnology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Nanobiotechnology, bionanotechnology, and nanobiology are terms that refer to the intersection of nanotechnology and biology. Given that the subject is one that has only emerged very recently, bionanotechnology and nanobiotechnology serve as blanket terms for various related technologies.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nanobiotechnology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::NasalCavity(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large, air-filled space above and behind the nose in the middle of the face. The nasal septum divides the cavity into two cavities, also known as fossae. Each cavity is the continuation of one of the two nostrils. The nasal cavity is the uppermost part of the respiratory system and provides the nasal passage for inhaled air from the nostrils to the nasopharynx and rest of the respiratory tract. The paranasal sinuses surround and drain into the nasal cavity.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nasal Cavity";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Nasopharynx(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The upper portion of the pharynx, the nasopharynx, extends from the base of the skull to the upper surface of the soft palate. It includes the space between the internal nares and the soft palate and lies above the oral cavity. The adenoids, also known as the pharyngeal tonsils, are lymphoid tissue structures located in the posterior wall of the nasopharynx. Waldeyer\'s tonsillar ring is an annular arrangement of lymphoid tissue in both the nasopharynx and oropharynx. The nasopharynx is lined by respiratory epithelium that is pseudostratified, columnar, and ciliated.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nasopharynx";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Navel(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a protruding, flat, or hollowed area on the abdomen at the attachment site of the umbilical cord. All placental mammals have a navel.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Navel";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Nephrology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a specialty of medicine that concerns with study of the kidneys, specifically normal kidney function and kidney disease, the preservation of kidney health, and the treatment of kidney disease, from diet and medication to renal replacement therapy (dialysis and kidney transplantation).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nephrology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Nerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an enclosed, cable-like bundle of nerve fibres called axons, in the peripheral nervous system. A nerve transmits electrical impulses and is the basic unit of the peripheral nervous system. A nerve provides a common pathway for the electrochemical nerve impulses called action potentials that are transmitted along each of the axons to peripheral organs or, in the case of sensory nerves, from the periphery back to the central nervous system. Each axon within the nerve is an extension of an individual neuron, along with other supportive cells such as some Schwann cells that coat the axons in myelin.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::NervousSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a highly complex part of an animal that coordinates its actions and sensory information by transmitting signals to and from different parts of its body. The nervous system detects environmental changes that impact the body, then works in tandem with the endocrine system to respond to such events.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nervous System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Neurology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine dealing with disorders of the nervous system. Neurology deals with the diagnosis and treatment of all categories of conditions and disease involving the central and peripheral nervous systems (and their subdivisions, the autonomic and somatic nervous systems), including their coverings, blood vessels, and all effector tissue, such as muscle. Neurological practice relies heavily on the field of neuroscience, the scientific study of the nervous system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Neurology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Neuroscience(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the scientific study of the nervous system. It is a multidisciplinary science that combines physiology, anatomy, molecular biology, developmental biology, cytology, mathematical modeling, and psychology to understand the fundamental and emergent properties of neurons and neural circuits.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Neuroscience";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Neurosurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical specialty concerned with the prevention, diagnosis, surgical treatment, and rehabilitation of disorders which affect any portion of the nervous system including the brain, spinal cord, central and peripheral nervous system, and cerebrovascular system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Neurosurgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::NuclearMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty involving the application of radioactive substances in the diagnosis and treatment of disease. Nuclear medicine imaging, in a sense, is \"radiology done inside out\" or \"endoradiology\" because it records radiation emitting from within the body rather than radiation that is generated by statical sources like X-rays. In addition, nuclear medicine scans differ from radiology, as the emphasis is not on imaging anatomy, but on the function. For such reason, it is called a physiological imaging modality. Single photon emission computed tomography (SPECT) and positron emission tomography (PET) scans are the two most common imaging modalities in nuclear medicine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nuclear Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Nutrition(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the science that interprets the nutrients and other substances in food in relation to maintenance, growth, reproduction, health and disease of an organism. It includes ingestion, absorption, assimilation, biosynthesis, catabolism and excretion.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Nutrition";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ObliqueMuscleOfAuricle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The oblique muscle of auricle (oblique auricular muscle or Tod muscle) is an intrinsic muscle of the outer ear. The oblique muscle of auricle is placed on the cranial surface of the pinna. It consists of a few fibers extending from the upper and back part of the concha to the convexity immediately above it.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Oblique Muscle Of Auricle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Obstetrics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the field of study concentrated on pregnancy, childbirth and the postpartum period. As a medical specialty, obstetrics is combined with gynecology under the discipline known as obstetrics and gynecology (OB/GYN), which is a surgical field.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Obstetrics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ObstetricsAndGynaecology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical specialty that encompasses the two subspecialties of obstetrics (covering pregnancy, childbirth, and the postpartum period) and gynecology (covering the health of the female reproductive system - vagina, uterus, ovaries, and breasts). It is commonly abbreviated as OB-GYN or OB/GYN.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Obstetrics And Gynaecology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OccipitalBone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a cranial dermal bone and the main bone of the occiput (back and lower part of the skull). It is trapezoidal in shape and curved on itself like a shallow dish. The occipital bone overlies the occipital lobes of the cerebrum. At the base of skull in the occipital bone, there is a large oval opening called the foramen magnum, which allows the passage of the spinal cord.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Occipital Bone";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OcularSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = " surgery performed on the eye or its adnexa. Eye surgery is part of ophthalmology and is performed by an ophthalmologist or eye surgeon. The eye is a fragile organ, and requires due care before, during, and after a surgical procedure to minimize or prevent further damage. An eye surgeon is responsible for selecting the appropriate surgical procedure for the patient, and for taking the necessary safety precautions. Mentions of eye surgery can be found in several ancient texts dating back as early as 1800 BC, with cataract treatment starting in the fifth century BC. It continues to be a widely practiced class of surgery, with various techniques having been developed for treating eye problems.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ocular Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Olfaction(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the sense of smell, is the process of creating the perception of smell. It occurs when an odor binds to a receptor within the nose, transmitting a signal through the olfactory system. Olfaction has many purposes, including detecting hazards, pheromones, and plays a role in taste.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Olfaction";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Oncology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine that deals with the prevention, diagnosis, and treatment of cancer. A medical professional who practices oncology is an oncologist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Oncology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ophthalmology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine and surgery which deals with the diagnosis and treatment of eye disorders. An ophthalmologist is a specialist in ophthalmology. The credentials include a degree in medicine, followed by additional four to five years of ophthalmology residency training. Ophthalmology residency training programs may require a one-year pre-residency training in internal medicine, pediatrics, or general surgery. Additional specialty training (or fellowship) may be sought in a particular aspect of eye pathology. Ophthalmologists are allowed to use medications to treat eye diseases, implement laser therapy, and perform surgery when needed. Ophthalmologists may participate in academic research on the diagnosis and treatment for eye disorders.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ophthalmology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Optometry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a health care profession that involves examining the eyes and applicable visual systems for defects or abnormalities as well as prescribing the correction of refractive error with glasses or contact lenses and the treatment of eye diseases.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Optometry";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Organ(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of tissues with similar functions. Plant life and animal life rely on many organs that co-exist in organ systems.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Organ";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OralAndMaxillofacialSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a surgical specialty focusing on reconstructive surgery of the face, facial trauma surgery, the oral cavity, head and neck, mouth, and jaws, as well as facial cosmetic surgery.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Oral And Maxillofacial Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OrbicularisOculiMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a sphincter-like muscle in the face that closes the eyelids. It arises from the nasal part of the frontal bone, from the frontal process of the maxilla in front of the lacrimal groove, and from the anterior surface and borders of a short fibrous band, the medial palpebral ligament.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Orbicularis Oculi Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OrbicularisOrisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a complex of muscles in the lips that encircles the mouth. It is not a true sphincter, as was once thought, as it is actually composed of four independent quadrants that interlace and give only an appearance of circularity. It is also one of the muscles used in the playing of all brass instruments and some woodwind instruments. This muscle closes the mouth and puckers the lips when it contracts.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Orbicularis Oris Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::OrthopedicSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the branch of surgery concerned with conditions involving the musculoskeletal system. Orthopedic surgeons use both surgical and nonsurgical means to treat musculoskeletal trauma, spine diseases, sports injuries, degenerative diseases, infections, tumors and congenital disorders.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Orthopedic Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ossicles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "three irregular bones in the middle ear of humans and other mammals, and are among the smallest bones in the human body. Although the term \"ossicle\" literally means \"tiny bone\" (from Latin ossiculum) and may refer to any small bone throughout the body, it typically refers specifically to the malleus, incus and stapes (\"hammer, anvil, and stirrup\") of the middle ear.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ossicles";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Otitis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a general term for inflammation in ear or ear infection, inner ear infection, middle ear infection of the ear, in both humans and other animals. When infection is present, it may be viral or bacterial. When inflammation is present due to fluid build up in the middle ear and infection is not present it is considered Otitis media with effusion. It is subdivided into the following:";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Otitis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Otorhinolaryngology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a surgical subspecialty within medicine that deals with the surgical and medical management of conditions of the head and neck. Doctors who specialize in this area are called otorhinolaryngologists, otolaryngologists, head and neck surgeons, or ENT surgeons or physicians.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Otorhinolaryngology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ovary(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a gonad in the female reproductive system that produces ova; when released, an ovum travels through the fallopian tube/oviduct into the uterus. There is an ovary on the left and the right side of the body. The ovaries are endocrine glands, secreting various hormones that play a role in the menstrual cycle and fertility. The ovary progresses through many stages beginning in the prenatal period through menopause.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ovary";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Palate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the roof of the mouth in humans and other mammals. It separates the oral cavity from the nasal cavity. A similar structure is found in crocodilians, but in most other tetrapods, the oral and nasal cavities are not truly separated. The palate is divided into two parts, the anterior, bony hard palate and the posterior, fleshy soft palate (or velum).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Palate";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PalliativeCare(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an interdisciplinary medical caregiving approach aimed at optimizing quality of life and mitigating suffering among people with serious, complex illness. Within the published literature, many definitions of palliative care exist; most notably, the World Health Organization describes palliative care as \"an approach that improves the quality of life of patients and their families facing the problems associated with life-threatening illness, through the prevention and relief of suffering by means of early identification and impeccable assessment and treatment of pain and other problems, physical, psychosocial, and spiritual.\" In the past, palliative care was a disease specific approach, but today the World Health Organization takes a more broad approach, that the principles of palliative care should be applied as early as possible to any chronic and ultimately fatal illness.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Palliative Care";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Palpation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the process of using one\'s hands to check the body, especially while perceiving/diagnosing a disease or illness.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Palpation";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pancreas(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ of the digestive system and endocrine system of vertebrates. In humans, it is located in the abdomen behind the stomach and functions as a gland. The pancreas has both an endocrine and a digestive exocrine function. As an endocrine gland, it functions mostly to regulate blood sugar levels, secreting the hormones insulin, glucagon, somatostatin, and pancreatic polypeptide. As a part of the digestive system, it functions as an exocrine gland secreting pancreatic juice into the duodenum through the pancreatic duct. This juice contains bicarbonate, which neutralizes acid entering the duodenum from the stomach; and digestive enzymes, which break down carbohydrates, proteins, and fats in food entering the duodenum from the stomach.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pancreas";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Papillary(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In oncology, papillary refers to neoplasms with projections (\"papillae\", from Latin, \'nipple\') that have fibrovascular cores.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Papillary";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Parasitology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the study of parasites, their hosts, and the relationship between them. As a biological discipline, the scope of parasitology is not determined by the organism or environment in question but by their way of life. This means it forms a synthesis of other disciplines, and draws on techniques from fields such as cell biology, bioinformatics, biochemistry, molecular biology, immunology, genetics, evolution and ecology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parasitology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ParathyroidGlands(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "small endocrine glands in the neck of humans and other tetrapods. Humans usually have four parathyroid glands, located on the back of the thyroid gland in variable locations. The parathyroid gland produces and secretes parathyroid hormone in response to a low blood calcium, which plays a key role in regulating the amount of calcium in the blood and within the bones.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parathyroid Glands";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ParkinsonsDisease(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a neurodegenerative disease primarily of the central nervous system, affecting both motor and non-motor systems. Symptoms typically develop gradually and non-motor issues become more prevalent as the disease progresses. The motor symptoms are collectively called parkinsonism and include tremors, bradykinesia, rigidity as well as postural instability (i.e., difficulty maintaining balance). Non-motor symptoms develop later in the disease and include behavioral changes or neuropsychiatric problems such as sleep abnormalities, psychosis, anosmia, and mood swings.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Parkinsons Disease";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Patella(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as the kneecap, is a flat, rounded triangular bone which articulates with the femur (thigh bone) and covers and protects the anterior articular surface of the knee joint. The patella is found in many tetrapods, such as mice, cats, birds, and dogs, but not in whales, or most reptiles.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Patella";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pathology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the study of disease. The word pathology also refers to the study of disease in general, incorporating a wide range of biology research fields and medical practices. However, when used in the context of modern medical treatment, the term is often used in a narrower fashion to refer to processes and tests that fall within the contemporary medical field of \"general pathology\", an area that includes a number of distinct but inter-related medical specialties that diagnose disease, mostly through analysis of tissue and human cell samples. Idiomatically, \"a pathology\" may also refer to the predicted or actual progression of particular diseases (as in the statement \"the many different forms of cancer have diverse pathologies\", in which case a more proper choice of word would be \"pathophysiologies\"). The suffix pathy is sometimes used to indicate a state of disease in cases of both physical ailment (as in cardiomyopathy) and psychological conditions (such as psychopathy). A physician practicing pathology is called a pathologist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pathology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PectineusMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "is a flat, quadrangular muscle, situated at the anterior (front) part of the upper and medial (inner) aspect of the thigh. The pectineus muscle is the most anterior adductor of the hip. The muscle\'s primary action is hip flexion; it also produces adduction and statical rotation of the hip.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pectineus Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PectoralisMajor(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a thick, fan-shaped or triangular convergent muscle of the human chest. It makes up the bulk of the chest muscles and lies under the breast. Beneath the pectoralis major is the pectoralis minor muscle. The pectoralis major arises from parts of the clavicle and sternum, costal cartilages of the true ribs, and the aponeurosis of the abdominal statical oblique muscle; it inserts onto the lateral lip of the bicipital groove. It receives double motor innervation from the medial pectoral nerve and the lateral pectoral nerve. The pectoralis major\'s primary functions are flexion, adduction, and internal rotation of the humerus. The pectoral major may colloquially be referred to as \"pecs\", \"pectoral muscle\", or \"chest muscle\", because it is the largest and most superficial muscle in the chest area.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pectoralis Major";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PectoralisMinor(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a thin, triangular muscle, situated at the upper part of the chest, beneath the pectoralis major in the human body. It arises from ribs III-V; it inserts onto the coracoid process of the scapula. It is innervated by the medial pectoral nerve. Its function is to stabilise the scapula by holding it fast in position against the chest wall.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pectoralis Minor";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pediatrics(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the branch of medicine that involves the medical care of infants, children, adolescents, and young adults. In the United Kingdom, pediatrics covers many of their youth until the age of 18. The American Academy of Pediatrics recommends people seek pediatric care through the age of 21, but some pediatric subspecialists continue to care for adults up to 25. Worldwide age limits of pediatrics have been trending upward year after year. A medical doctor who specializes in this area is known as a pediatrician, or paediatrician. The word pediatrics and its cognates mean \"healer of children\", derived from the two Greek words: πα?? (pais \"child\") and ?ατ??? (iatros \"doctor, healer\"). Pediatricians work in clinics, research centers, universities, general hospitals and children\'s hospitals, including those who practice pediatric subspecialties (e.g. neonatology requires resources available in a NICU).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pediatrics";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pelvis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the lower part of an anatomical trunk, between the abdomen and the thighs (sometimes also called pelvic region), together with its embedded skeleton (sometimes also called bony pelvis or pelvic skeleton). The pelvic region of the trunk includes the bony pelvis, the pelvic cavity (the space enclosed by the bony pelvis), the pelvic floor, below the pelvic cavity, and the perineum, below the pelvic floor. The pelvic skeleton is formed in the area of the back, by the sacrum and the coccyx and anteriorly and to the left and right sides, by a pair of hip bones.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pelvis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Penis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an statical sex organ (intromittent organ) through which males urinate and ejaculate, as on other animals. Together with the testes and surrounding structures, the penis functions as part of the male reproductive system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Penis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Percussion(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "Percussion is a technique of clinical examination.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Percussion";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PeripheralNervousSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of two components that make up the nervous system of bilateral animals, with the other part being the central nervous system (CNS). The PNS consists of nerves and ganglia, which lie outside the brain and the spinal cord. The main function of the PNS is to connect the CNS to the limbs and organs, essentially serving as a relay between the brain and spinal cord and the rest of the body. Unlike the CNS, the PNS is not protected by the vertebral column and skull, or by the blood-brain barrier, which leaves it exposed to toxins.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Peripheral Nervous System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PeripheralVision(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "vision as it occurs outside the point of fixation, i.e. away from the center of gaze or, when viewed at large angles, in (or out of) the \"corner of one\'s eye\". The vast majority of the area in the visual field is included in the notion of peripheral vision. \"Far peripheral\" vision refers to the area at the edges of the visual field, \"mid-peripheral\" vision refers to medium eccentricities, and \"near-peripheral\", sometimes referred to as \"para-central\" vision, exists adjacent to the center of gaze.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Peripheral Vision";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PhalanxBone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "digital bones in the hands and feet of most vertebrates. In primates, the thumbs and big toes have two phalanges while the other digits have three phalanges. The phalanges are classed as long bones.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Phalanx Bone";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pharmacology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the science of drugs and medications, including a substance\'s origin, composition, pharmacokinetics, pharmacodynamics, therapeutic use, and toxicology. More specifically, it is the study of the interactions that occur between a living organism and chemicals that affect normal or abnormal biochemical function. If substances have medicinal properties, they are considered pharmaceuticals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pharmacology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pharynx(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the part of the throat behind the mouth and nasal cavity, and above the esophagus and trachea (the tubes going down to the stomach and the lungs respectively). It is found in vertebrates and invertebrates, though its structure varies across species. The pharynx carries food to the esophagus and air to the larynx. The flap of cartilage called the epiglottis stops food from entering the larynx.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pharynx";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Physician(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a health professional who practices medicine, which is concerned with promoting, maintaining or restoring health through the study, diagnosis, prognosis and treatment of disease, injury, and other physical and mental impairments. Physicians may focus their practice on certain disease categories, types of patients, and methods of treatment-known as specialities-or they may assume responsibility for the provision of continuing and comprehensive medical care to individuals, families, and communities-known as general practice. Medical practice properly requires both a detailed knowledge of the academic disciplines, such as anatomy and physiology, underlying diseases, and their treatment, which is the science of medicine, and a decent competence in its applied practice, which is the art or craft of the profession.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Physician";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Physiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the scientific study of functions and mechanisms in a living system. As a subdiscipline of biology, physiology focuses on how organisms, organ systems, individual organs, cells, and biomolecules carry out chemical and physical functions in a living system. According to the classes of organisms, the field can be divided into medical physiology, animal physiology, plant physiology, cell physiology, and comparative physiology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Physiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PinealGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a small endocrine gland in the brain of most vertebrates. It produces melatonin, a serotonin-derived hormone, which modulates sleep patterns following the diurnal cycles. The shape of the gland resembles a pine cone, which gives it its name. The pineal gland is located in the epithalamus, near the center of the brain, between the two hemispheres, tucked in a groove where the two halves of the thalamus join. It is one of the neuroendocrine secretory circumventricular organs in which capillaries are mostly permeable to solutes in the blood.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pineal Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PituitaryGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "located at the base of the brain, protruding off the bottom of the hypothalamus. The pituitary gland and the hypothalamus control much of the body\'s endocrine system. It is seated in part of the sella turcica a depression in the sphenoid bone, known as the hypophyseal fossa. The human pituitary gland is oval shaped, about 1 cm in diameter, 0.5-1 gram (0.018-0.035 oz) in weight on average, and about the size of a kidney bean.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pituitary Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Placenta(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a temporary embryonic and later fetal organ that begins developing from the blastocyst shortly after implantation. It plays critical roles in facilitating nutrient, gas, and waste exchange between the physically separate maternal and fetal circulations, and is an important endocrine organ, producing hormones that regulate both maternal and fetal physiology during pregnancy. The placenta connects to the fetus via the umbilical cord, and on the opposite aspect to the maternal uterus in a species-dependent manner. In humans, a thin layer of maternal decidual (endometrial) tissue comes away with the placenta when it is expelled from the uterus following birth (sometimes incorrectly referred to as the \'maternal part\' of the placenta). Placentas are a defining characteristic of placental mammals, but are also found in marsupials and some non-mammals with varying levels of development.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Placenta";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PlasticSurgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a surgical specialty involving restoration, reconstruction, or alteration of the human body. It can be divided into two main categories: reconstructive surgery and cosmetic surgery. Reconstructive surgery covers a wide range of specialties, including craniofacial surgery, hand surgery, microsurgery, and the treatment of burns. This kind of surgery focuses on restoring a body part or improving its function. In contrast, cosmetic (or aesthetic) surgery focuses solely on improving the physical appearance of the body. A comprehensive definition of plastic surgery has never been established, because it has no distinct anatomical object and thus overlaps with practically all other surgical specialties. An essential feature of plastic surgery is that it involves the treatment of conditions that require or may require tissue relocation skills.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Plastic Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Plexus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branching network of blood vessels, lymphatic vessels, or nerves. The nerves are typically axons outside the central nervous system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Plexus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pons(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "part of the brainstem that in humans and other mammals, lies inferior to the midbrain, superior to the medulla oblongata and anterior to the cerebellum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pons";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PosteriorTibialArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the lower limb is an artery that carries blood to the posterior compartment of the leg and plantar surface of the foot. It branches from the popliteal artery via the tibial-fibular trunk.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Posterior Tibial Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PreventiveHealthcare(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "or prophylaxis, is the application of healthcare measures to prevent diseases. Disease and disability are affected by environmental factors, genetic predisposition, disease agents, and lifestyle choices, and are dynamic processes that begin before individuals realize they are affected. Disease prevention relies on anticipatory actions that can be categorized as primal, primary, secondary, and tertiary prevention.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Preventive Healthcare";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Prognosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical term for predicting the likelihood or expected development of a disease, including whether the signs and symptoms will improve or worsen (and how quickly) or remain stable over time; expectations of quality of life, such as the ability to carry out daily activities; the potential for complications and associated health issues; and the likelihood of survival (including life expectancy). A prognosis is made on the basis of the normal course of the diagnosed disease, the individual\'s physical and mental condition, the available treatments, and additional factors. A complete prognosis includes the expected duration, function, and description of the course of the disease, such as progressive decline, intermittent crisis, or sudden, unpredictable crisis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Prognosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Prostate(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an accessory gland of the male reproductive system and a muscle-driven mechanical switch between urination and ejaculation. It is found in all male mammals. It differs between species anatomically, chemically, and physiologically. Anatomically, the prostate is found below the bladder, with the urethra passing through it. It is described in gross anatomy as consisting of lobes and in microanatomy by zone. It is surrounded by an elastic, fibromuscular capsule and contains glandular tissue, as well as connective tissue.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Prostate";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Psychiatry(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical specialty devoted to the diagnosis, treatment, and prevention of deleterious mental conditions. These include matters related to cognition, perceptions, mood, emotion, and behavior.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Psychiatry";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pubis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "forms the lower and anterior part of each side of the hip bone. The pubis is the most forward-facing (ventral and anterior) of the three bones that make up the hip bone. The left and right pubic bones are each made up of three sections; a superior ramus, an inferior ramus, and a body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pubis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PulmonaryArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an artery in the pulmonary circulation that carries deoxygenated blood from the left side of the heart to the lungs. The largest pulmonary artery is the main pulmonary artery or pulmonary trunk from the heart, and the smallest ones are the arterioles, which lead to the capillaries that surround the pulmonary alveoli.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pulmonary Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PulmonaryCirculation(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a division of the circulatory system in all vertebrates. The circuit begins with deoxygenated blood returned from the body to the right atrium of the heart where it is pumped out from the right ventricle to the lungs. In the lungs the blood is oxygenated and returned to the left atrium to complete the circuit.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pulmonary Circulation";
        }
    }

    return "NULL";
}

string _Medicine::Terms::PulmonaryVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the veins that transfer oxygenated blood from the lungs to the heart. The largest pulmonary veins are the four main pulmonary veins, two from each lung that drain into the left atrium of the heart. The pulmonary veins are part of the pulmonary circulation.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pulmonary Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pulmonology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty that deals with diseases involving the respiratory tract. It is also known as respirology, respiratory medicine, or chest medicine in some countries and areas.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pulmonology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Pulse(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "refers to the rhythmic pulsations (expansion and contraction) of an artery in response to the cardiac cycle (heartbeat). The pulse may be felt (palpated) in any place that allows an artery to be compressed near the surface of the body close to the skin, such as at the neck (carotid artery), wrist (radial artery or ulnar artery), at the groin (femoral artery), behind the knee (popliteal artery), near the ankle joint (posterior tibial artery), and on foot (dorsalis pedis artery). The pulse is most commonly measured at the wrist or neck for adults and at the brachial artery (inner upper arm between the shoulder and elbow) for infants and very young children. A sphygmograph is an instrument for measuring the pulse.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Pulse";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Quadriplegia(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "paralysis caused by illness or injury that results in the partial or total loss of use of all four limbs and torso; paraplegia is similar but does not affect the arms. The loss is usually sensory and motor, which means that both sensation and control are lost. The paralysis may be flaccid or spastic.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Quadriplegia";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RadialArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the main artery of the lateral aspect of the forearm.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Radial Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RadialNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a nerve in the human body that supplies the posterior portion of the upper limb. It innervates the medial and lateral heads of the triceps brachii muscle of the arm, as well as all 12 muscles in the posterior osteofascial compartment of the forearm and the associated joints and overlying skin. It originates from the brachial plexus, carrying fibers from the ventral roots of spinal nerves C5, C6, C7, C8 & T1.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Radial Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Radiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the medical discipline that uses medical imaging to diagnose and treat diseases within the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Radiology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Radius(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the two large bones of the forearm, the other being the ulna. It extends from the lateral side of the elbow to the thumb side of the wrist and runs parallel to the ulna. The ulna is usually slightly longer than the radius, but the radius is thicker. Therefore, the radius is considered to be the larger of the two. It is a long bone, prism-shaped and slightly curved longitudinally.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Radius";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Rectum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the final straight portion of the large intestine in humans and some other mammals, and the gut in others. The adult human rectum is about 12 centimetres (4.7 in) long, and begins at the rectosigmoid junction, the end of the sigmoid colon, at the level of the third sacral vertebra or the sacral promontory depending upon what definition is used. Its caliber is similar to that of the sigmoid colon at its commencement, but it is dilated near its termination, forming the rectal ampulla. It terminates at the level of the anorectal ring (the level of the puborectalis sling) or the dentate line, again depending upon which definition is used. In humans, the rectum is followed by the anal canal which is about 4 centimetres (1.6 in) long, before the gastrointestinal tract terminates at the anal verge. The word rectum comes from the Latin rectum intestinum, meaning straight intestine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rectum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RectusAbdominisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a paired muscle running vertically on each side of the anterior wall of the human abdomen, as well as that of some other mammals. There are two parallel muscles, separated by a midline band of connective tissue called the linea alba. It extends from the pubic symphysis, pubic crest and pubic tubercle inferiorly, to the xiphoid process and costal cartilages of ribs V to VII superiorly. The proximal attachments are the pubic crest and the pubic symphysis. It attaches distally at the costal cartilages of ribs 5-7 and the xiphoid process of the sternum.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rectus Abdominis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RectusFemorisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the four quadriceps muscles of the human body. The others are the vastus medialis, the vastus intermedius (deep to the rectus femoris), and the vastus lateralis. All four parts of the quadriceps muscle attach to the patella (knee cap) by the quadriceps tendon. The rectus femoris is situated in the middle of the front of the thigh; it is fusiform in shape, and its superficial fibers are arranged in a bipenniform manner, the deep fibers running straight (Latin: rectus) down to the deep";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rectus Femoris Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RedBloodCell(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The most common type of blood cell and the vertebrate\'s principal means of delivering oxygen to the body tissues - via blood flow through the circulatory system. Red blood cells take up oxygen in the lungs and release it into tissues while squeezing through the body\'s capillaries.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Red Blood Cell";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RenalArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "paired arteries that supply the kidneys with blood. Each is directed across the crus of the diaphragm, so as to form nearly a right angle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Renal Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RenalVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "in the renal circulation, are large-calibre veins that drain blood filtered by the kidneys into the inferior vena cava. There is one renal vein draining each kidney. Each renal vein is formed by the convergence of the interlobar veins of one kidney. Because the inferior vena cava is on the right half of the body, the left renal vein is longer than the right one.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Renal Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Residency(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a qualified physician (one who holds the degree of MD, DO, MBBS/MBChB), veterinarian (DVM/VMD, BVSc/BVMS), dentist (DDS or DMD), podiatrist (DPM), optometrist (OD), pharmacist (PharmD), or Medical Laboratory Scientist (Doctor of Medical Laboratory Science) who practices medicine or surgery, veterinary medicine, dentistry, optometry, podiatry, clinical pharmacy, or Clinical Laboratory Science, respectively, usually in a hospital or clinic, under the direct or indirect supervision of a senior medical clinician registered in that specialty such as an attending physician or consultant.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Residency";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RespiratorySystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a biological system consisting of specific organs and structures used for gas exchange in animals and plants. The anatomy and physiology that make this happen varies greatly, depending on the size of the organism, the environment in which it lives and its evolutionary history. In land animals, the respiratory surface is internalized as linings of the lungs. Gas exchange in the lungs occurs in millions of small air sacs; in mammals and reptiles, these are called alveoli, and in birds, they are known as atria. These microscopic air sacs have a very rich blood supply, thus bringing the air into close contact with the blood. These air sacs communicate with the statical environment via a system of airways, or hollow tubes, of which the largest is the trachea, which branches in the middle of the chest into the two main bronchi. These enter the lungs where they branch into progressively narrower secondary and tertiary bronchi that branch into numerous smaller tubes, the bronchioles. In birds, the bronchioles are termed parabronchi. It is the bronchioles, or parabronchi that generally open into the microscopic alveoli in mammals and atria in birds. Air has to be pumped from the environment into the alveoli or atria by the process of breathing which involves the muscles of respiration.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Respiratory System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Rheumatology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine devoted to the diagnosis and management of disorders whose common feature is inflammation in the bones, muscles, joints, and internal organs. Rheumatology covers more than 100 different complex diseases, collectively known as rheumatic diseases, which includes many forms of arthritis as well as lupus and Sjogren\'s syndrome. Doctors who have undergone formal training in rheumatology are called rheumatologists.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rheumatology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RibCage(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an endoskeletal enclosure in the thorax of most vertebrates that comprises the ribs, vertebral column and sternum, which protect the vital organs of the thoracic cavity, such as the heart, lungs and great vessels and support the shoulder girdle to form the core part of the axial skeleton.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Rib Cage";
        }
    }

    return "NULL";
}

string _Medicine::Terms::RingFinger(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the fourth digit of the human hand, located between the middle finger and the little finger.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ring Finger";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Sacrum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large, triangular bone at the base of the spine that forms by the fusing of sacral vertebrae S1-S5 between 18 and 30 years of age.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sacrum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SalivaryGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "exocrine glands that produce saliva through a system of ducts. Humans have three paired major salivary glands (parotid, submandibular, and sublingual), a pair of seromucous tubarial glands (discovered in 2020) as well as hundreds of minor salivary glands. Salivary glands can be classified as serous, mucous or seromucous (mixed).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Salivary Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SaphenousNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the largest cutaneous branch of the femoral nerve. It is a strictly sensory nerve, and has no motor function.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Saphenous Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SaphenousVeinGreat(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large, subcutaneous, superficial vein of the leg. It is the longest vein in the body, running along the length of the lower limb, returning blood from the foot, leg, and thigh to the deep femoral vein at the femoral triangle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Saphenous Vein Great";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SmallSaphenousVein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a relatively large superficial vein of the posterior leg.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Small Saphenous Vein";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SartoriusMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the longest muscle in the human body. It is a long, thin, superficial muscle that runs down the length of the thigh in the anterior compartment.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sartorius Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Scalp(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the area of the head where head hair grows. It is made up of skin, layers of connective and fibrous tissues, and the membrane of the skull. Anatomically, the scalp is part of the epicranium, a collection of structures covering the cranium. The scalp is bordered by the face at the front, and by the neck at the sides and back. The scientific study of hair and scalp is called trichology.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Scalp";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Scapula(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the bone that connects the humerus (upper arm bone) with the clavicle (collar bone). Like their connected bones, the scapulae are paired, with each scapula on either side of the body being roughly a mirror image of the other. The name derives from the Classical Latin word for trowel or small shovel, which it was thought to resemble.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Scapula";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SciaticNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large nerve in humans and other vertebrate animals. It is the largest branch of the sacral plexus and runs alongside the hip joint and down the lower limb. It is the longest and widest single nerve in the human body, going from the top of the leg to the foot on the posterior aspect. The sciatic nerve has no cutaneous branches for the thigh. This nerve provides the connection to the nervous system for the skin of the lateral leg and the whole foot, the muscles of the back of the thigh, and those of the leg and foot. It is derived from spinal nerves L4 to S3. It contains fibres from both the anterior and posterior divisions of the lumbosacral plexus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sciatic Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Scrotum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a part of the statical male genitalia located at the base of the penis. It consists of a sac of skin containing the statical spermatic fascia, testicles, epididymides, and vasa deferentia. The scrotum will usually tighten when exposed to cold temperatures. The scrotum is homologous to the labia majora in females.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Scrotum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SebaceousGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a microscopic exocrine gland in the skin that opens into a hair follicle to secrete an oily or waxy matter, called sebum, which lubricates the hair and skin of mammals. In humans, sebaceous glands occur in the greatest number on the face and scalp, but also on all parts of the skin except the palms of the hands and soles of the feet. In the eyelids, meibomian glands, also called tarsal glands, are a type of sebaceous gland that secrete a special type of sebum into tears. Surrounding the female nipples, areolar glands are specialized sebaceous glands for lubricating the nipples. Fordyce spots are benign, visible, sebaceous glands found usually on the lips, gums and inner cheeks, and genitals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sebaceous Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SeminalVesicles(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a pair of convoluted tubular accessory glands that lie behind the urinary bladder of male mammals. They secrete fluid that largely composes the semen. The vesicles are 5-10 cm in size, 3-5 cm in diameter, and are located between the bladder and the rectum. They have multiple outpouchings, which contain secretory glands, which join together with the vasa deferentia at the ejaculatory ducts. They receive blood from the vesiculodeferential artery, and drain into the vesiculodeferential veins. The glands are lined with column-shaped and cuboidal cells. The vesicles are present in many groups of mammals, but not marsupials, monotremes or carnivores. Inflammation of the seminal vesicles is called seminal vesiculitis and most often is due to bacterial infection as a result of a sexually transmitted infection or following a surgical procedure. Seminal vesiculitis can cause pain in the lower abdomen, scrotum, penis or peritoneum, painful ejaculation, and blood in the semen. It is usually treated with antibiotics, although may require surgical drainage in complicated cases. Other conditions may affect the vesicles, including congenital abnormalities such as failure or incomplete formation, and, uncommonly, tumours. The seminal vesicles have been described as early as the second century AD by Galen, although the vesicles only received their name much later, as they were initially described using the term from which the word prostate is derived.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Seminal Vesicles";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SensoryNervousSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a part of the nervous system responsible for processing sensory information. A sensory system consists of sensory neurons (including the sensory receptor cells), neural pathways, and parts of the brain involved in sensory perception and interoception. Commonly recognized sensory systems are those for vision, hearing, touch, taste, smell, balance and visceral sensation. Sense organs are transducers that convert data from the outer physical world to the realm of the mind where people interpret the information, creating their perception of the world around them.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sensory Nervous System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SensoryProcessing(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the process that organizes and distinguishes sensation (sensory information) from one\'s own body and the environment, thus making it possible to use the body effectively within the environment. Specifically, it deals with how the brain processes multiple sensory modality inputs, such as proprioception, vision, auditory system, tactile, olfactory, vestibular system, interoception, and taste into usable functional outputs. It has been believed for some time that inputs from different sensory organs are processed in different areas in the brain. The communication within and among these specialized areas of the brain is known as functional integration. Newer research has shown that these different regions of the brain may not be solely responsible for only one sensory modality, but could use multiple inputs to perceive what the body senses about its environment. Multisensory integration is necessary for almost every activity that we perform because the combination of multiple sensory inputs is essential for us to comprehend our surroundings.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sensory Processing";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SerratusAnteriorMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscle of the chest. It originates at the side of the chest from the upper 8 or 9 ribs; it inserts along the entire length of the anterior aspect of the medial border of the scapula. It is innervated by the long thoracic nerve from the brachial plexus. The serratus anterior acts to pull the scapula forward around the thorax.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Serratus Anterior Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SerratusPosteriorInferiorMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The serratus posterior inferior draws the lower ribs backward and downward to assist in rotation and extension of the trunk. This movement of the ribs may also contribute to inhalation and forced expiration of air from the lungs. The muscle is situated at the junction of the thoracic and lumbar regions. It has an irregularly quadrilateral form, broader than the serratus posterior superior muscle, and separated from it by a wide interval. It arises by a thin aponeurosis from the spinous processes of the lower two thoracic and upper two or three lumbar vertebrae. Passing obliquely upward and lateralward, it becomes fleshy, and divides into four flat digitations. These are inserted into the inferior borders of the lower four ribs, a little beyond their angles. The thin aponeurosis of origin is intimately blended with the thoracolumbar fascia, and aponeurosis of the latissimus dorsi muscle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Serratus Posterior Inferior Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SerratusPosteriorSuperiorMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a thin, quadrilateral muscle. It is situated at the upper back part of the thorax, deep to the rhomboid muscles.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Serratus Posterior Superior Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SkeletalMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the three types of vertebrate muscle tissue, the others being cardiac muscle and smooth muscle. They are part of the voluntary muscular system and typically are attached by tendons to bones of a skeleton. The skeletal muscle cells are much longer than in the other types of muscle tissue, and are also known as muscle fibers. The tissue of a skeletal muscle is striated - having a striped appearance due to the arrangement of the sarcomeres.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Skeletal Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanSkin(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the outer covering of the body and is the largest organ of the integumentary system. The skin has up to seven layers of ectodermal tissue guarding muscles, bones, ligaments and internal organs. Human skin is similar to most of the other mammals\' skin, and it is very similar to pig skin. Though nearly all human skin is covered with hair follicles, it can appear hairless. There are two general types of skin: hairy and glabrous skin (hairless). The adjective cutaneous literally means \"of the skin\" (from Latin cutis, skin). Skin plays an important immunity role in protecting the body against pathogens and excessive water loss. Its other functions are insulation, temperature regulation, sensation, synthesis of vitamin D, and the protection of vitamin B folates. Severely damaged skin will try to heal by forming scar tissue. This is often discoloured and depigmented. In humans, skin pigmentation (affected by melanin) varies among populations, and skin type can range from dry to non-dry and from oily to non-oily. Such skin variety provides a rich and diverse habitat for the approximately one thousand species of bacteria from nineteen phyla which have been found on human skin.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Skin";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Skull(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "typically a bony enclosure around the brain of a vertebrate. In some fish, and amphibians, the skull is of cartilage. The skull is at the head end of the vertebrate. In the human, the skull comprises two prominent parts: the neurocranium and the facial skeleton, which evolved from the first pharyngeal arch. The skull forms the frontmost portion of the axial skeleton and is a product of cephalization and vesicular enlargement of the brain, with several special senses structures such as the eyes, ears, nose, tongue and, in fish, specialized tactile organs such as barbels near the mouth. ";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Skull";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SmallIntestine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ in the gastrointestinal tract where most of the absorption of nutrients from food takes place. It lies between the stomach and large intestine, and receives bile and pancreatic juice through the pancreatic duct to aid in digestion. The small intestine is about 5.5 metres (18 feet) long and folds many times to fit in the abdomen. Although it is longer than the large intestine, it is called the small intestine because it is narrower in diameter.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Small Intestine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SmoothMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the three major types of vertebrate muscle tissue, the others being skeletal and cardiac muscle. It can also be found in invertebrates and is controlled by the autonomic nervous system. It is non-striated, so-called because it has no sarcomeres and therefore no striations (bands or stripes). It can be divided into two subgroups, single-unit and multi-unit smooth muscle. Within single-unit muscle, the whole bundle or sheet of smooth muscle cells contracts as a syncytium. Smooth muscle is found in the walls of hollow organs, including the stomach, intestines, bladder and uterus. In the walls of blood vessels, and lymph vessels, (excluding blood and lymph capillaries) it is known as vascular smooth muscle. There is smooth muscle in the tracts of the respiratory, urinary, and reproductive systems. In the eyes, the ciliary muscles, iris dilator muscle, and iris sphincter muscle are types of smooth muscles. The iris dilator and sphincter muscles are contained in the iris and contract in order to dilate or constrict the pupils. The ciliary muscles change the shape of the lens to focus on objects in accommodation. In the skin, smooth muscle cells such as those of the arrector pili cause hair to stand erect in response to cold temperature and fear.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Smooth Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SpecialSenses(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In medicine and anatomy, the special senses are the senses that have specialized organs devoted to them: vision (the eye), hearing and balance (the ear, which includes the auditory system and vestibular system), smell (the nose), and taste (the tongue).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Special Senses";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::MedicalSpecialty(string text)
//{
//    string model = llm_model;
//    string definition = "a branch of medical practice that is focused on a defined group of patients, diseases, skills, or philosophy. Examples include those branches of medicine that deal exclusively with children (pediatrics), cancer (oncology), laboratory medicine (pathology), or primary care (family medicine). After completing medical school or other basic training, physicians or surgeons and other clinicians usually further their medical education in a specific specialty of medicine by completing a multiple-year residency to become a specialist.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Medical Specialty";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::SpinalCord(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long, thin, tubular structure made up of nervous tissue that extends from the medulla oblongata in the lower brainstem to the lumbar region of the vertebral column (backbone) of vertebrate animals. The center of the spinal cord is hollow and contains a structure called the central canal, which contains cerebrospinal fluid. The spinal cord is also covered by meninges and enclosed by the neural arches. Together, the brain and spinal cord make up the central nervous system. In humans, the spinal cord is a continuation of the brainstem and anatomically begins at the occipital bone, passing out of the foramen magnum and then enters the spinal canal at the beginning of the cervical vertebrae. The spinal cord extends down to between the first and second lumbar vertebrae, where it tapers to become the cauda equina. The enclosing bony vertebral column protects the relatively shorter spinal cord. It is around 45 cm (18 in) long in adult men and around 43 cm (17 in) long in adult women. The diameter of the spinal cord ranges from 13 mm (1/2 in) in the cervical and lumbar regions to 6.4 mm (1/4 in) in the thoracic area.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Spinal Cord";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SpinalNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a mixed nerve, which carries motor, sensory, and autonomic signals between the spinal cord and the body. In the human body there are 31 pairs of spinal nerves, one on each side of the vertebral column. These are grouped into the corresponding cervical, thoracic, lumbar, sacral and coccygeal regions of the spine. There are eight pairs of cervical nerves, twelve pairs of thoracic nerves, five pairs of lumbar nerves, five pairs of sacral nerves, and one pair of coccygeal nerves. The spinal nerves are part of the peripheral nervous system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Spinal Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Sole(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The glabrous skin on the sole of the foot lacks the hair and pigmentation found elsewhere on the body, and it has a high concentration of sweat pores. The sole contains the thickest layers of skin on the body due to the weight that is continually placed on it. It is crossed by a set of creases that form during the early stages of embryonic development. Like those of the palm, the sweat pores of the sole lack sebaceous glands. The sole is a sensory organ by which the ground can be perceived while standing and walking. The subcutaneous tissue in the sole has adapted to deal with the high local compressive forces on the heel and the ball (between the toes and the arch) by developing a system of \"pressure chambers.\" Each chamber is composed of internal fibrofatty tissue covered by statical collagen connective tissue. The septa (internal walls) of these chambers are permeated by numerous blood vessels, making the sole one of the most vascularized, or blood-enriched, regions in the human body. ";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sole";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SoleusMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a powerful muscle in the back part of the lower leg (the calf). It runs from just below the knee to the heel and is involved in standing and walking. It is closely connected to the gastrocnemius muscle, and some anatomists consider this combination to be a single muscle, the triceps surae. Its name is derived from the Latin word \"solea\", meaning \"sandal\".";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Soleus Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Spleen(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an organ found in almost all vertebrates. Similar in structure to a large lymph node, it acts primarily as a blood filter. The spleen plays important roles in regard to red blood cells (erythrocytes) and the immune system. It removes old red blood cells and holds a reserve of blood, which can be valuable in case of hemorrhagic shock, and also recycles iron. As a part of the mononuclear phagocyte system, it metabolizes hemoglobin removed from senescent red blood cells. The globin portion of hemoglobin is degraded to its constitutive amino acids, and the heme portion is metabolized to bilirubin, which is removed in the liver. The spleen houses antibody-producing lymphocytes in its white pulp and monocytes which remove antibody-coated bacteria and antibody-coated blood cells by way of blood and lymph node circulation. These monocytes, upon moving to injured tissue (such as the heart after myocardial infarction), turn into dendritic cells and macrophages while promoting tissue healing. The spleen is a center of activity of the mononuclear phagocyte system and is analogous to a large lymph node, as its absence causes a predisposition to certain infections. In humans, the spleen is purple in color and is in the left upper quadrant of the abdomen. The surgical process to remove the spleen is known as a splenectomy.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Spleen";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SportsMedicine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a branch of medicine that deals with physical fitness and the treatment and prevention of injuries related to sports and exercise. Although most sports teams have employed team physicians for many years, it is only since the late 20th century that sports medicine emerged as a distinct field of health care. In many countries, now over 50, sports medicine (or sport and exercise medicine) is a recognized medical specialty (with similar training and standards to other medical specialties or sub-specialties). In the majority of countries where sports medicine is recognized and practiced, it is a physician (non-surgical) specialty, but in some (such as the USA), it can equally be a surgical or non-surgical medical specialty, and also a specialty field within primary care. In other contexts, the field of sports medicine encompasses the scope of both medical specialists as well as allied health practitioners who work in the field of sport, such as physiotherapists, athletic trainers, podiatrists and exercise physiologists.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sports Medicine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SternohyoidMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a bilaterally paired, long, thin, narrow strap muscle of the anterior neck. It is one of the infrahyoid muscles. It is innervated by the ansa cervicalis. It acts to depress the hyoid bone. The sternohyoid muscle is a flat muscle located on both sides of the neck, part of the infrahyoid muscle group. It originates from the medial edge of the clavicle, sternoclavicular ligament, and posterior side of the manubrium, and ascends to attach to the body of the hyoid bone. The sternohyoid muscle, along with other infrahyoid muscles, functions to depress the hyoid bone, which is important for activities such as speaking, chewing, and swallowing. Additionally, this muscle group contributes to the protection of the trachea, esophagus, blood vessels, and thyroid gland. The sternohyoid muscle also plays a minor role in head movements.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sternohyoid Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Sternum(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long flat bone located in the central part of the chest. It connects to the ribs via cartilage and forms the front of the rib cage, thus helping to protect the heart, lungs, and major blood vessels from injury. Shaped roughly like a necktie, it is one of the largest and longest flat bones of the body. Its three regions are the manubrium, the body, and the xiphoid process.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sternum";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Stomach(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscular, hollow organ in the upper gastrointestinal tract of humans and many other animals, including several invertebrates. The Ancient Greek name for the stomach is gaster which is used as gastric in medical terms related to the stomach. The stomach has a dilated structure and functions as a vital organ in the digestive system. The stomach is involved in the gastric phase of digestion, following the cephalic phase in which the sight and smell of food and the act of chewing are stimuli. In the stomach a chemical breakdown of food takes place by means of secreted digestive enzymes and gastric acid. It also plays a role in regulating gut microbiota, influencing digestion and overall health. The stomach is located between the esophagus and the small intestine. The pyloric sphincter controls the passage of partially digested food (chyme) from the stomach into the duodenum, the first and shortest part of the small intestine, where peristalsis takes over to move this through the rest of the intestines.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Stomach";
        }
    }

    return "NULL";
}

string _Medicine::Terms::StriatedMuscleTissue(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscle tissue that features repeating functional units called sarcomeres. Under the microscope, sarcomeres are visible along muscle fibers, giving a striated appearance to the tissue. The two types of striated muscle are skeletal muscle and cardiac muscle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Striated Muscle Tissue";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SubclavianArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "paired major arteries of the upper thorax, below the clavicle. They receive blood from the aortic arch. The left subclavian artery supplies blood to the left arm and the right subclavian artery supplies blood to the right arm, with some branches supplying the head and thorax. On the left side of the body, the subclavian comes directly off the aortic arch, while on the right side it arises from the relatively short brachiocephalic artery when it bifurcates into the subclavian and the right common carotid artery. The usual branches of the subclavian on both sides of the body are the vertebral artery, the internal thoracic artery, the thyrocervical trunk, the costocervical trunk and the dorsal scapular artery, which may branch off the transverse cervical artery, which is a branch of the thyrocervical trunk. The subclavian becomes the axillary artery at the lateral border of the first rib.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Subclavian Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SubcutaneousTissue(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the lowermost layer of the integumentary system in vertebrates. The types of cells found in the layer are fibroblasts, adipose cells, and macrophages. The subcutaneous tissue is derived from the mesoderm, but unlike the dermis, it is not derived from the mesoderm\'s dermatome region. It consists primarily of loose connective tissue and contains larger blood vessels and nerves than those found in the dermis. It is a major site of fat storage in the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Subcutaneous Tissue";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SuperficialTemporalArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a major artery of the head. It arises from the statical carotid artery when it splits into the superficial temporal artery and maxillary artery. Its pulse can be felt above the zygomatic arch, above and in front of the tragus of the ear.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Superficial Temporal Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SuperiorObliqueMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a fusiform muscle originating in the upper, medial side of the orbit (i.e. from beside the nose) which abducts, depresses and internally rotates the eye. It is the only extraocular muscle innervated by the trochlear nerve (the fourth cranial nerve).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Superior Oblique Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SuperiorThyroidArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "arises from the statical carotid artery just below the level of the greater cornu of the hyoid bone and ends in the thyroid gland.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Superior Thyroid Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SuperiorVenaCava(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the superior of the two venae cavae, the great venous trunks that return deoxygenated blood from the systemic circulation to the right atrium of the heart. It is a large-diameter (24 mm) short length vein that receives venous return from the upper half of the body, above the diaphragm. Venous return from the lower half, below the diaphragm, flows through the inferior vena cava. The SVC is located in the anterior right superior mediastinum. It is the typical site of central venous access via a central venous catheter or a peripherally inserted central catheter. Mentions of \"the cava\" without further specification usually refer to the SVC.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Superior Vena Cava";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Surgery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a medical specialty that uses manual and instrumental techniques to diagnose or treat pathological conditions (e.g., trauma, disease, injury, malignancy), to alter bodily functions (e.g., malabsorption created by bariatric surgery such as gastric bypass), to reconstruct or alter aesthetics and appearance (cosmetic surgery), or to remove unwanted tissues (body fat, glands, scars or skin tags) or foreign bodies. The act of performing surgery may be called a surgical procedure or surgical operation, or simply \"surgery\" or \"operation\". In this context, the verb \"operate\" means to perform surgery. The adjective surgical means pertaining to surgery; e.g. surgical instruments, surgical facility or surgical nurse. Most surgical procedures are performed by a pair of operators: a surgeon who is the main operator performing the surgery, and a surgical assistant who provides in-procedure manual assistance during surgery. Modern surgical operations typically require a surgical team that typically consists of the surgeon, the surgical assistant, an anaesthetist (often also complemented by an anaesthetic nurse), a scrub nurse (who handles sterile equipment), a circulating nurse and a surgical technologist, while procedures that mandate cardiopulmonary bypass will also have a perfusionist. All surgical procedures are considered invasive and often require a period of postoperative care (sometimes intensive care) for the patient to recover from the iatrogenic trauma inflicted by the procedure. The duration of surgery can span from several minutes to tens of hours depending on the specialty, the nature of the condition, the target body parts involved and the circumstance of each procedure, but most surgeries are designed to be one-off interventions that are typically not intended as an ongoing or repeated type of treatment.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Surgery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SweatGland(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as sudoriferous or sudoriparous glands, from Latin sudor \'sweat\', are small tubular structures of the skin that produce sweat. Sweat glands are a type of exocrine gland, which are glands that produce and secrete substances onto an epithelial surface by way of a duct. There are two main types of sweat glands that differ in their structure, function, secretory product, mechanism of excretion, anatomic distribution, and distribution across species: Eccrine sweat glands are distributed almost all over the human body, in varying densities, with the highest density in palms and soles, then on the head, but much less on the trunk and the extremities. Their water-based secretion represents a primary form of cooling in humans. Apocrine sweat glands are mostly limited to the axillae (armpits) and perineal area in humans. They are not significant for cooling in humans, but are the sole effective sweat glands in hoofed animals, such as the camels, donkeys, horses, and cattle. Ceruminous glands (which produce ear wax), mammary glands (which produce milk), and ciliary glands in the eyelids are modified apocrine sweat glands.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Sweat Gland";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Symptoms(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "are diagnostic indications of an illness, injury, or condition.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Symptoms";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SynovialBursa(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a small fluid-filled sac lined by synovial membrane with an inner capillary layer of viscous synovial fluid (similar in consistency to that of a raw egg white). It provides a cushion between bones and tendons and/or muscles around a joint. This helps to reduce friction between the bones and allows free movement. Bursae are found around most major joints of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Synovial Bursa";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SynovialJoint(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "joins bones or cartilage with a fibrous joint capsule that is continuous with the periosteum of the joined bones, constitutes the outer boundary of a synovial cavity, and surrounds the bones\' articulating surfaces. This joint unites long bones and permits free bone movement and greater mobility. The synovial cavity/joint is filled with synovial fluid. The joint capsule is made up of an outer layer of fibrous membrane, which keeps the bones together structurally, and an inner layer, the synovial membrane, which seals in the synovial fluid. They are the most common and most movable type of joint in the body. As with most other joints, synovial joints achieve movement at the point of contact of the articulating bones. They originated 400 million years ago in the first jawed vertebrates.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Synovial Joint";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SynovialMembrane(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a specialized connective tissue that lines the inner surface of capsules of synovial joints, tendon sheaths, and synovial bursas. It makes direct contact with the fibrous membrane on the outside surface and with the synovial fluid lubricant on the inside surface. In contact with the synovial fluid at the tissue surface are many rounded macrophage-like synovial cells (type A) and also type B cells, which are also known as fibroblast-like synoviocytes (FLS). Type A cells maintain the synovial fluid by removing wear-and-tear debris. As for the FLS, they produce hyaluronan, as well as other extracellular components in the synovial fluid.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Synovial Membrane";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Lupus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an autoimmune disease in which the body\'s immune system mistakenly attacks healthy tissue in many parts of the body. Symptoms vary among people and may be mild to severe. Common symptoms include painful and swollen joints, fever, chest pain, hair loss, mouth ulcers, swollen lymph nodes, feeling tired, and a red rash which is most commonly on the face. Often there are periods of illness, called flares, and periods of remission during which there are few symptoms. Children up to 18 years old develop a more severe form of SLE termed childhood-onset systemic lupus erythematosus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Lupus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SystemsBiology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the computational and mathematical analysis and modeling of complex biological systems. It is a biology-based interdisciplinary field of study that focuses on complex interactions within biological systems, using a holistic approach (holism instead of the more traditional reductionism) to biological research. This multifaceted research domain necessitates the collaborative efforts of chemists, biologists, mathematicians, physicists, and engineers to decipher the biology of intricate living systems by merging various quantitative molecular measurements with carefully constructed mathematical models. It represents a comprehensive method for comprehending the complex relationships within biological systems. In contrast to conventional biological studies that typically center on isolated elements, systems biology seeks to combine different biological data to create models that illustrate and elucidate the dynamic interactions within a system. This methodology is essential for understanding the complex networks of genes, proteins, and metabolites that influence cellular activities and the traits of organisms.  One of the aims of systems biology is to model and discover emergent properties, of cells, tissues and organisms functioning as a system whose theoretical description is only possible using techniques of systems biology. By exploring how function emerges from dynamic interactions, systems biology bridges the gaps that exist between molecules and physiological processes.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Systems Biology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Tarsus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In the human body, the tarsus is a cluster of seven articulating bones in each foot situated between the lower end of the tibia and the fibula of the lower leg and the metatarsus. It is made up of the midfoot (cuboid, medial, intermediate, and lateral cuneiform, and navicular) and hindfoot (talus and calcaneus).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tarsus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Taste(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The gustatory system or sense of taste is the sensory system that is partially responsible for the perception of taste (flavor). Taste is the perception produced or stimulated when a substance in the mouth reacts chemically with taste receptor cells located on taste buds in the oral cavity, mostly on the tongue. Taste, along with smell (olfaction) and trigeminal nerve stimulation (registering texture, pain, and temperature), determines flavors of food and other substances. Humans have taste receptors on taste buds and other areas including the upper surface of the tongue and the epiglottis. The gustatory cortex is responsible for the perception of taste.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Taste";
        }
    }

    return "NULL";
}

string _Medicine::Terms::HumanTooth(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "function to mechanically break down items of food by cutting and crushing them in preparation for swallowing and digesting. As such, they are considered part of the human digestive system. Humans have four types of teeth: incisors, canines, premolars, and molars, which each have a specific function. The incisors cut the food, the canines tear the food and the molars and premolars crush the food. The roots of teeth are embedded in the maxilla (upper jaw) or the mandible (lower jaw) and are covered by gums. Teeth are made of multiple tissues of varying density and hardness. Humans, like most other mammals, are diphyodont, meaning that they develop two sets of teeth. The first set, deciduous teeth, also called \"primary teeth\", \"baby teeth\", or \"milk teeth\", normally eventually contains 20 teeth. Primary teeth typically start to appear (\"erupt\") around six months of age and this may be distracting and/or painful for the infant. However, some babies are born with one or more visible teeth, known as neonatal teeth or \"natal teeth\".";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Human Tooth";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Temple(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a latch where four skull bones intersect: the frontal, parietal, temporal, and sphenoid. It is located on the side of the head behind the eye between the forehead and the ear. The temporal muscle covers this area and is used during mastication. Cladistics classifies land vertebrates based on the presence of an upper hole, a lower hole, both, or neither in the cover of dermal bone that formerly covered the temporalis muscle, whose origin is the temple and whose insertion is the jaw.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Temple";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::DeepTemporalArteries(string text)
//{
//    string model = llm_model;
//    string definition = "are two arteries of the head. They ascend between the temporalis muscle and the pericranium. They anastomose with the middle temporal artery, among other vessels. They supply the temporalis muscle. The deep temporal arteries consist of an anterior and a posterior artery. They are branches of the maxillary artery, a terminal branch of the statical carotid artery. They ascend between the temporalis muscle and the pericranium. The deep temporal arteries anastomose with the middle temporal artery. The anterior artery communicates with the lacrimal artery by means of small branches which perforate the zygomatic bone and greater wing of the sphenoid bone. It may also communicate with the ophthalmic artery, a branch of the internal carotid artery.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Deep Temporal Arteries";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::TemporalisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the muscles of mastication (chewing). It is a broad, fan-shaped convergent muscle on each side of the head that fills the temporal fossa, superior to the zygomatic arch so it covers much of the temporal bone. Temporal refers to the head\'s temples.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Temporalis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Tendon(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a tough band of dense fibrous connective tissue that connects muscle to bone. It sends the mechanical forces of muscle contraction to the skeletal system, while withstanding tension. Tendons, like ligaments, are made of collagen. The difference is that ligaments connect bone to bone, while tendons connect muscle to bone. There are about 4,000 tendons in the adult human body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tendon";
        }
    }

    return "NULL";
}

string _Medicine::Terms::TensorFasciaeLataeMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscle of the thigh. Together with the gluteus maximus, it acts on and is continuous with the iliotibial band, which attaches to the tibia. The muscle assists in keeping the balance of the pelvis while standing, walking, or running. The tensor fasciae latae arises from the anterior part of the outer lip of the iliac crest; from the outer surface of the anterior superior iliac spine, and part of the outer border of the notch below it, between the gluteus medius and sartorius; and from the deep surface of the fascia lata. The tensor fasciae latae is inserted between the two layers of the iliotibial tract of the fascia lata about the junction of the middle and upper thirds of the thigh. It tautens the iliotibial tract and braces the knee, especially when the opposite foot is lifted. The terminal insertion point lies on the lateral condyle of the tibia.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tensor Fasciae Latae Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Testicle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the gonad in all male bilaterians, including humans, and is homologous to the ovary in females. Its primary functions are the production of sperm and the secretion of androgens, primarily testosterone. The release of testosterone is regulated by luteinizing hormone (LH) from the anterior pituitary gland. Sperm production is controlled by follicle-stimulating hormone (FSH) from the anterior pituitary gland and by testosterone produced within the gonads.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Testicle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Thigh(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the area between the hip (pelvis) and the knee. Anatomically, it is part of the lower limb. The single bone in the thigh is called the femur. This bone is very thick and strong (due to the high proportion of bone tissue), and forms a ball and socket joint at the hip, and a modified hinge joint at the knee.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thigh";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ThoracicDiaphragm(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a sheet of internal skeletal muscle in humans and other mammals that extends across the bottom of the thoracic cavity. The diaphragm is the most important muscle of respiration, and separates the thoracic cavity, containing the heart and lungs, from the abdominal cavity: as the diaphragm contracts, the volume of the thoracic cavity increases, creating a negative pressure there, which draws air into the lungs. Its high oxygen consumption is noted by the many mitochondria and capillaries present; more than in any other skeletal muscle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thoracic Diaphragm";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Thorax(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "or chest is a part of the anatomy of mammals and other tetrapod animals located between the neck and the abdomen. In insects, crustaceans, and the extinct trilobites, the thorax is one of the three main divisions of the body, each in turn composed of multiple segments. The human thorax includes the thoracic cavity and the thoracic wall. It contains organs including the heart, lungs, and thymus gland, as well as muscles and various other internal structures. The chest may be affected by many diseases, of which the most common symptom is chest pain.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thorax";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Throat(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the front part of the neck, internally positioned in front of the vertebrae. It contains the pharynx and larynx. An important section of it is the epiglottis, separating the esophagus from the trachea (windpipe), preventing food and drinks being inhaled into the lungs. The throat contains various blood vessels, pharyngeal muscles, the nasopharyngeal tonsil, the tonsils, the palatine uvula, the trachea, the esophagus, and the vocal cords. Mammal throats consist of two bones, the hyoid bone and the clavicle. The \"throat\" is sometimes thought to be synonymous for the fauces.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Throat";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Thumb(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the first digit of the hand, next to the index finger. When a person is standing in the medical anatomical position (where the palm is facing to the front), the thumb is the outermost digit. The Medical Latin English noun for thumb is pollex (compare hallux for big toe), and the corresponding adjective for thumb is pollical.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thumb";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Thymus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a specialized primary lymphoid organ of the immune system. Within the thymus, T cells mature. T cells are critical to the adaptive immune system, where the body adapts to specific foreign invaders. The thymus is located in the upper front part of the chest, in the anterior superior mediastinum, behind the sternum, and in front of the heart. It is made up of two lobes, each consisting of a central medulla and an outer cortex, surrounded by a capsule.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thymus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Thyroid(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an endocrine gland in vertebrates. In humans, it is a butterfly-shaped gland located in the neck below the Adam\'s apple. It consists of two connected lobes. The lower two thirds of the lobes are connected by a thin band of tissue called the isthmus (pl.: isthmi). Microscopically, the functional unit of the thyroid gland is the spherical thyroid follicle, lined with follicular cells (thyrocytes), and occasional parafollicular cells that surround a lumen containing colloid.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thyroid";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ThyroidImaArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an artery of the head and neck. It is an anatomical variant that, when present, supplies blood to the thyroid gland primarily, or the trachea, the parathyroid gland and the thymus gland (as thymica accessoria) in rare cases. It has also been reported to be a compensatory artery when one or both of the inferior thyroid arteries are absent, and in a few cases the only source of blood to the thyroid gland. Furthermore, it varies in origin, size, blood supply, and termination, and occurs in around 3.8% of the population and is 4.5 times more common in fetuses than in adults. Because of the variations and rarity, it may lead to surgical complications, particularly during tracheostomy and other airway managements.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Thyroid Ima Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Tibia(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the larger, stronger, and anterior (frontal) of the two bones in the leg below the knee in vertebrates (the other being the fibula, behind and to the outside of the tibia); it connects the knee with the ankle. The tibia is found on the medial side of the leg next to the fibula and closer to the median plane. The tibia is connected to the fibula by the interosseous membrane of leg, forming a type of fibrous joint called a syndesmosis with very little movement. The tibia is named for the flute tibia. It is the second largest bone in the human body, after the femur. The leg bones are the strongest long bones as they support the rest of the body.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tibia";
        }
    }

    return "NULL";
}

string _Medicine::Terms::TibialisAnteriorMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscle of the anterior compartment of the lower leg. It originates from the upper portion of the tibia; it inserts into the medial cuneiform and first metatarsal bones of the foot. It acts to dorsiflex and invert the foot. This muscle is mostly located near the shin. It is situated on the lateral side of the tibia; it is thick and fleshy above, tendinous below. The tibialis anterior overlaps the anterior tibial vessels and deep peroneal nerve in the upper part of the leg.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tibialis Anterior Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::TibialisPosteriorMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the most central of all the leg muscles, and is located in the deep posterior compartment of the leg. It is the key stabilizing muscle of the lower leg.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tibialis Posterior Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Tissue(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an assembly of similar cells and their extracellular matrix from the same embryonic origin that together carry out a specific function. Tissues occupy a biological organizational level between cells and a complete organ. Accordingly, organs are formed by the functional grouping together of multiple tissues.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tissue";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Toe(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the digits of the foot of a tetrapod. Animal species such as cats that walk on their toes are described as being digitigrade. Humans, and other animals that walk on the soles of their feet, are described as being plantigrade; unguligrade animals are those that walk on hooves at the tips of their toes.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Toe";
        }
    }

    return "NULL";
}

string _Medicine::Terms::TollLikeReceptor(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a class of proteins that play a key role in the innate immune system. They are single-spanning receptors usually expressed on sentinel cells such as macrophages and dendritic cells, that recognize structurally conserved molecules derived from microbes. Once these microbes have reached physical barriers such as the skin or intestinal tract mucosa, they are recognized by TLRs, which activate immune cell responses.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Toll Like Receptor";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Tongue(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a muscular organ in the mouth of a typical tetrapod. It manipulates food for chewing and swallowing as part of the digestive process, and is the primary organ of taste. The tongue\'s upper surface (dorsum) is covered by taste buds housed in numerous lingual papillae. It is sensitive and kept moist by saliva and is richly supplied with nerves and blood vessels. The tongue also serves as a natural means of cleaning the teeth. A major function of the tongue is to enable speech in humans and vocalization in other animals. The human tongue is divided into two parts, an oral part at the front and a pharyngeal part at the back. The left and right sides are also separated along most of its length by a vertical section of fibrous tissue (the lingual septum) that results in a groove, the median sulcus, on the tongue\'s surface. There are two groups of glossal muscles. The four intrinsic muscles alter the shape of the tongue and are not attached to bone. The four paired extrinsic muscles change the position of the tongue and are anchored to bone.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Tongue";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Toxicology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a scientific discipline, overlapping with biology, chemistry, pharmacology, and medicine, that involves the study of the adverse effects of chemical substances on living organisms and the practice of diagnosing and treating exposures to toxins and toxicants. The relationship between dose and its effects on the exposed organism is of high significance in toxicology. Factors that influence chemical toxicity include the dosage, duration of exposure (whether it is acute or chronic), route of exposure, species, age, sex, and environment. Toxicologists are experts on poisons and poisoning. There is a movement for evidence-based toxicology as part of the larger movement towards evidence-based practices. Toxicology is currently contributing to the field of cancer research, since some toxins can be used as drugs for killing tumor cells. One prime example of this is ribosome-inactivating proteins, tested in the treatment of leukemia.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Toxicology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Trachea(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a cartilaginous tube that connects the larynx to the bronchi of the lungs, allowing the passage of air, and so is present in almost all animals\' lungs. The trachea extends from the larynx and branches into the two primary bronchi. At the top of the trachea, the cricoid cartilage attaches it to the larynx. The trachea is formed by a number of horseshoe-shaped rings, joined together vertically by overlying ligaments, and by the trachealis muscle at their ends. The epiglottis closes the opening to the larynx during swallowing.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Trachea";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Trapezius(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large paired trapezoid-shaped surface muscle that extends longitudinally from the occipital bone to the lower thoracic vertebrae of the spine and laterally to the spine of the scapula. It moves the scapula and supports the arm. The trapezius has three functional parts: an upper (descending) part which supports the weight of the arm; a middle region (transverse), which retracts the scapula; and a lower (ascending) part which medially rotates and depresses the scapula.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Trapezius";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Triceps(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a large muscle on the back of the upper limb of many vertebrates. It consists of three parts: the medial, lateral, and long head. All three heads cross the elbow joint. However, the long head also crosses the shoulder joint. The triceps muscle contracts when the elbow is straightened and expands when the elbow is bent. The long head gets a further contraction when the arm is behind the torso due to how it crosses the shoulder joint.  It is the muscle principally responsible for extension of the elbow joint (straightening of the arm).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Triceps";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ulna(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a long bone found in the forearm that stretches from the elbow to the smallest finger, and when in anatomical position, is found on the medial side of the forearm. It runs parallel to the radius, the other long bone in the forearm. The ulna is usually slightly longer than the radius, but the radius is thicker. Therefore, the radius is considered to be the larger of the two.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ulna";
        }
    }

    return "NULL";
}

string _Medicine::Terms::UlnarArtery(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "is the main blood vessel, with oxygenated blood, of the medial aspects of the forearm. It arises from the brachial artery and terminates in the superficial palmar arch, which joins with the superficial branch of the radial artery. It is palpable on the anterior and medial aspect of the wrist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ulnar Artery";
        }
    }

    return "NULL";
}

string _Medicine::Terms::UlnarNerve(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In human anatomy, the ulnar nerve is a nerve that runs near the ulna bone. The ulnar collateral ligament of elbow joint is in relation with the ulnar nerve. The nerve is the largest in the human body unprotected by muscle or bone, so injury is common. This nerve is directly connected to the little finger, and the adjacent half of the ring finger, innervating the palmar aspect of these fingers, including both front and back of the tips, perhaps as far back as the fingernail beds.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ulnar Nerve";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Ureter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();


    string model = llm_model;
    string definition = "The ureters are tubes made of smooth muscle that propel urine from the kidneys to the urinary bladder. In the human adult, the ureters are usually 20-30 cm (8-12 in) long and around 3-4 mm (0.12-0.16 in) in diameter. The ureter is lined by urothelial cells, a type of transitional epithelium, and has an additional smooth muscle layer in third closest to the bladder that assists with peristalsis.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ureter";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Urethra(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a tube that connects the urinary bladder to the urinary meatus for the removal of urine from the body of both females and males. In human females and other primates, the urethra connects to the urinary meatus above the vagina, whereas in marsupials, the female\'s urethra empties into the urogenital sinus. Females use their urethra only for urinating, but males use their urethra for both urination and ejaculation. The statical urethral sphincter is a striated muscle that allows voluntary control over urination. The internal sphincter, formed by the involuntary smooth muscles lining the bladder neck and urethra, receives its nerve supply by the sympathetic division of the autonomic nervous system. The internal sphincter is present both in males and females.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Urethra";
        }
    }

    return "NULL";
}

string _Medicine::Terms::UrinaryBladder(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a hollow muscular organ in humans and other vertebrates that stores urine from the kidneys before disposal by urination. In the human the bladder is a hollow muscular, and distensible organ that sits on the pelvic floor. Urine enters the bladder via the ureters and exits via the urethra. The typical human bladder will hold between 300 and 500 ml (10.14 and 16.91 fl oz) before the urge to empty occurs, but can hold considerably more.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Urinary Bladder";
        }
    }

    return "NULL";
}

string _Medicine::Terms::UrinarySystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as the renal system or urinary tract, consists of the kidneys, ureters, bladder, and the urethra. The purpose of the urinary system is to eliminate waste from the body, regulate blood volume and blood pressure, control levels of electrolytes and metabolites, and regulate blood pH. The urinary tract is the body\'s drainage system for the eventual removal of urine. The kidneys have an extensive blood supply via the renal arteries which leave the kidneys via the renal vein. Each kidney consists of functional units called nephrons. Following filtration of blood and further processing, wastes (in the form of urine) exit the kidney via the ureters, tubes made of smooth muscle fibres that propel urine towards the urinary bladder, where it is stored and subsequently expelled from the body by urination (voiding). The female and male urinary system are very similar, differing only in the length of the urethra.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Urinary System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Urology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as genitourinary surgery, is the branch of medicine that focuses on surgical and medical diseases of the male and female urinary-tract system and the male reproductive organs. Organs under the domain of urology include the kidneys, adrenal glands, ureters, urinary bladder, urethra, and the male reproductive organs (testes, epididymis, vas deferens, seminal vesicles, prostate, and penis).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Urology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Uterus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();
    string model = llm_model;
    string definition = "or womb is a major female hormone-responsive secondary sex organ of the reproductive system in humans and most other mammals. In the human, the lower end of the uterus, the cervix, opens into the vagina, while the upper end, the fundus, is connected to the fallopian tubes. It is within the uterus that the fetus develops during gestation. In the human embryo, the uterus develops from the paramesonephric ducts which fuse into the single organ known as a simplex uterus. The uterus has different forms in many other animals and in some it exists as two separate uteri known as a duplex uterus.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Uterus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Vaccine(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a biological preparation that provides active acquired immunity to a particular disease. A vaccine typically contains an agent that resembles a disease-causing microorganism and is often made from weakened or killed forms of the microbe, its toxins, or one of its surface proteins. The agent stimulates the body\'s immune system to recognize the agent as a threat, destroy it, and to further recognize and destroy any of the microorganisms associated with that agent that it may encounter in the future. Vaccines can be prophylactic (to prevent or ameliorate the effects of a future infection by a natural or \"wild\" pathogen), or therapeutic (e.g., vaccines against cancer, which are being investigated).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vaccine";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Vagina(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In mammals, the vagina is the elastic, muscular part of the female genital tract. In humans, it extends from the vulva to the cervix. The outer vaginal opening is normally partly covered by a membrane called the hymen. At the deep end, the cervix (neck of the uterus) bulges into the vagina. The vagina allows for sexual intercourse and birth. It also channels menstrual flow (menses), which occurs in humans and closely related primates as part of the monthly menstrual cycle.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vagina";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VasDeferens(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "is part of the male reproductive system of many vertebrates; these ducts transport sperm from the epididymis to the ejaculatory ducts in anticipation of ejaculation. It is a partially coiled tube which exits the abdominal cavity through the inguinal canal.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vas Deferens";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VastusIntermediusMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "arises from the front and lateral surfaces of the body of the femur in its upper two-thirds, sitting under the rectus femoris muscle and from the lower part of the lateral intermuscular septum. Its fibers end in a superficial aponeurosis, which forms the deep part of the quadriceps femoris tendon.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vastus Intermedius Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VastusLateralisMuscle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "is the largest and most powerful part of the quadriceps femoris, a muscle in the thigh. Together with other muscles of the quadriceps group, it serves to extend the knee joint, moving the lower leg forward. It arises from a series of flat, broad tendons attached to the femur, and attaches to the outer border of the patella. It ultimately joins with the other muscles that make up the quadriceps in the quadriceps tendon, which travels over the knee to connect to the tibia. The vastus lateralis is the recommended site for intramuscular injection in infants less than 7 months old and those unable to walk, with loss of muscular tone.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vastus Lateralis Muscle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VastusMedialis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "is an extensor muscle located medially in the thigh that extends the knee. The vastus medialis is part of the quadriceps muscle group.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vastus Medialis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Vein(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "are blood vessels in the circulatory system of humans and most other animals that carry blood towards the heart. Most veins carry deoxygenated blood from the tissues back to the heart; exceptions are those of the pulmonary and fetal circulations which carry oxygenated blood to the heart. In the systemic circulation, arteries carry oxygenated blood away from the heart, and veins return deoxygenated blood to the heart, in the deep veins. There are three sizes of veins: large, medium, and small. Smaller veins are called venules, and the smallest the post-capillary venules are microscopic that make up the veins of the microcirculation. Veins are often closer to the skin than arteries. Veins have less smooth muscle and connective tissue and wider internal diameters than arteries. Because of their thinner walls and wider lumens they are able to expand and hold more blood. This greater capacity gives them the term of capacitance vessels. At any time, nearly 70% of the total volume of blood in the human body is in the veins. In medium and large sized veins the flow of blood is maintained by one-way (unidirectional) venous valves to prevent backflow. In the lower limbs this is also aided by muscle pumps, also known as venous pumps that exert pressure on intramuscular veins when they contract and drive blood back to the heart.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vein";
        }
    }

    return "NULL";
}

//string _Medicine::Terms::InferiorVenaCava(string text)
//{
//    string model = llm_model;
//    string definition = "a large vein that carries the deoxygenated blood from the lower and middle body into the right atrium of the heart. It is formed by the joining of the right and the left common iliac veins, usually at the level of the fifth lumbar vertebra. The inferior vena cava is the lower (\"inferior\") of the two venae cavae, the two large veins that carry deoxygenated blood from the body to the right atrium of the heart: the inferior vena cava carries blood from the lower half of the body whilst the superior vena cava carries blood from the upper half of the body. Together, the venae cavae (in addition to the coronary sinus, which carries blood from the muscle of the heart itself) form the venous counterparts of the aorta.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Inferior Vena Cava";
//        }
//    }
//
//    return "NULL";
//}

//string _Medicine::Terms::SuperiorVenaCava(string text)
//{
//    string model = llm_model;
//    string definition = "the superior of the two venae cavae, the great venous trunks that return deoxygenated blood from the systemic circulation to the right atrium of the heart. It is a large-diameter (24 mm) short length vein that receives venous return from the upper half of the body, above the diaphragm. Venous return from the lower half, below the diaphragm, flows through the inferior vena cava. The SVC is located in the anterior right superior mediastinum. It is the typical site of central venous access via a central venous catheter or a peripherally inserted central catheter. Mentions of \"the cava\" without further specification usually refer to the SVC.";
//    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
//    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
//    response = _Parsers::LLM::json_parser(response);
//    vector<string> words = _Utilities::String2Words(response);
//    words = _Utilities::RemovePunctuationArr(words);
//    words = _Utilities::ArrayOfStringsToLowercase(words);
//
//    for (int x = 0; x < words.size(); x++)
//    {
//        if (words[x] == "yes")
//        {
//            return "Superior Vena Cava";
//        }
//    }
//
//    return "NULL";
//}

string _Medicine::Terms::Ventricle(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of two large chambers located toward the bottom of the heart that collect and expel blood towards the peripheral beds within the body and lungs. The blood pumped by a ventricle is supplied by an atrium, an adjacent chamber in the upper heart that is smaller than a ventricle. Interventricular means between the ventricles (for example the interventricular septum), while intraventricular means within one ventricle (for example an intraventricular block). In a four-chambered heart, such as that in humans, there are two ventricles that operate in a double circulatory system: the right ventricle pumps blood into the pulmonary circulation to the lungs, and the left ventricle pumps blood into the systemic circulation through the aorta.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ventricle";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VentricularSystem(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a set of four interconnected cavities known as cerebral ventricles in the brain. Within each ventricle is a region of choroid plexus which produces the circulating cerebrospinal fluid (CSF). The ventricular system is continuous with the central canal of the spinal cord from the fourth ventricle, allowing for the flow of CSF to circulate. All of the ventricular system and the central canal of the spinal cord are lined with ependyma, a specialised form of epithelium connected by tight junctions that make up the blood-cerebrospinal fluid barrier.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Ventricular System";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Venule(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a very small vein in the microcirculation that allows blood to return from the capillary beds to drain into the venous system via increasingly larger veins. Post-capillary venules are the smallest of the veins with a diameter of between 10 and 30 micrometres (µm). When the post-capillary venules increase in diameter to 50µm they can incorporate smooth muscle and are known as muscular venules. Veins contain approximately 70% of total blood volume, while about 25% is contained in the venules. Many venules unite to form a vein.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Venule";
        }
    }

    return "NULL";
}

string _Medicine::Terms::SpinalColumn(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also known as the vertebral column, spine or backbone, is the core part of the axial skeleton in vertebrates. The vertebral column is the defining and eponymous characteristic of the vertebrate. The spinal column is a segmented column of vertebrae that surrounds and protects the spinal cord. The vertebrae are separated by intervertebral discs in a series of cartilaginous joints. The dorsal portion of the spinal column houses the spinal canal, an elongated cavity formed by the alignment of the vertebral neural arches that encloses and protects the spinal cord, with spinal nerves exiting via the intervertebral foramina to innervate each body segment.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Spinal Column";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Virology(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the study of viral - submicroscopic, parasitic particles of genetic material contained in a protein coat - and virus-like agents. It focuses on the following aspects of viruses: their structure, classification and evolution, their ways to infect and exploit host cells for reproduction, their interaction with host organism physiology and immunity, the diseases they cause, the techniques to isolate and culture them, and their use in research and therapy. Virology is considered to be a subfield of microbiology or of medicine.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Virology";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VisualAcuity(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "commonly refers to the clarity of vision, but technically rates an examinee\'s ability to recognize small details with precision. Visual acuity is dependent on optical and neural factors, i.e., (1) the sharpness of the retinal image within the eye, (2) the health and functioning of the retina, and (3) the sensitivity of the interpretative faculty of the brain.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Visual Acuity";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VisualCortex(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "The visual cortex of the brain is the area of the cerebral cortex that processes visual information. It is located in the occipital lobe. Sensory input originating from the eyes travels through the lateral geniculate nucleus in the thalamus and then reaches the visual cortex. The area of the visual cortex that receives the sensory input from the lateral geniculate nucleus is the primary visual cortex, also known as visual area 1 (V1), Brodmann area 17, or the striate cortex. The extrastriate areas consist of visual areas 2, 3, 4, and 5 (also known as V2, V3, V4, and V5, or Brodmann area 18 and all Brodmann area 19).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Visual Cortex";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VisualFieldTest(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an eye examination that can detect dysfunction in central and peripheral vision which may be caused by various medical conditions such as glaucoma, stroke, pituitary disease, brain tumours or other neurological deficits. Visual field testing can be performed clinically by keeping the subject\'s gaze fixed while presenting objects at various places within their visual field. Simple manual equipment can be used such as in the tangent screen test or the Amsler grid. When dedicated machinery is used it is called a perimeter.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Visual Field Test";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VisualPerception(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the ability to interpret the surrounding environment using light in the visible spectrum reflected by the objects in the environment. This is different from visual acuity, which refers to how clearly a person sees (for example \"20/20 vision\"). A person can have problems with visual perceptual processing even if they have 20/20 vision.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Visual Perception";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VitalSigns(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of the four to six most important medical signs that indicate the status of the body\'s vital (life-sustaining) functions. These measurements are taken to help assess the general physical health of a person, give clues to possible diseases, and show progress toward recovery. The normal ranges for a person\'s vital signs vary with age, weight, gender, and overall health. There are four primary vital signs: body temperature, blood pressure, pulse (heart rate), and breathing rate (respiratory rate), often notated as BT, BP, HR, and RR. However, depending on the clinical setting, the vital signs may include other measurements called the \"fifth vital sign\" or \"sixth vital sign\". Vital signs are recorded using the LOINC internationally accepted standard coding system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vital Signs";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VitaminD(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of fat-soluble secosteroids responsible for increasing intestinal absorption of calcium, magnesium, and phosphate, and many other biological effects. In humans, the most important compounds in this group are vitamin D3 (also known as cholecalciferol) and vitamin D2 (ergocalciferol).";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vitamin D";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Vitrectomy(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a surgery to remove some or all of the vitreous humor from the eye. Anterior vitrectomy entails removing small portions of the vitreous humor from the front structures of the eye-often because these are tangled in an intraocular lens or other structures. Pars plana vitrectomy is a general term for a group of operations accomplished in the deeper part of the eye, all of which involve removing some or all of the vitreous humor-the eye\'s clear internal jelly.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vitrectomy";
        }
    }

    return "NULL";
}

string _Medicine::Terms::VitreousBody(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the clear gel that fills the space between the lens and the retina of the eyeball (the vitreous chamber) in humans and other vertebrates. It is often referred to as the vitreous humor (also spelled humour), from Latin meaning liquid, or simply \"the vitreous\". Vitreous fluid or \"liquid vitreous\" is the liquid component of the vitreous gel, found after a vitreous detachment. It is not to be confused with the aqueous humor, the other fluid in the eye that is found between the cornea and lens.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vitreous Body";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Vulva(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "comprises mostly statical, visible structures of the female genitalia leading into the interior of the female reproductive tract. For humans, it includes the mons pubis, labia majora, labia minora, clitoris, vestibule, urinary meatus, vaginal introitus, hymen, and openings of the vestibular glands (Bartholin\'s and Skene\'s). The folds of the outer and inner labia provide a double layer of protection for the vagina (which leads to the uterus). Pelvic floor muscles support the structures of the vulva. Other muscles of the urogenital triangle also give support. Blood supply to the vulva comes from the three pudendal arteries. The internal pudendal veins give drainage. Afferent lymph vessels carry lymph away from the vulva to the inguinal lymph nodes. The nerves that supply the vulva are the pudendal nerve, perineal nerve, ilioinguinal nerve and their branches. Blood and nerve supply to the vulva contribute to the stages of sexual arousal that are helpful in the reproduction process. Following the development of the vulva, changes take place at birth, childhood, puberty, menopause and post-menopause. There is a great deal of variation in the appearance of the vulva, particularly in relation to the labia minora. The vulva can be affected by many disorders, which may often result in irritation. Vulvovaginal health measures can prevent many of these. Other disorders include a number of infections and cancers. There are several vulval restorative surgeries known as genitoplasties, and some of these are also used as cosmetic surgery procedures.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Vulva";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Waist(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the part of the abdomen between the rib cage and hips. Normally, it is the narrowest part of the torso. Waistline refers to the horizontal line where the waist is narrowest, or to the general appearance of the waist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Waist";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Wart(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "typically small, rough, hard growths that are similar in color to the rest of the skin. They typically do not result in other symptoms, except when on the bottom of the feet, where they may be painful. While they usually occur on the hands and feet, they can also affect other locations. One or many warts may appear. They are not cancerous.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Wart";
        }
    }

    return "NULL";
}

string _Medicine::Terms::WeberTest(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a screening test for hearing performed with a tuning fork. It can detect unilateral (one-sided) conductive hearing loss (middle ear hearing loss) and unilateral sensorineural hearing loss (inner ear hearing loss). The test is named after Ernst Heinrich Weber (1795-1878). Conductive hearing ability is mediated by the middle ear composed of the ossicles: the malleus, the incus, and the stapes. Sensorineural hearing ability is mediated by the inner ear composed of the cochlea with its internal basilar membrane and attached cochlear nerve (cranial nerve VIII). The outer ear consisting of the pinna, ear canal, and ear drum or tympanic membrane transmits sounds to the middle ear but does not contribute to the conduction or sensorineural hearing ability save for hearing transmissions limited by cerumen impaction (wax collection in the ear canal). The Weber test has had its value as a screening test questioned in the literature.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Weber Test";
        }
    }

    return "NULL";
}

string _Medicine::Terms::WernickeKorsakoffSyndrome(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the combined presence of Wernicke encephalopathy (WE) and alcoholic Korsakoff syndrome. Due to the close relationship between these two disorders, people with either are usually diagnosed with WKS as a single syndrome. It mainly causes vision changes, ataxia and impaired memory.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Wernicke Korsakoff Syndrome";
        }
    }

    return "NULL";
}

string _Medicine::Terms::WernickesArea(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "one of the two parts of the cerebral cortex that are linked to speech, the other being Broca\'s area. It is involved in the comprehension of written and spoken language, in contrast to Broca\'s area, which is involved in the production of language. It is traditionally thought to reside in Brodmann area 22, which is located in the superior temporal gyrus in the dominant cerebral hemisphere, which is the left hemisphere in about 95% of right-handed individuals and 60% of left-handed individuals.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Wernicke's Area";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Whiplash(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a non-medical term describing a range of injuries to the neck caused by or related to a sudden distortion of the neck associated with extension, although the exact injury mechanisms remain unknown. The term \"whiplash\" is a colloquialism. \"Cervical acceleration-deceleration\" (CAD) describes the mechanism of the injury, while the term \"whiplash associated disorders\" (WAD) describes the injury sequelae and symptoms.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Whiplash";
        }
    }

    return "NULL";
}

string _Medicine::Terms::WhiteBloodCell(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "also called leukocytes or leucocytes, are the cells of the immune system that are involved in protecting the body against both infectious disease and foreign invaders. All white blood cells are produced and derived from multipotent cells in the bone marrow known as hematopoietic stem cells. Leukocytes are found throughout the body, including the blood and lymphatic system.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "White Blood Cell";
        }
    }

    return "NULL";
}

string _Medicine::Terms::WhiteMatter(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "areas of the central nervous system (CNS) that are mainly made up of myelinated axons, also called tracts. Long thought to be passive tissue, white matter affects learning and brain functions, modulating the distribution of action potentials, acting as a relay and coordinating communication between different brain regions.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "White Matter";
        }
    }

    return "NULL";
}

string _Medicine::Terms::WorkingMemory(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a cognitive system with a limited capacity that can hold information temporarily. Working memory is important for reasoning and the guidance of decision-making and behavior. Working memory is often used synonymously with short-term memory, but some theorists consider the two forms of memory distinct, assuming that working memory allows for the manipulation of stored information, whereas short-term memory only refers to the short-term storage of information. Working memory is a theoretical concept central to cognitive psychology, neuropsychology, and neuroscience.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Working Memory";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Wrist(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "In human anatomy, the wrist is variously defined as 1) the carpus or carpal bones, the complex of eight bones forming the proximal skeletal segment of the hand; (2) the wrist joint or radiocarpal joint, the joint between the radius and the carpus and; (3) the anatomical region surrounding the carpus including the distal parts of the bones of the forearm and the proximal parts of the metacarpus or five metacarpal bones and the series of joints between these bones, thus referred to as wrist joints. This region also includes the carpal tunnel, the anatomical snuff box, bracelet lines, the flexor retinaculum, and the extensor retinaculum. As a consequence of these various definitions, fractures to the carpal bones are referred to as carpal fractures, while fractures such as distal radius fracture are often considered fractures to the wrist.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Wrist";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Xanthoma(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a deposition of yellowish cholesterol-rich material that can appear anywhere in the body in various disease states. They are cutaneous manifestations of lipidosis in which lipids accumulate in large foam cells within the skin. They are associated with hyperlipidemias, both primary and secondary types.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Xanthoma";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Yaws(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a tropical infection of the skin, bones and joints caused by the spirochete bacterium Treponema pallidum pertenue. The disease begins with a round, hard swelling of the skin, 2 to 5 centimeters in diameter. The center may break open and form an ulcer. This initial skin lesion typically heals after three to six months. After weeks to years, joints and bones may become painful, fatigue may develop, and new skin lesions may appear. The skin of the palms of the hands and the soles of the feet may become thick and break open. The bones (especially those of the nose) may become misshapen. After five years or more large areas of skin may die, leaving a scar.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Yaws";
        }
    }

    return "NULL";
}

string _Medicine::Terms::YellowFever(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a viral disease of typically short duration. In most cases, symptoms include fever, chills, loss of appetite, nausea, muscle pains particularly in the back, and headaches. Symptoms typically improve within five days. In about 15% of people, within a day of improving the fever comes back, abdominal pain occurs, and liver damage begins causing yellow skin. If this occurs, the risk of bleeding and kidney problems is increased.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Yellow Fever";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ZellwegerSpectrumDisorders(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "a group of rare disorders that create the same disease process. The subdivisions of this spectrum are hyperpipecolic acidemia, Infantile Refsum disease, neonatal adrenoleukodystrophy (NALD), and Zellweger syndrome. It can also be referred to as Peroxisomal Biogenesis Disorders, Zellweger Syndrome Spectrum, NALD, Cerebrohepatorenal Syndrome, and ZSS. It can affect many body organs, including the kidneys, eyes, and hearing. It is named after Hans Zellweger.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Zellweger Spectrum Disorders";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ZikaVirus(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "It is spread by daytime-active Aedes mosquitoes, such as A. aegypti and A. albopictus. Its name comes from the Ziika Forest of Uganda, where the virus was first isolated in 1947. Zika virus shares a genus with the dengue, yellow fever, Japanese encephalitis, and West Nile viruses. Since the 1950s, it has been known to occur within a narrow equatorial belt from Africa to Asia. From 2007 to 2016, the virus spread eastward, across the Pacific Ocean to the Americas, leading to the 2015-2016 Zika virus epidemic.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Zika Virus";
        }
    }

    return "NULL";
}

string _Medicine::Terms::Zoonosis(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "an infectious disease caused by a pathogen (an infectious agent, such as a bacterium, virus, parasite or prion) that has jumped from a non-human animal (usually a vertebrate) to a human. Typically, the first infected human transmits the infectious agent to at least one other human, who, in turn, infects others.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Zoonosis";
        }
    }

    return "NULL";
}

string _Medicine::Terms::ZygomaticBone(string text)
{
    string start_time;
    string end_time;
    stringstream oss;

    auto entry = time(nullptr);
    auto tm1 = *localtime(&entry);

    oss << put_time(&tm1, "%d-%m-%Y_%H-%M-%S");
    start_time = oss.str();

    string model = llm_model;
    string definition = "the human skull, the zygomatic bone (cheekbone or malar bone) is a paired irregular bone which articulates with the maxilla, the temporal bone, the sphenoid bone and the frontal bone. It is situated at the upper and lateral part of the face and forms the prominence of the cheek, part of the lateral wall and floor of the orbit, and parts of the temporal fossa and the infratemporal fossa. It presents a malar and a temporal surface; four processes (the frontosphenoidal, orbital, maxillary, and temporal), and four borders.";
    string prompt = "Check if the following definition matches the following and limit the response to yes or no:  " + definition + " " + text;
    string response = _LLM2::OllamaAPI(model, prompt, "NULL");
    response = _Parsers::LLM::json_parser(response);
    vector<string> words = _Utilities::String2Words(response);
    words = _Utilities::RemovePunctuationArr(words);
    words = _Utilities::ArrayOfStringsToLowercase(words);
    _WorkingMemory::wm_llm_history_funct(prompt, response);

    // Calculate the duration of the command being executed
	oss.clear();
    auto entry2 = time(nullptr);
    auto tm2 = *localtime(&entry2);

    oss << put_time(&tm2, "%d-%m-%Y_%H-%M-%S");
    end_time = oss.str();

    long long llm_duration = _Time::GetDuration(start_time, end_time);

    // Encrypt all information while sitting in the database??? YES
    // todo: Save llm data to database
    //vector<string> encrypted = _Encryption::aesEncrypt(response);

    _Thought::Thought2SQL(model, prompt, response, "NULL", "NULL", to_string(llm_duration));

    for (int x = 0; x < words.size(); x++)
    {
        if (words[x] == "yes")
        {
            return "Zygomatic Bone";
        }
    }

    return "NULL";
}

